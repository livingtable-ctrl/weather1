# Fix AUD-0012: _poll_pending_orders / _count_open_live_orders can silently lose a still-open live order

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0012`; full record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active exposure today. Note this finding was surfaced later in the audit pipeline (by an independent final-review pass, not the original 19-pass sweep) — the audit report itself flags this as the strongest candidate for "a new HIGH finding the final review added," so treat it with the same weight as the others despite arriving via a different route.

**This finding shares its underlying root-cause theme with AUD-0001, AUD-0002, AUD-0005, and AUD-0009** (see their prompts in this same `audit/handoff_prompts/` directory) — the general pattern of live-position visibility gaps in the risk-control chain. It's most directly related to **AUD-0009 specifically** (same function family — `_count_open_live_orders` — same underlying table, a different flavor of "wrong query" bug). Read AUD-0009's prompt alongside this one; they may be fixable together, and the audit's own recommendation for AUD-0009 already points at reusing `get_live_pnl_summary`'s correctly-scoped query pattern, which is directly relevant here too.

## The problem

**Root cause:** `get_recent_orders(limit=N)` is `SELECT * FROM orders ORDER BY placed_at DESC LIMIT ?` with **no `live` WHERE clause**. Both `_poll_pending_orders` (limit=200) and `_count_open_live_orders` (limit=500) fetch this fixed-size mixed paper+live window and filter for live+pending *in Python, after truncation* — a real still-pending live order can fall entirely outside the window once enough other (mostly paper) orders accumulate afterward. The correct pattern already exists elsewhere in the same file: `get_live_pnl_summary`'s `open_count` metric uses an unlimited, properly-scoped `WHERE live=1` query — mirror that, don't invent a new pattern.

**Evidence (E2, independently re-run this session):** `audit/reproductions/pass11_stale_pending_window_eviction.py` confirmed: after 250 interleaved paper orders, the live pending order becomes invisible to `_poll_pending_orders`'s selection (limit=200); `_count_open_live_orders`'s selection (limit=500) still sees it at 250 but drops to `False`/0 after 520 interleaved orders. `get_order_by_id` confirms the row **remains in the DB with `status='pending'` throughout** — this is a visibility bug, not a data-loss bug; the row is fine, the code just stops looking at it.

**Files/lines:** `execution_log.py:937-944` (`get_recent_orders`); `order_executor.py:172-175, 424-443, 3083-3092, 1610-1613`; `execution_log.py`'s `get_live_pnl_summary` (the correctly-scoped pattern to mirror — locate its exact line range as part of Process step 2).

**Financial risk:** A genuinely open live position can silently stop being polled for fill/settlement and stop being counted against `max_open_positions`, once production order volume (paper + live combined) is high enough — plausible given `REFRESH_SECS=300` (`main.py:211`) accumulating hundreds of orders over a multi-hour session.

**Recommendation (from the audit):** Replace both call sites' use of `get_recent_orders` with a dedicated `WHERE live=1 AND status='pending'` unlimited SQL query, matching `get_live_pnl_summary`'s existing pattern.

**Confidence note from the audit:** MEDIUM confidence — the mechanism is solidly E2-proven, but real-world trigger frequency depends on unobserved production order volume; don't overstate urgency beyond what the evidence supports when communicating this to anyone.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This touches a live-order/live-money/safety-gate path — full ceremony required, no LOW-tier downgrade. Non-negotiable highlights:

1. **Re-verify against live state first** — `git fetch` + check for parallel-session overlap on `execution_log.py`/`order_executor.py` (especially if AUD-0009 is being worked in the same session); re-run `pass11_stale_pending_window_eviction.py` yourself before trusting this prompt's numbers, since production order volume assumptions may have changed.
2. **Research the real code structure** — read `get_live_pnl_summary`'s exact query shape in full before writing the replacement queries for `_poll_pending_orders`/`_count_open_live_orders`, so all three end up consistent rather than three slightly different `WHERE live=1` variants.
3. **Surface real design decisions via `AskUserQuestion`** — e.g., whether to just call `get_live_pnl_summary`'s underlying query directly from both sites (removing `get_recent_orders`'s use here entirely) vs. add a new dedicated helper function; read AUD-0009's prompt first since the answer likely affects both fixes together.
7. **Write real, mutation-tested tests** — reproduce the audit's own eviction scenario as a permanent regression test (250+ interleaved paper orders, assert the live pending order stays visible after the fix); mutation-test by reverting to the limited `get_recent_orders` call (via Edit, not a string-replace script) and confirming the test then fails.
8-9. Scoped test run (`execution_log.py`, `order_executor.py` test files), then lint/mypy.
11. **Independent opus review at `effort: high` before push.** The fix-of-the-fix needs its own independent review too if the first review finds anything.
13. Address every review finding regardless of severity.
14-16. Compressed-pointer memory update before commit; explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
29. Refresh `graphify-out/` (AST always; semantic `--update` too, non-LOW-tier) before committing, if it exists.

Full step list and tiering rules live in memory under `feedback-implementation-workflow` — apply all 29 steps in order. **If this memory entry isn't loaded in your session**, its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
