# Fix AUD-0005: LiveTradingGate's drawdown/streak checks read only the paper ledger, blind to real live losses

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0005`; full record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active exposure today, but this is the master risk-halt gate and would be structurally blind to real losses the moment live trading is enabled.

**This finding shares one root cause with AUD-0001, AUD-0002, AUD-0009, and AUD-0012** (see their prompts in this same `audit/handoff_prompts/` directory): `execution_log.db` was added as a second ledger for live trades, but the risk-control chain was never systematically re-pointed at it. The audit's recommendation: **"This is a single coordinated fix, not five independent ones."** Read the other four prompts before starting.

## The problem

**Root cause:** `is_paused_drawdown()`/`is_streak_paused()`/`get_daily_pnl()` were written when `paper.py`'s ledger was the only trade record, and were never extended to `execution_log.db` once live trading became a separate ledger. `e5331a8d` (2026-08-17) stopped routing live fills into the paper ledger, cementing the blindness.

**Evidence (E2, reproduced this session):** Confirmed via direct read that all 5 sub-checks of `LiveTradingGate.check()` read only `paper.py` state; even `is_daily_loss_halted(client)`'s `client` param is used only to mark paper positions to market, not to query live losses. Independently ran `audit/reproductions/verify_pass20_gate_paper_only.py` this session (throwaway tempdir, monkeypatched paths): after simulating 10 real $95 live losses via `execution_log.add_live_loss` ($950 total, correctly reported by `get_today_live_loss()`), `paper.is_paused_drawdown()` and `paper.is_streak_paused()` both **still returned `False`**.

**Files/lines:** `trading_gates.py:72-110` (`LiveTradingGate.check()`); `paper.py:575-598, 626-635, 2436-2454, 2664-2754, 3383-3437`; `order_executor.py:1567-1602`.

**Financial risk:** A slow live-account bleed across multiple days, or a genuine consecutive-loss streak, is never caught by either check. Note: `_place_live_order` does have its own separate, working single-day check via `execution_log.get_today_live_loss()` — distinct from and not covering this multi-day/streak gap, so don't mistake that existing check for a fix already in place.

**Recommendation (from the audit):** Add live-account-aware drawdown/streak checks sourced from `execution_log.db`, called from `LiveTradingGate.check()` or `_place_live_order()`.

**Audit's own noted limitation:** did not search for an out-of-band monitor that might independently catch a slow multi-day live bleed — check for one before assuming this is the only safety net.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This touches a live-order/live-money/safety-gate path — full ceremony required, no LOW-tier downgrade. Non-negotiable highlights:

1. **Re-verify against live state first** — `git fetch` + check whether AUD-0001/0002's fix already introduced a shared live-position/live-loss helper this finding should reuse; re-run the reproduction script yourself before trusting this prompt's numbers.
2. **Research the real code structure** — read `is_paused_drawdown()`/`is_streak_paused()`/`get_daily_pnl()` in full, and `_place_live_order`'s existing single-day check (`execution_log.get_today_live_loss()`) as the closest sibling pattern to mirror for the multi-day/streak version.
3. **Surface real design decisions via `AskUserQuestion`** — e.g., what "streak" means for live trades specifically (consecutive losing days? consecutive losing trades?) if it differs from the paper-side definition already coded; don't guess at a definition that affects when real trading halts.
7. **Write real, mutation-tested tests** — replicate the audit's own reproduction as a permanent test (10 simulated live losses → drawdown/streak checks correctly return `True`); mutation-test via Edit, not a string-replace script.
8-9. Scoped test run (`trading_gates.py`, `paper.py` risk-check test files), then lint/mypy.
11. **Independent opus review at `effort: high` before push** — this is the master risk-halt gate; do not skip. The fix-of-the-fix needs its own independent review too if the first review finds anything.
13. Address every review finding regardless of severity.
14-16. Compressed-pointer memory update before commit; explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
29. Refresh `graphify-out/` (AST always; semantic `--update` too, non-LOW-tier) before committing, if it exists.

Full step list and tiering rules live in memory under `feedback-implementation-workflow` — apply all 29 steps in order. **If this memory entry isn't loaded in your session**, its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
