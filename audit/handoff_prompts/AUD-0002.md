# Fix AUD-0002: _auto_place_trades' position-count/VaR/concentration gates seeded only from the paper ledger

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0002`; full record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active exposure today, but this activates the moment live trading is enabled, and unlike the other findings in this group, this one gates the **automated** cron/watch trading loop specifically (not manual `cmd_order`).

**This finding shares one root cause with AUD-0001, AUD-0005, AUD-0009, and AUD-0012** (see their prompts in this same `audit/handoff_prompts/` directory): `execution_log.db` was added as a second ledger for live trades, but the risk-control chain was never systematically re-pointed at it. The audit's recommendation: **"This is a single coordinated fix, not five independent ones."** Read the other four prompts before starting — AUD-0001 in particular covers the manual `cmd_order` path's identical blindness in `check_position_limits()`, and the audit explicitly flagged that this finding (`_auto_place_trades`) is the automated-path counterpart of that same gap.

## The problem

**Root cause:** No `execution_log`/`_get_live_open_positions()` call exists anywhere inside `_auto_place_trades` (`order_executor.py:2294-3340`) feeding `MAX_CONCURRENT_POSITIONS`, per-date concentration caps, or the VaR gate — only a same-cycle in-memory append (lines 3037-3053) covers the current cycle's own fills, not positions opened in prior cycles.

**Evidence (E1):** Grepped the full function body for `execution_log`/`_get_live_open_positions` calls — none found (only `was_ordered_recently`/`was_traded_today`/`log_order`/`get_today_live_loss` appear). The existing regression test `pass11_state_repro.py::test_auto_place_trades_open_trades_list_is_paper_only` passed, confirming the gap as currently-correct-but-untested-as-a-gap behavior. Cross-checked against `get_today_live_spend()` (`execution_log.py:439-486`) — the analogous dollar-spend blind spot that was already fixed — confirming this is the same bug class left unfixed specifically for position count/VaR.

**Files/lines:** `order_executor.py:2364, 2402-2443, 2916-2941, 3037-3053`.

**Financial risk:** Real portfolio risk is uncapped on every automated cron/watch cycle whenever any standing live position already exists from a prior cycle.

**Recommendation (from the audit):** Merge `execution_log`-derived live open positions into `_open_trades_list` before evaluating these gates — ideally via the same shared helper AUD-0001's fix uses, so the manual and automated paths don't drift into two different partial implementations.

**Duplicates absorbed by this finding during the audit's dedup pass:** AUD-0089, AUD-0090.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This touches a live-order/live-money/safety-gate path — full ceremony required, no LOW-tier downgrade. Non-negotiable highlights:

1. **Re-verify against live state first** — `git fetch` + check whether AUD-0001's fix (or another parallel session's work) already introduced the shared live-position helper this finding should reuse; re-read the current code at `order_executor.py:2294-3340` rather than trusting this prompt.
2. **Research the real code structure** — trace `_open_trades_list`'s full lifecycle within `_auto_place_trades`, and check whether AUD-0001 is being fixed in the same session (if so, reuse its helper rather than writing a second one).
3. **Surface real design decisions via `AskUserQuestion`** — e.g., whether VaR/concentration/position-count should all pull from one merged list built once per cycle, or three separate calls; don't guess.
7. **Write real, mutation-tested tests** — simulate a live position surviving from a prior cycle via `execution_log`, assert the current cycle's gates correctly see it; mutation-test the fix via Edit (not a string-replace script).
8-9. Scoped test run, then lint/mypy.
11. **Independent opus review at `effort: high` before push** — and the fix-of-the-fix needs its own independent review too if the first review finds anything.
13. Address every review finding regardless of severity.
14-16. Compressed-pointer memory update before commit; explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
29. Refresh `graphify-out/` (AST always; semantic `--update` too, non-LOW-tier) before committing, if it exists.

Full step list and tiering rules live in memory under `feedback-implementation-workflow` — apply all 29 steps in order. **If this memory entry isn't loaded in your session**, its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
