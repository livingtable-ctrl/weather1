# Fix AUD-0010: _quick_paper_buy()'s maker-order branch places real live orders but never records the fill

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0010`; full record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active exposure today, but the audit's confidence on this one is specifically "medium confidence on reachability but severe if triggered" — verify reachability carefully as your first step (see below), since this is the one HIGH finding in this batch where whether the bug can actually fire at all is itself not fully settled.

No shared root-cause group with other findings, but it's part of the same "order lifecycle / crash-recovery" cluster as AUD-0007, AUD-0008, and AUD-0013 — read those prompts too.

## The problem

**Root cause:** `_quick_paper_buy` was written and reviewed as a paper-trading helper. Its live maker-order fallback branch is functionally live-order code that never went through the `execution_log`-routing review the `cmd_order`/`order_executor` cluster received (`e5331a8d` and predecessors `bb91374f`/`105cf4ce` never touched this function). `backlog.txt` already has a RESOLVED entry (dated 2026-07-30) flagging a related concern, but it only added ticker-family shadow-gate guards, not post-fill bookkeeping — check that entry's exact scope before assuming it already covers this.

**Files/lines:** `main.py:2170-2539` (the function), `main.py:2465-2511` (the maker branch), `main.py:2494-2496` (the `place_maker_order` call).

**Recommendation (from the audit):** First confirm reachability precisely (is this function's live path actually invocable given current call sites and gates, or has something already made it dead code?), then either wire it into `execution_log` the same way `cmd_order`/`order_executor` are, or if genuinely unreachable, make that explicit (e.g. an assertion or a removed live branch) rather than leaving live-capable-but-unrecorded code sitting reachable.

**Audit's own noted limitation:** reachability was assessed at medium confidence, not fully settled — this is exactly the kind of ambiguity that step 3 of the workflow (below) exists to surface via `AskUserQuestion` rather than silently assume either direction.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This touches a live-order/live-money/safety-gate path — full ceremony required, no LOW-tier downgrade. Non-negotiable highlights:

1. **Re-verify against live state first** — `git fetch` + check for parallel-session overlap on `main.py`; re-read `main.py:2170-2539` in full and independently re-derive whether the maker branch is actually reachable from current call sites, since this prompt inherits only MEDIUM confidence on that specific point — do not treat this prompt's characterization of reachability as settled fact. Also re-check the exact scope of the RESOLVED `backlog.txt` entry from 2026-07-30 to confirm it really doesn't already cover post-fill bookkeeping.
2. **Research the real code structure** — trace every call site of `_quick_paper_buy`, and compare its maker-branch structure against `cmd_order`'s already-fixed live-order routing as the pattern to potentially mirror.
3. **Surface real design decisions via `AskUserQuestion`** — this finding has two materially different possible resolutions (wire it into `execution_log` properly, vs. confirm it's dead/unreachable and remove or assert against the live path) — this is exactly the kind of judgment call that must go through `AskUserQuestion`, not be silently decided either way.
7. **Write real, mutation-tested tests** — depending on which resolution is chosen: either a test proving a maker-branch fill now gets recorded in `execution_log`, or a test proving the live path is now provably unreachable (e.g. raises/asserts rather than silently executing); mutation-test via Edit, not a string-replace script.
8-9. Scoped test run (`main.py`'s quick-buy test files), then lint/mypy.
11. **Independent opus review at `effort: high` before push.** The fix-of-the-fix needs its own independent review too if the first review finds anything.
13. Address every review finding regardless of severity.
14-16. Compressed-pointer memory update before commit; explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
19. If `backlog.txt` gets edited (updating the existing 2026-07-30 entry with a note on this follow-up), run `python backlog_index.py` afterward and confirm the entry landed correctly in `BACKLOG_OPEN.md`.
29. Refresh `graphify-out/` (AST always; semantic `--update` too, non-LOW-tier) before committing, if it exists.

Full step list and tiering rules live in memory under `feedback-implementation-workflow` — apply all 29 steps in order. **If this memory entry isn't loaded in your session**, its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
