# Fix AUD-0001: check_position_limits() exposure caps are structurally blind to live positions

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (see Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0001`; full structured record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active financial exposure today, but this gap activates the moment live trading is enabled.

**This finding shares one root cause with AUD-0002, AUD-0005, AUD-0009, and AUD-0012** (see their own prompts in this same `audit/handoff_prompts/` directory): `execution_log.db` was added as a second ledger for live trades, but the risk-control chain was never systematically re-pointed at it — it still only reads the old paper ledger (`paper.py`). The audit's own recommendation: **"This is a single coordinated fix, not five independent ones."** Read the other four prompts before starting so the fix doesn't end up with five inconsistent partial implementations of "read live positions from execution_log." Consider whether one shared helper (e.g. `_get_live_open_positions()`, which already exists per AUD-0005's evidence) should be the single source every one of these five call sites is updated to use.

## The problem

**Root cause:** Exposure accounting was built around `paper_trades.json`, which used to also receive live fills. Commit `e5331a8d` correctly stopped that routing (live fills now go to `execution_log.db` instead) but never added the compensating read — `check_position_limits()` was never updated to also look at `execution_log`.

**Evidence (E2, reproduced this session):** All five caps (per-market, global-portfolio, city/date, directional, correlated-group) in `check_position_limits()` (`paper.py:3447-3686`) call `get_open_trades()` (`paper.py:1299-1301`) exclusively. Grepping `paper.py` for `execution_log` returns zero functional references. Reproduction (`audit/reproductions/repro_exposure_blind.py`, re-run and confirmed this session): a second live buy that should breach the 15% directional cap returns `{'ok': True, 'reason': None, 'existing_cost': 0, 'limit': 250.0}` — the cap silently doesn't see the first live position at all.

**Files/lines:** `paper.py:3447-3686` (`check_position_limits`), `paper.py:1299-1301` (`get_open_trades`), `paper.py:1598-1673` (exposure getters), `main.py:4528-4569` (`cmd_order`'s live path that this gate is supposed to protect).

**Financial risk:** Once live trading is enabled, repeated manual `cmd_order` live buys on the same city/date or correlated group can silently exceed every configured exposure cap while the CLI reports each one as "ok" — no error, no warning, just a false "ok".

**Recommendation (from the audit):** Incorporate live open positions from `execution_log` (via `_get_live_open_positions()`) into exposure accounting before comparing to caps. Already tracked in `backlog.txt` — re-check `backlog.txt` for the existing entry and its current status before starting (Process step 1).

**Duplicates absorbed by this finding during the audit's dedup pass:** AUD-0085, AUD-0086, AUD-0087, AUD-0088 (same root cause, different symptom framing — no separate action needed for those ids).

**Audit's own noted limitation:** did not re-verify the automated (non-manual) `_auto_place_trades` live path or `web_app.py` live-order paths for the same blindness — check those too while you're in this code, since AUD-0002 (the `_auto_place_trades` sibling finding) covers exactly that path.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This finding touches a live-order/live-money/safety-gate code path, so it does **not** qualify for the steps 11-12 LOW-tier downgrade — apply the full ceremony as written, all 29 steps in order. The most consequence-bearing ones to not shortcut here:

1. **Re-verify against live state first** — `git fetch` + check `origin/master`'s recent commit history for whether another parallel worktree session already touched `check_position_limits()` or the shared `_get_live_open_positions()` helper since this prompt was written; re-read the actual current code at every file:line cited above rather than trusting this prompt's transcription.
2. **Research the real code structure** before designing anything — trace all 5 cap call sites, find `_get_live_open_positions()`'s existing signature/usage (per AUD-0005's evidence it already exists), check whether AUD-0002/0005/0009/0012 are being fixed in this same session or separately.
3. **Surface real design decisions via `AskUserQuestion`** — e.g., whether to merge live positions into the same in-memory list `get_open_trades()` returns vs. a separate parallel accounting path; whether this becomes the shared helper the sibling findings also use. Don't guess.
7. **Write real, mutation-tested tests** — a test that actually opens a live position via `execution_log`, then asserts a second live buy correctly hits the cap; mutation-test by reverting the fix (via Edit, not a string-replace script) and confirming the test now fails.
8-9. Scoped test run (the `paper.py`/`execution_log.py`/exposure-cap test files), then lint/mypy.
11. **Independent opus review at `effort: high` before push** — and if that review's findings get fixed, the fix itself needs its own independent opus review too, not just self-verification.
13. Address every review finding regardless of severity.
14-16. Update memory (compressed pointer, not narrative) before commit; get explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
19. If `backlog.txt` gets edited (updating the existing entry this finding is already tracked under), run `python backlog_index.py` afterward and confirm the entry landed correctly in `BACKLOG_OPEN.md`.
29. If `graphify-out/` exists in this repo, refresh it (AST always; semantic `--update` too, since this is not LOW tier) before committing.

Full step list and the "How to apply" tiering rules live in memory under `feedback-implementation-workflow` — follow all 29 steps in order, not just the ones highlighted above. **If this memory entry isn't loaded in your session** (e.g. you're not running in this project's usual Claude Code context), its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
