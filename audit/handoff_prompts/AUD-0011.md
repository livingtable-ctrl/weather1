# Fix AUD-0011: LIVE_TRADING_RUNBOOK.md falsely claims only `watch --auto --live` can place live orders

## Context

Repo: weather1 (Kalshi weather-trading bot). Branch `claude/code-max-depth-audit-5518e9`, HEAD `d190d09dd699df5266e85650a6ddf8e2d1420891` at the time this prompt was written (2026-08-18) — re-verify this is still current before starting (Process step 1). This comes from a maximum-depth forensic audit (`audit/AUDIT_REPORT.md`, finding `AUD-0011`; full record in `audit/AUDIT_REPORT.json`). Live trading is currently dormant (`LIVE_TRADING_ENABLED` unset) — no active exposure today, but this is a documentation defect that would mislead an operator during an actual live-trading incident, so it's high-value to fix even though it isn't itself a code bug.

No shared root-cause group with other findings, but it's the same underlying misconception as AUD-0014 (the `KALSHI_ENV=prod` startup banner making the identical false claim in-app) — there's already an open `backlog.txt` entry for the banner specifically. Consider fixing both the banner (AUD-0014, if also in scope) and this runbook doc together so the same false claim doesn't get half-corrected.

## The problem

**Evidence (E1):** Read `LIVE_TRADING_RUNBOOK.md:102-104` and `:131` directly — text matches the claim exactly: *"cron never places live orders regardless of LIVE_TRADING_ENABLED — only `watch --auto --live` does"* and *"python main.py cron never places live orders — only `watch --auto --live` does."* Read `main.py:4333` (`def cmd_order(...)`) and confirmed lines 4519-4537: `_is_live = getattr(client, "base_url", None) != DEMO_BASE` (line 4528) followed by `pre_live_trade_check(client)` (line 4534) inside `if _is_live:` — a fully independent live-order path, unconditioned on the runbook's claim. Also found an existing OPEN `backlog.txt` entry (lines 1947-1952) titled *"KALSHI_ENV=prod STARTUP BANNER WRONGLY CLAIMS ONLY `watch --auto --live` CAN PLACE LIVE ORDERS — FALSE FOR cmd_order (buy/sell) ONCE LIVE_TRADING_ENABLED=true"* — confirming this is a known-but-only-partially-addressed misconception (that entry covers the startup banner, not this runbook doc).

**Files/lines:** `LIVE_TRADING_RUNBOOK.md:102-104, 131`; `main.py:4519-4537` (`cmd_order`'s independent live path, the actual current truth to document).

**Financial risk:** An operator relying on this runbook during a live-trading incident would misjudge which commands are safe to run — believing `buy`/`sell` (`cmd_order`) cannot place a live order when it in fact can, once `LIVE_TRADING_ENABLED=true` and pointed at prod.

**Recommendation (from the audit):** Correct the runbook to describe the real precondition (`LIVE_TRADING_ENABLED=true` + a `PROD_BASE`-pointed client), not a single named command.

## Process — follow the 29-step implementation workflow from memory (`feedback-implementation-workflow`) exactly, in order

This is a documentation-only fix, but it directly describes a live-order/live-money/safety-gate surface, so treat it as full ceremony rather than assuming the LOW tier — a wrong safety document is arguably worse than no document, and the tiering rule in memory explicitly does not downgrade anything "touching a live-order, live-money, or safety-gate code path" (a runbook describing exactly that surface counts). Non-negotiable highlights:

1. **Re-verify against live state first** — `git fetch` + check for parallel-session overlap on `LIVE_TRADING_RUNBOOK.md`; re-read `main.py:4519-4537` directly to confirm the described live-order precondition is still accurate (line numbers may have shifted if other findings in this batch — especially AUD-0001/0003, both of which touch this same function — get fixed first).
3. **Surface real design decisions via `AskUserQuestion`** — e.g., whether to also fix AUD-0014 (the in-app banner with the identical false claim) in the same change, since they're the same underlying misconception described in two places; don't silently decide the scope.
6. **Follow existing documentation conventions** — check how other precondition-style callouts are phrased elsewhere in the runbook and match that style rather than inventing new phrasing.
7. Documentation changes don't need mutation-tested code tests, but DO verify the corrected text by actually re-tracing `cmd_order`'s live path end-to-end one more time immediately before finalizing the wording — a documentation fix that's subtly still wrong is worse than the original.
11. **Independent opus review at `effort: high` before push** — even for a documentation-only change, given it describes a live-trading safety precondition and this task's blast radius (an operator's mental model during a real incident) justifies not downgrading review rigor just because no code changed.
13. Address every review finding regardless of severity.
14-16. Compressed-pointer memory update before commit; explicit confirmation before commit/push; `git fetch` + rebase-if-diverged immediately before the actual push.
19. If `backlog.txt` gets edited (e.g. cross-referencing this fix from the existing AUD-0014-adjacent entry), run `python backlog_index.py` afterward and confirm it landed correctly in `BACKLOG_OPEN.md`.
29. Refresh `graphify-out/` if it exists — for a docs-only change this may reasonably qualify for the LOW tier's AST-only refresh (no semantic `--update`) per step 29's own tiering; use judgment and state the reasoning if downgrading this one step.

Full step list and tiering rules live in memory under `feedback-implementation-workflow` — apply all 29 steps in order, downgrading only step 29's semantic-refresh half if this stays a pure documentation change with no code touched. **If this memory entry isn't loaded in your session**, its full text is preserved at `C:\Users\thesa\.claude\projects\C--Users-thesa-claude-kalshi\memory\feedback_implementation_workflow.md` — read it directly rather than proceeding without it.
