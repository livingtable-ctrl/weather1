# Post-Merge Whole-Program Audit — weather1

**Tree audited:** HEAD `f4291771` = `origin/master` (verified via fetch). Batch window `8bdff1e5^..f4291771`: 41 linear commits, 2026-08-18 → 2026-08-23, **zero merge commits** (evil-merge/remerge-diff check trivially satisfied).
**Method:** Pass 1 collision map → 10 subsystem review agents (opus) over all 51 production modules + frontend, the two 10k+-line files chunked → Pass 4 tests/mutations + Pass 5 hygiene/docs/lint → prior-audit baseline spot-check (15 items) → **every CRITICAL/HIGH candidate adversarially re-verified by an independent skeptic agent with executed reproductions**. Severity tiers below are post-verification. Coverage manifest: `audit/POST_MERGE_REVIEW_COVERAGE.md` (all 51 modules `done`; one explicitly bounded item — see Pass 4).
**Report-only. Nothing was fixed. The main clone's working tree was never modified** (all mutation testing ran in a throwaway detached worktree in the session scratchpad, since removed).

---

## VERDICT

**Safe to resume normal (paper) operation.** No finding causes wrong or unsafe behavior in the current configuration: `LIVE_TRADING_ENABLED` is unset (verified absent from `.env` and environment, enforced solely at `trading_gates.py:69` on every order path — mutation-confirmed), `ENABLE_MICRO_LIVE` is a hard `False` source literal, and the paper pipeline's tests pass.

**Before enabling live trading, CR-1 and H-1 must be fixed** (CR-1 sits directly on the live-order crash-recovery path with a designed `os._exit` kill window landing in it), and the MEDIUM items marked ⚡ below should be batched with them. Two data-hygiene items (M-19, M-20) are worth doing this week regardless.

**Merge quality:** genuinely good. Zero dropped or miscombined batch changes anywhere (mechanical added-line sweeps in groups A, B, D, E-main), zero same-AUD-fixed-twice, 15/15 spot-checked "resolved" items verified actually fixed with no later clobbering, and no test was weakened or deleted to pass. The two real cross-batch collisions found are CR-1 and M-1.

---

## CRITICAL

### CR-1 — Batch-22 × batch-23 collision: pre-logged idempotency key no longer matches the wire key, so crash recovery marks REAL live orders `'failed'`
`order_executor.py:1347-1349` vs `:1378` (`_replace_live_order`); `order_executor.py:2084-2086` vs `:2133` (`_exit_live_position`). Batches `7dbd7ee3` (batch-22 item 2) × `f704c581` (batch-23 item 1). **Skeptic verdict: CONFIRMED, with executed repro.**

Batch-22's crash-recovery mechanism pre-logs `client_order_id` before placement so a crash between pre-log and result can be reconciled against the exchange. Batch-23 then scoped `place_order`'s `cycle` to `f"{cycle}:replace:{id}"` / `f"{cycle}:exit:{log_id}"` — but left the pre-log `compute_client_order_id(...)` calls on the bare `cycle`. Two independent sessions reproduced the inequality by executing the real function (replace: `eb1ad79c…`≠`dc6097830…`; exit: `b04fd7ea…`≠`b8942458…`; `_place_live_order` control: equal). The exit case is structural: the wire key depends on `log_id`, which doesn't exist until after the pre-log commits.

**Failure scenario:** process dies in the pre-log→result window — and `cron.py:3108-3156` arms a watchdog that `os._exit(1)`s at 720s, landing in exactly this window by design. Recovery: pending → sent (wrong cid preserved, `:553-558`) → `claim_sent_order` → unknown → `_find_order_by_client_id(wrong_cid)` completes all 3 passes, finds nothing, `uncertain=False` → `:844-849` writes `'failed'`.
- **Replace:** a real live BUY (possibly resting or filled) becomes an **untracked live position** — no protective exits, no settlement accounting, no `add_live_loss` — and after a 12h forecast-cycle rollover the ticker is fully re-orderable (duplicate position). The untracked-position harm is immediate and unmitigated.
- **Exit:** a real protective SELL (possibly filled) is recorded `'failed'`; the position row keeps `settled_at=NULL` forever, so the exit scanner places a **fresh real SELL every cycle** against an already-sold position (opening opposite-side exposure) and permanently consumes a `max_open_positions` slot.

Scope corrections from verification: the `OrderStatusUnknownError` path is NOT affected (handlers store the correct wire cid) — the bug is confined to the hard-crash/kill window; `main.py`'s own pre-log sites (`cmd_order`, `_quick_paper_buy`) are correct. No test covers pre-log-cid == wire-cid for these two sites (the batch-22 F7 test pattern covers only `cmd_order`/`_quick_paper_buy`). Corroborating: `_amend_live_order:1485-1501`'s comment shows the batch-23 author reasoned about this interaction for amend and missed these two. Latent today (live trading disabled) — **must be fixed before any live enablement.**

---

## HIGH

### H-1 — Batch-29 re-opened the import-time-validation regression batch-09 closed: a Settings-menu edit can brick the entire CLI, including the documented emergency halt
`config.py:339-403`, `main.py:165`, `main.py:6833-6941`. Batch `f21f8ffd` (batch-29), contradicting the documented batch-09 decision at `backlog.txt:22265-22283`. **Skeptic verdict: CONFIRMED, empirically reproduced.**

`_bot_config = _load_config()` runs at module import (`main.py:165`, before the excepthook at `:193` — no crash.log). Batch-29 added 7 range checks to `validate()`. The interactive Settings menu validates only against its own format strings and never calls `validate()` before writing `.env`. Four menu-writable values were reproduced to brick every subsequent `py main.py <anything>` with a raw ValueError: `KALSHI_FEE_RATE=0` or `1` (menu says inclusive "0-1", validate requires exclusive — and it's labeled "reference only", the bot pays $0), `KALSHI_MAKER_FEE_RATE=1`, `MIN_EDGE=0` (paper_min_edge 0.04 > 0), `MIN_EDGE>STRONG_EDGE`. Batch-29 added a bespoke `"0-1 excl"` format for `MAX_DAILY_LOSS_PCT` after review caught this exact class — and didn't audit the other rows.

**What dies:** `kill`, `resume`, `setup` (the .env-repair wizard that exists to fix a broken `.env`), `cron`, `emos-deactivate` — including the runbook's documented Option-A emergency halt (`LIVE_TRADING_RUNBOOK.md:202-213`). Scheduled cron exits 1 raw with no alert; open live positions would get no protective-exit scans. Mitigations: trading also stops (loss of control, not runaway), and the web dashboard is unaffected (`web_app.py` never imports `main`; `/api/halt` writes the kill switch itself) — but the dashboard is not the documented emergency path and its halt button fails silently (M-8). Related riders: `MAX_DAILY_SPEND=0`/`MAX_SAME_DAY_SPEND=0` are legitimate "spend nothing" sentinels (all 7 consumers verified, enforcement reads `utils.py:308-312` env-direct) that validate now rejects — but they're hand-edit-only, so LOW on their own (L-rider). `paper_min_edge` being disk-derived means a bot-written `walk_forward_params.json` value above an unset-default `MIN_EDGE` could also brick startup (currently safe: 0.04 vs 0.15).

---

## MEDIUM

⚡ = fix before/with live enablement.

**M-1 ⚡ (batch-24 self-collision) — Halt push-alert edge consumed on delivery failure.** `alerts.py:84-106` persists the false→true halt transition before delivery; `notify.py:532-551`'s rollback restores only the cooldown. Total delivery failure at halt engagement permanently eats that engagement's alert (early return next cycle). Skeptic scope-down: `drawdown`/`daily_loss` have a redundant second observer (different flag key, same cooldown → same-cycle retry); only `anomaly` (cron.py:1356, 1442) is single-observer; and this deployment currently has **zero push channels configured** — only the plyer desktop toast can succeed — so today it means "drops the one toast retry". Becomes real the day a webhook/ntfy channel is configured; fix (persist the flag only after ≥1 channel succeeds, or roll it back too) belongs with live enablement. Batches `651bbe3f`.

**M-2 ⚡ — `record_live_settlement` has no `settled_at IS NULL` guard** (`execution_log.py:918-933`; caller `order_executor.py:1191-1192` unconditionally `add_live_loss`). Skeptic downgraded from HIGH: the claimed cron-vs-watch race is unreachable (cron never calls `_poll_pending_orders`; exit fills are impossible on finalized markets), but narrower races survive (cron's `_settle_recovered_exit_order` vs watch's stale snapshot; two concurrent watch processes), and the danger direction is real: a **winning** position credited twice makes the live daily-loss brake *looser* than reality; the unguarded UPDATE also silently overwrites an early-exit's realized pnl (tax CSV / summary corruption). Mutation testing independently showed the guarded sibling's guard has **no killing test** (M-23). Pre-existing writer; every sibling was hardened in-window.

**M-3 ⚡ (904ea92d × batch-30) — `_release_cron_lock` fails open to `unlink()` when its best-effort mutex isn't held** (`cron.py:464-480`): a torn/denied read defaults `owner_pid=None` → falls through to deleting a possibly-fresh lock another process just wrote — the exact H2 scenario batch-30's check exists to prevent. `cmd_watch --auto --live` holds this lock. Safe default is to skip the unlink on an unreadable read.

**M-4 ⚡ (batch-23) — Per-attempt exit key removed the only server-side dedup against concurrent cron+watch double-exit** (`order_executor.py:2108-2133`). Verified: both processes derive identical `cycle` internally; concurrency is real and AUD-0013-documented; no other serialization exists (`LivePositionStore.exit` has no row claim; the `settled_at` guard fires only after the second SELL executed). Skeptic reframe: the hazard pre-existed — batch-23 removed an accidental, price-equality-dependent mitigation; the deliberate tradeoff is documented. **Actionable fix: an atomic position-row claim before `place_order` in `_exit_live_position` — which also closes CR-1's exit half and M-2's residual race.**

**M-5 (batch-20 × batch-24 contradiction) — `last_full_scan` stamped by cycles that never scanned** (`cron.py:3249-3252` keys off the `sameday_only` *argument*, not the `_full_scan` *outcome* computed 50 lines up; batch-24 deliberately froze the sibling `.cron_last_run` under the kill switch, this file got no equivalent). Kill-switch-aborted, black-swan-aborted, and crashing cycles all record full scans → all three staleness alarms (`main.py:10894-10906`, `cron_full_scan_gap`, `cron_gap`) permanently silent while cron fails every cycle.

**M-6 (batch-20 × 1f618417) — `--sameday-only` inflates the rain-arb shadow denominator** (`trade_cycle.py:314-350`): the sameday filter runs before `find_violations`, and no rain ladder is ever a sameday market — yet `cycles_observed` (the documented denominator for the future graduation decision, `backlog.txt:8060`) still increments. Every sameday cycle deflates the violation rate the graduation decision will read.

**M-7 (batch-25) — `api_resume`'s `unlink()` lacks `missing_ok`** (`web_app.py:2104-2106`; the sibling line 15 rows down has it), racing `main.py:425`'s kill-switch park → `FileNotFoundError` → 500 → the parked-copy cleanup at `:2121-2124` never runs → **kill switch silently re-arms after the override window** — the exact M5 failure batch-25 wrote those lines to prevent. Same pattern at `api_override_clear` (`web_app.py:2516-2518`) racing cron's expiry auto-clear (HTML 500, cosmetic).

**M-8 — Dashboard halt/resume/kill buttons discard the server response** (`App.jsx:238, 1675, 2152, 2156`; verified present in the shipped `static/dist` bundle). Batch-25 added a real 500 path to `/api/halt` that no client code reads; failures are silent, the ⛔ badge lags up to 60s. Skeptic scope-down from HIGH: the CLI is the documented kill procedure and a watching operator sees the badge not transition. Trivial fix (`.then` → existing `addToast`).

**M-9 — `/api/close-position` lacks the kill-switch and TRADING_PAUSED gates its sibling order route has, plus any price cross-check** (`web_app.py:3127-3169`), and `ee22c44c` added an operator-typed manual exit price feeding straight into `proceeds`/`pnl`/`balance` → drawdown tier, peak balance, graduation `total_pnl`. Price-check absence pre-existing (`backlog.txt:20408`); **the missing gates are untracked**.

**M-10 — `/api/paper-order`'s entry-price cross-check silently disabled when `parse_market_price` raises** (`web_app.py:2909-2962`): city/date bind first, so the fail-closed guard passes while `_mkt_prices_dash` stays None and the ±0.15 deviation check is skipped — the guard batch-26 and `ee22c44c` both depend on. Misleading log line.

**M-11 — Mock data survives real empty responses in the dashboard** (`useData.js:433` opportunities — 7 fabricated signals with live Approve buttons under a real "Last scan" header; `:107`/`App.jsx:586-596` — the graduation Brier gate bar renders MOCK 0.151 and paints green whenever the real value is null). The `.length`-vs-`!= null` bug `ee22c44c` fixed for positions/closedTrades, unapplied here. Server fails closed on the mock tickers, so decision-corrupting display rather than order risk.

**M-12 (batch-28 follow-through) — The live METAR lock-in calibration file is still the pre-contamination-fix fit** (`data/metar_lockout_calibration.json`, a=b=0.22619… bit-identical — the degenerate self-feeding shape; fitted 08-20, fix landed 08-22). Applied on the live entry path (`weather_markets.py:13407-13430`) and settlement force-close path. Self-heals at the next weekly cron retrain, but two downstream conclusions were derived from the contaminated fit and must be re-derived after a clean refit: `settlement_monitor.py:352-357`'s "≥0.80 gate is unreachable" claim and `ml_bias.py:571-640`'s AUD-0038 ceiling model (also backlog L4 / L22908 reasoning). **Note: the "unreachable gate" claim is also what currently caps D-group's T-ticker finding at LOW (L-13) — verify it still holds post-refit.**

**M-13 — `calibration.py` trio (untouched by the window, live evidence):** (a) `:131-134` the improvement-gate rejection path omits the `_uncalibrated` flag — `data/seasonal_weights.json`'s `summer` entry is exactly ⅓/⅓/⅓ with no flag **right now, in August**, suppressing the hardcoded days-out schedule for tier-3 markets; (b) `:384-436` `calibrate_and_save` preserves hand-tuned weights for condition only — one data-thin retrain permanently overwrites city (`{}` on disk now) and seasonal; (c) `:310-323` `calibrate_condition_weights` lacks the shadow-condition-type exclusion its sibling `_load_rows` has — a shadow family with 60 settled rows silently gains a live blend-weight entry with no graduation gate.

**M-14 — Between-bracket calibration hole (architectural, pre-existing; skeptic-confirmed structurally and understated, downgraded on exposure).** 100% of between METAR-lock trades price off the raw, never-validated `_dynamic_lock_in_confidence` (0.72 base + hour term; clearance moves ≤1.35pp): every correction stage excludes them (`weather_markets.py:13386, 13452, 13491, 13064`) and — three more exclusions than originally found — the sameday T fit, the settlement calibrator, and `tracker.brier_score()` all exclude them too, with the last one feeding `paper._dynamic_kelly_cap`: **the Kelly cap governing between trades is set by a Brier score that structurally cannot see them.** No measurement isolates between and none alerts on it. Exposure today: 1 shadow prediction since the path was armed 08-09, zero real trades — latent until first real volume. (The old n=70 Brier-0.28 evidence is from the deleted pre-June implementation and must not be cited.) Related, already tracked: the HIGH-market non-monotone NO-lock gap (BACKLOG_OPEN L26470) — the skeptic's data (93% predicted → 50% actual on n=6 NO-locks) is new supporting evidence for that entry.

**M-15 — `load_member_quarantine_state` fails open on a transient read error** (`weather_markets.py:3541-3556`), returning `{}` and re-admitting a quarantined ensemble member into the live blend mid-scan (siblings all preserve last-known-good cache with comments saying why). Also causes cache-tag mismatch → full per-market refetch, and different markets in one scan can be blended from different model sets.

**M-16 — `_compute_persistence_prob` never received either of the window's two `_metar_lock_in` hardenings** (`weather_markets.py:6750-6774`; `9eb2f154` touched this exact function and its own note flagged the gap): no stale-extreme/live combine (AUD-0016 class — cached 86 vs live 91 on "above 88.5" → 0.309 vs 0.691, 0.15 blend weight → ~5.7pt wrong-side shift) and no per-observation local-date guard (00:15 local hands yesterday's 23:53 reading to today).

**M-17 — Prewarm cache defects in `weather_markets.py` (pre-existing, each guarded by a sibling path in the same file):** `:1801-1807` seasonal ECMWF weight keyed off scan month not target month (the precip block 350 lines down documents exactly why that's wrong; prewarm wins the cache so the wrong value is what trades across season boundaries); `:2206-2246` temperature blend lacks the "every model contributed" guard its precip sibling has — a CB-open mid-loop writes a single-model blend over a complete 3-model entry for a full cycle TTL, indistinguishable in logs.

**M-18 — Weather-data fetch layer:** `nws.py:242-246` (batch-13) — `validate_nws_response` only type-checks `properties`, so `{"properties": {}}` credits the circuit breaker and caches an empty forecast for 3600s; `metar.py:168` — the `reportTime` fallback can only ever raise `TypeError` on the real payload shape (repro'd), landing between the negative-cache writes → repeated ~21s HTTP calls; `metar.py:296-307` — `_extract_obs_time`'s naive-string path silently misdates observations via system-local `astimezone` (latent; wrong-trade direction); `mos.py:191` — UTC `ftime` rows filtered by city-local date → daily extreme over a misaligned window (identified in a backlog resolution note but **never filed**); `climate_indices.py:627` — `apply_pdo_pna_correction` drops the caller's target month (same defect class as the already-fixed `get_indices`); `climatology.py:89/133` — unguarded `json.load` reachable under `_sigma_lock` → all cities silently revert to static sigma at DEBUG level.

**M-19 — `cleanup_data_dir` deletes two files this window introduced** (`main.py:808-851`, runs on *every* invocation): the AUD-0026 phantom-live-position sentinel `execution_log_unsettled_exit_rows.json` (written only on failure, mtime never refreshes → gone in 2 days while the dangerous DB row survives) and `rain_arb_shadow_observations.json` (any ≥2-day cron pause — e.g. kill switch — plus one CLI invocation deletes the multi-week graduation history). `2315636d` added `member_quarantine.json` to `_PERMANENT_DATA_FILES` for exactly this reason in the same window; these two were missed. **Cheap, do now.**

**M-20 — `data/walk_forward_params.json` is test-fixture output in the real data dir** (skeptic side-finding): it claims `n_folds: 6`, `saved_at` 2026-08-23 (today), but the real corpus can produce **0 folds** (3 distinct months < 4 required) — so the live `PAPER_MIN_EDGE = 0.04` gating all paper placement is currently sourced from a test run that wrote the real `data/` (the known manual-scripts-bypass-isolation hazard). The related `_find_optimal_min_edge` metric bug is L-14. **Delete/regenerate the file; do now.**

**M-21 — `cloud_backup.backup_data`'s new `all_readable` return is discarded at its only caller** (`cloud_backup.py:257` ← batch-25; `cron.py:3019`): a permanently failing WAL-safe `execution_log.db` backup surfaces as one log warning per cycle — the silent-backup-failure shape batch-25 was written to eliminate, one layer up.

**M-22 — Settlement-monitor restart truncates still-valid signals** (`settlement_monitor.py:638` seeds from disk with `max_age_minutes=120`, then full-file-overwrites; cron reads with 720; a scheduled run is ~310 min, so a restart >2h in drops signals cron would still act on).

**M-23 — Test/infra gaps (Pass 4/5):** (a) mutation survivor — removing the `settled_at IS NULL` guard from `record_live_early_exit`'s unconditional branch passes 281 tests across the four most relevant files (no killing test for the guard class M-2 belongs to); (b) `tests/test_disputed_row_guard.py` + `test_isoformat_cutoff_guard.py` walk `.venv` (no exclusion/dot-rule) and crash on joblib's big5 fixture → **both disputed-row guard tests are RED on the deployment checkout** (pre-existing; passes on venv-less checkouts, so batches/CI never saw it); (c) `.venv` is missing `properscoring` despite `requirements.txt:16` → 22 EMOS tests fail as errors (production degrades gracefully at `main.py:7633`; install it); (d) `test_safe_io`'s concurrent-writers test fails under load — evidence the default 0.5s replace budget batch-30 raised for `paper.py` only is insufficient for the other irreplaceable-state callers; (e) CI runs ubuntu-only, so every Windows-only concurrency-guard test is skipped where it gates master; (f) `pdf_report.py`: zero tests, web-reachable, excluded from the coverage gate; (g) `frontend/node_modules` lacks vitest (`npm test` fails on the live tree; suite passes 29/29 when installed).

**M-24 — Runbook Part 2's gate verification can never print its documented PASS** (`LIVE_TRADING_RUNBOOK.md:120-129`): bare `python -c` with no `load_dotenv` → `KALSHI_ENV` reads "demo" → always BLOCKED. Fails safe but unusable as written, and invites shell-var workarounds. (Part 4's snippet does call `load_dotenv` — inconsistent.) Also `README.md:95` documents a nonexistent `main.py order` command (real: `buy`/`sell`); survived batch-11's accuracy sweep; only doc/dispatch mismatch found across all three docs vs 74 commands.

**M-25 — `cmd_settings` KALSHI_ENV edit desyncs the menu banner from the live client in the unsafe direction** (`main.py:6939-6973`): banner flips to `[DEMO]` immediately, but the session's `client.base_url` — which every live gate keys off — stays PROD; a real order can be placed under a DEMO banner. (demo→prod direction fail-safe.)

**M-26 — `_load_live_config` doesn't catch `OSError`** (`main.py:2474-2499`; call inside `cmd_watch`'s `while True` at `:4235` whose only handler is KeyboardInterrupt): a transient AV-scan `PermissionError` kills the persistent watch process — the exact failure mode AUD-0008 fixed for the sibling block 200 lines down. Adjacent: no defaults merge → missing `daily_loss_limit` fails open to `float("inf")` on batch-22's manual-live gates; a valid-JSON non-dict raises `AttributeError`.

**M-27 — Kill-switch `.tmp` restore races an in-flight override** (`main.py:299-345` runs unconditionally before the cron lock; a scheduled cron firing during an operator's authorized override restores the switch mid-cycle and halts it, with a duplicate alert and a misleading "restored" print from the first process). Fail-closed direction. Batches `904ea92d`×`651bbe3f`×`077052ad`.

**M-28 — Discord webhook URLs (bearer credentials) logged at WARNING on failure** (`notify.py:322`), and batch-24 both rerouted all safety alerts through the sender and made the rollback retry every cycle during an outage → `bot.log` accumulates the full secret repeatedly. No redaction helper exists in the repo. Latent (no webhook configured today).

**M-29 — `cmd_weekly_summary` filters "settled this week" by `entered_at`, not `settled_at`** (`main.py:10292-10316`) — weekly P&L, win rate, best/worst are wrong in both directions.

**M-30 — Monte-Carlo VaR correlation is side-blind** (`monte_carlo.py:487`): correlates win indicators, not weather, so mixed YES/NO cross-city positions are modeled positively correlated when truly anti-correlated. Conservative direction for that pairing; the limitation is documented on `run_stress_test` but not on the function feeding the live pre-trade VaR gate.

**M-31 — Regime Kelly boost is climatology-blind (pre-existing, skeptic-confirmed at MEDIUM):** `regime.py:58,67` absolute thresholds → `confidence_boost=1.20` via an **unclamped** multiply into `_price_and_size` (`weather_markets.py:8542-8543`), plus a second consumer (`:12862` blend-weight shift — that one gated, the Kelly boost gated by nothing). Verified live: Phoenix/Vegas summer KXHIGH sits squarely in the firing band (sigma 3.2–3.5, means >95) on ordinary days; the winter cold_snap half is weakened (MSP/CHI winter sigma 7–8.4 > the 5.0 cutoff); real delta is often +4.3% (vs the 1.15 blocking_high fallback), full +20% only in the 3≤std<5 band. Untouched by any batch, not in backlog.

---

## LOW / INFO (compressed; full detail in the per-agent findings)

- **L-1** ACIS mem-cache pins the stale-fallback for process lifetime and freezes `end_year` across New Year (`acis_precip.py:190`, `acis_snow.py:201`); unit guards fail open if `monthly_units` is absent (`:364`/`:361`).
- **L-2** `hurricane_climatology.py:42-45` dated HURDAT2 URLs rotate yearly → silent stale-cache serving below the staleness-warning threshold.
- **L-3** `forecast_cache.py:223` locale-codepage read vs utf-8 writes (same at `climatology.py:89/133`, acis); "LRU" docstring is FIFO.
- **L-4** `notify.py:41-43` `NOTIFY_CHANNELS` parsed without strip/lower → "discord, email" silently drops email (latent).
- **L-5** `cron.py:664-691` naive `date.today()` (sole exceptions in the file; batch-29's tz sweep missed); `cmd_loop` naive local time incl. the 21:00 auto-settle trigger (`main.py:1247-1318` — wrong after a UTC-host VM move); `cmd_export` UTC tax year (`main.py:5016`).
- **L-6** Cron-lock `heartbeat` field written once, never updated — the no-psutil fallback reasons on lock age, not heartbeat age (`cron.py:412, 394-406`); `started_at` default 0 lets the 24h backstop override a live holder on a valid-JSON-but-keyless lock (low reachability); `lp.write_text` non-atomic.
- **L-7** `output_formatters.py:342-345` confusion-matrix off-diagonals transposed (display-only; P/R/F1 correct; pre-existing).
- **L-8** `cron.py:1829` log-rotation `Path.replace()` — knowingly allowlisted cross-batch deferral, 4 later cron batches passed over it.
- **L-9** `cmd_menu`'s kill-switch banner and `_check_cron_staleness` both swallow all exceptions silently (`main.py:8625-8674, 10875-10908`) — fails open on safety displays; arb placement block's bare `except: pass` (`main.py:2221-2403`) incl. naked-leg unwind.
- **L-10** `_finalize_cancel` fallback nulls `fill_quantity`/`response` (non-COALESCE) → a partially-filled position can go untracked (pre-existing F9-era; same class the window closed elsewhere); `_place_live_order` step 3 bare `config["max_trade_dollars"]` subscript (KeyError crash on a hand-edited file); micro-live gate gaps (skeptic-downgraded to LOW: hard-disabled source literal + spend bounded upstream; the `.get("daily_loss_limit", 0.0)` fail-open is real and opposite its sibling).
- **L-11** `_poll_pending_orders` has exactly one caller (`cmd_watch`) — on a cron-only host, GTC cancels, settlement, and the live daily-loss brake never run (relevant to the VM move plan; becomes ⚡ if the VM shape is cron-only live).
- **L-12** Stale cross-batch comments: `_reconcile_live_positions` "unpaginated" note vs batch-23's pagination (and batch-23's own comment predating AUD-0025's landing); batch-13's commit message claims a `kalshi_client` fix that isn't in the commit (accurately tracked as PARTIALLY RESOLVED L23595).
- **L-13** Settlement-monitor T-ticker branch settles from the instantaneous evening METAR reading (`settlement_monitor.py:551-584`) — skeptic-downgraded to LOW: with the current fitted calibration the ≥0.80 force-close gate is mathematically unreachable (max 0.766 YES / 0.595 NO, independently recomputed), the monitor has never been scheduled or run (zero signals in 1.88MB of cron.log), and the gap is already OPEN in backlog.txt L4-70 with the same numbers. **Novel residue to append to that entry: `metar_lockout_calibration.json` is not git-tracked, so a fresh host (VM move) fails open to the raw 0.857 confidence.** Interlocks with M-12's refit.
- **L-14** `_find_optimal_min_edge` maximizes YES-settlement rate, not win rate (`backtest.py:923`; 125/198 settled trades are NO-side) — skeptic-downgraded: all 198 edges ≥0.15 so every threshold ties and 0.04 is selected under either metric, and the real corpus currently yields 0 folds so the function never runs via cron. One-line fix (carry `side`); see M-20 for the file it would have written.
- **L-15** `deactivate_emos` unlinks params before the snapshot restore and discards the restore's bool; `cmd_emos_deactivate` prints unconditional success; `t_pinned` structurally blind post-deactivate — skeptic-downgraded (snapshot survives → rerun self-heals; a WARNING does log; weekly retrain partially heals; EMOS never activated). Related observation: `temperature_scale.json`'s `below` is `{"T":1.0,"n":14}` with no marker — the exact zero-calibration shape current code can't write; below-condition probs are uncalibrated now; provenance unconfirmed.
- **L-16** Runbook Part 5 names `data/cron.lock`; real file is `data/.cron.lock` (`paths.py:57`) — skeptic-downgraded (del prints "Could Not Find"; the claimed non-self-heal case actually self-heals; worst case a bounded backstop delay + false-negative diagnosis). Three occurrences, one-line fix.
- **L-17** Misc: `param_sweep` sweep-vs-loader range mismatch still live (has audit repro); `_KXTEMP_HOURLY_CITY` not tied to `KNOWN_WEATHER_SERIES` (adding a 6th hourly series bypasses both the shadow gate and the hourly branch; registries in sync today, no test asserts it); hurricane next-event UTC-vs-ET off-by-one (shadow-only); `web_app` SSE threads never exit; `/api/paper-order` reads the kill switch via a different import than every sibling route; NO-side deviation check skipped when `yes_bid==1.0`; past-date clamp dodges the per-date slot cap (`web_app.py:3053`); `quantity` parse outside try; App.jsx dead Enter-handler, divide-by-qty NaN; `restore_window.ps1` orphaned; `docs/grade_audit/outputs/paper.py.md` documents the removed `spread_kelly_multiplier`; `BACKLOG_OPEN.md` staleness — L24116 should flip to RESOLVED (verified fixed), AUD-0047 L23259 needs its Resolution section, L23899 verified fixed and closable; `schema_validator.py` does not cover the tracked `data/*.json` at all (no schema validation exists for them — the audit doc's premise was wrong); `audit_settlement`'s hourly branch returns True without a rowcount check; `cloud_backup` UTC/local prune boundary + nested pre-restore snapshots; `ab_test.py` dormant (no live constructor); `PEAK_BALANCE_PATH` unused; `BotConfig.enable_micro_live` reads an env var the enforcement point ignores (dashboard could display "enabled" while dormant); `cmd_setup` echoes `KALSHI_KEY_ID` to scrollback.

---

## Pass 4/5 results

- **Tests:** batches 1–5 of 6 (≈140/162 files): 4,008 passed; **zero regressions attributable to the merge**. Failures: 2 pre-existing (.venv-scan guard, M-23b), 22 environment (`properscoring`, M-23c). Batch 6 stopped early at the user's instruction; most of its files have independent green evidence from scoped agent/baseline runs (settlement 45, gates+execution_log 122, web 70, weather_markets+metar 341, trade_validation 16). Frontend: 29/29.
- **Mutation tests (throwaway worktree):** 7 safety-critical behaviors — LIVE_TRADING_ENABLED gate, order side/price mapping, settlement membership-vs-magnitude, METAR lock-in combine, min-edge gate, mid-batch kill-switch: all **killed**. One **survivor**: the `settled_at IS NULL` guard (M-23a). All edits precisely reverted; worktree removed.
- **Lint:** full-tree pre-commit (correct interpreter) — ruff, ruff-format, mypy all pass; hooks rewrote nothing.
- **Baseline:** 15/15 spot-checked resolved AUD items VERIFIED-FIXED, zero clobbered (including the two highest-risk: batch-30 layered inside AUD-0006's mutex; batch-27/28 extended AUD-0016's fix).
- **Backlog:** `BACKLOG_OPEN.md` byte-identical to a clean regeneration (71 entries); staleness items listed in L-17.
- **Data files:** all 4 tracked `data/*.json` parse; weights sum to 1.0; `_uncalibrated` sentinel correctly guarded at every consumer.
- **Stray files:** none committed outside `audit/` (`run_and_sleep.bat` was *deleted*, not added; `static/dist` in sync with `frontend/src`).

## Recommended fix order

1. **CR-1** (pass `time_in_force`-consistent scoped cycles to the pre-log sites, or claim-then-derive; add the missing pre-log==wire tests for replace/exit) — before any live enablement.
2. **H-1** (wrap `main.py:165` for the exempted subcommands per batch-09's documented pattern, and align the Settings-menu formats with `validate()`).
3. **M-4's row-claim fix** (also closes CR-1's exit half and M-2's residual race), then M-2's guard + M-23a's test.
4. This week regardless: **M-19** (two `_PERMANENT_DATA_FILES` entries), **M-20** (delete the fixture-written `walk_forward_params.json`), M-12 refit + re-derive, `pip install properscoring`, `.venv` exclusion in the two guard tests, M-7's `missing_ok=True`.
