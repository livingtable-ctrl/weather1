# Pass 8 — Mathematics (Sections 19 & 51) — Independent Verification

## Finding 1: METAR settlement-lag force-close gate structurally unreachable

Read settlement_monitor.py in full (L1-500) and metar.py L1-90.

- Confirmed cron.py L1471 gate is literally `if _sig_conf >= 0.80 and _sig_ticker in _open_by_ticker:` — but note this only closes PAPER trades (`paper.close_paper_early`/`paper.get_open_trades`, cron.py L1455-1482), not live orders. Not claimed otherwise by the finding, but worth noting for context.
- Confirmed `_calibrate_metar_settlement_confidence` (settlement_monitor.py L277-359) is applied to the T-ticker signal's confidence BEFORE it's written to the signal dict that cron.py reads (settlement_monitor.py L468-471) — i.e. cron.py's 0.80 check really does see the calibrated number, not the raw one.
- Confirmed `_dynamic_lock_in_confidence` (metar.py L31-57) is hard-bounded to round(...,3) clipped to [0.72, 0.97].
- Confirmed a=b=0.2262, c=0.4001 are real fitted-on-real-data coefficients, not placeholders: tests/test_ml_bias.py L187-241 (`test_fit_metar_calibration_on_real_repo_data`) fits these exact values from a hardcoded 33-row real production dataset (27 YES-lock-direction / 6 NO-lock-direction rows, 11 settled_yes=0 minority class >= the 10-row EPV floor at ml_bias.py L393/462). tests/test_weather_markets.py L2390-2414 independently uses the same coefficients in a separate regression test.
- Independently re-ran (not just read) the audit's own audit/reproductions/metar_calibration_bound_check.py: max YES-lock calibrated confidence = 0.7661 at raw=0.97; max NO-lock = 0.5954 at raw confidence=0.97 (raw_p_yes=0.03). Both < 0.80. I additionally verified analytically that apply_metar_calibration is monotonic increasing in raw_prob when a=b>0 (sigmoid of a monotonic-increasing logit-like expression), so the fine sweep's max is provably the true max, not just an empirically-observed one.
- Additionally computed (own script, not from the audit's file) the max |delta| between raw and calibrated confidence across the full [0.72,0.97] domain for both directions: YES-lock delta up to ~0.20, NO-lock delta up to ~0.385 — both comfortably under `_METAR_CORRECTION_LIMIT = 0.60` (settlement_monitor.py L324), meaning the correction is never skipped by its own magnitude cap anywhere in the real input domain. This closes a gap the original finding didn't check: it's not enough that calibration *can* pull the number below 0.80, it also needs to always actually apply.
- Caveat NOT in the original finding's limitations: `_calibrate_metar_settlement_confidence` fails closed to the RAW (uncalibrated) confidence if `_load_metar_calibration()` returns None (no calibration file loaded yet, e.g. before the first successful fit/retrain). In that state the raw confidence (up to 0.97) would reach the 0.80 gate uncalibrated, i.e. the gate would NOT be dead. The "structurally unreachable" framing is therefore contingent on a calibration file already existing and being loaded, not an absolute property of the code. Evidence suggests this condition is met in real production (commit docstring's "verified against real production coefficients," cluster K's weekly auto-retrain, and the fitted-from-real-data test above), but this worktree has no live calibration cache file on disk to directly confirm the file's current existence server-side.
- Independently grepped the main clone's bot.log (C:\Users\thesa\claude kalshi\bot.log, 4100 lines) for "SETTLEMENT LAG signal" — 0 matches, corroborating (not just trusting) the original finding's claim that this mechanism has not fired in production. This is my own E3 runtime evidence, distinct from the original pass's cron.log/schtasks check which I could not independently reproduce (no cron.log found; schtasks not queried by me — read-only audit, and this is a Windows dev box not necessarily the production scheduler host).

Verdict: CONFIRMED. Upgraded evidence basis (I independently re-derived and re-ran the math myself, plus found an additional angle — the correction-cap sensitivity check — that further supports the claim, plus one legitimate unstated caveat about calibration-file existence that doesn't overturn the finding but sharpens its scope).

## Finding 2: trade_cycle.py net_edge fallback mismatch vs validate()

- Confirmed trade_cycle.py L467-471 fallback chain: `net_edge = analysis.get("net_edge"); if None: analysis.get("edge"); if still None: 0.0`.
- Confirmed order_executor.py L2011-2015 (`_validate_trade_opportunity`): `edge = opp.get("net_edge"); if edge is None: edge = 0.0` — no fallback to `opp.get("edge")`, exactly as claimed.
- Confirmed trade_cycle.py's own inline comment (L658-671) already documents this exact divergence and the "more permissive, never less" reasoning, word-for-word consistent with the finding.
- Independently verified reachability claim: grepped weather_markets.py for every literal `"net_edge":` and `"edge":` dict-key assignment (10 occurrences each, one inside `_price_and_size` itself + 9 call sites that copy `_priced["net_edge"]`/`_priced["edge"]` into the final analysis dict). All 9 real call sites of `_price_and_size` (L8295, 8486, 8985, 9268, 9624, 9847, 10241, 12807, plus 10043) source both fields from the same function, which computes `net_edge` (L7843) and `edge` (L7844) as plain deterministic float expressions — neither can be None. No other code path in weather_markets.py sets an "edge" dict key outside `_price_and_size`'s output. This is stronger evidence than the original finding's own "traced 9 call sites" claim, since it also rules out (by exhaustive grep, not just call-site tracing) any other literal-dict-key assignment path.
- Did not independently audit hourly-market or arbitrage-only analysis paths for a differently-shaped return dict (same limitation the original finding already disclosed) — grep found no `"edge":` literal assignment for those paths in weather_markets.py, which is suggestive but not conclusive if such paths build the dict via variable-key assignment (`d[key] = ...`) rather than a literal.

Verdict: CONFIRMED (as INFO-severity, no live risk). Matches original almost exactly; I found additional corroborating evidence (grep-based exhaustiveness check) beyond the original's spot-checked 9 call sites.

## Finding 3: Far-tail rain blend n_members metadata

- Read weather_markets.py L8778-8940 (`_analyze_monthly_rain_trade`'s far-tail branch) in full.
- Confirmed `combined_totals = [m + t for m in member_totals for t in tail_sums_tilted]` (L8896-8898), the deterministic cross-product as described.
- Confirmed `forecast_blend_signal` (L8918-8933) logs only `rain_forecast_blend_tail_days` (a calendar-day count: `days_in_month - tail_start_day + 1`, L8899) and `rain_forecast_blend_n_members` (`len(member_totals)`, the near-ensemble count, L8900) — `len(tail_sums_tilted)` / `len(tail_sums)` (the tail-year sample count) is computed and used at L8858 (`if len(tail_sums) >= 15`) and in a debug log on the skip branch (L8907), but is never included in the persisted `forecast_blend_signal` dict on the success path. Grepped the whole file for "n_tail" — no alternate key logs it either.
- Confirmed this signal is shadow/log-only: registered in SIGNAL_REGISTRY as key "rain_forecast_blend" (L7084-7098) with an explicit comment "only wire into blended_prob if it's a real improvement" — i.e. not currently wired in. Confirmed via code read that `blended_prob`/`ens_prob` (L8941-8943) are computed from a separate `remaining_sums_tilted`, not from `forecast_blend_prob`. The signal is merged into the analysis dict only under a nested `"signals"` key (L9050-9054), not into any field the trading decision reads directly.

Verdict: CONFIRMED. Matches original finding exactly; upgraded evidence_level from the original's E1 to E1 still (this is a documentation/completeness observation confirmed by direct code read, not something requiring execution) — but I removed the original's stated uncertainty about the SIGNAL_REGISTRY graduation-comparison framing, since I read that code directly (L7091-7097) and it explicitly confirms the "future graduation analysis" use case the finding assumed.
