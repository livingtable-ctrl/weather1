# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**71 unfinished entries** as of generation time.

- **L4** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L2796** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L3554** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L4609** [OPEN] HURRICANE NEXT-EVENT MODEL'S OCCURRED_THIS_SEASON SIGNAL IS SEASON-SCOPED, NOT ISSUANCE-SCOPED (KALSHI CONFIRMS ROLLOVER)
- **L6321** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L6940** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L8000** [PARTIALLY RESOLVED] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L8541** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L9645** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10234** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L10396** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L10800** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L11391** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L11625** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L12852** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L13178** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L13236** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L13363** [PARTIALLY RESOLVED] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L16020** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L18102** [PARTIALLY RESOLVED] main.py's FROZEN `from utils import ...` COULD REPEAT THE SAME IDENTITY-DIVERGENCE BUG -- utils IS RELOADED BY 4 TESTS
- **L22620** [OPEN] _log_shadow_predictions() re-opens the paper ledger and a fresh SQLite connection once per shadow ticker per cron cycle instead of once per batch
- **L22668** [OPEN] No automated reconciliation between execution_log's internally-tracked live positions and Kalshi's real /portfolio/positions
- **L22765** [OPEN] emos-train/emos-deactivate check cron-in-flight before an unbounded human confirmation prompt, not immediately before the write
- **L22936** [OPEN] Live cmd_order sell only closes the oldest of multiple tracked live positions sharing the same ticker+side (operator-warned, not auto-fixed)
- **L23259** [OPEN] settlement_monitor.py logs per-city polling failures at DEBUG only, invisible on console for an unattended daily task
- **L23271** [OPEN] execution_log.py and tracker.py never explicitly close SQLite connections
- **L23302** [OPEN] trade_cycle.py's prewarm ThreadPoolExecutor tasks are not cancelled on timeout and outlive the phase that spawned them
- **L23412** [OPEN] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L23595** [PARTIALLY RESOLVED] schema_validator.py's validate_market/validate_forecast/validate_nws_response return values are discarded everywhere they're called
- **L23613** [OPEN] README.md's environment-variable table omits the 6 shadow-only *_TRADING_ENABLED feature flags, 3 added within this audit's commit window
- **L23620** [OPEN] README.md's 'bot only trades temperature and precipitation' claim is stale given hurricane market support added this window
- **L23627** [OPEN] README.md's EMOS activation row count ('~25 rows') contradicts the actual 40-row floor added this window and contradicts COMMANDS.md
- **L23634** [OPEN] cmd_schedule()'s docstring claims 'auto-scan every hour' but registers a 3-hourly task
- **L23641** [OPEN] README.md's NOTIFY_CHANNELS default ('desktop,discord') doesn't match the actual code default
- **L23665** [OPEN] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L23677** [OPEN] cmd_watch and cron confirmed to share one effective safety-gate chain via run_trade_cycle (no residual divergence found)
- **L23688** [OPEN] safe_io bare os.replace() migration is complete repo-wide (cluster J fully resolved for os.replace specifically)
- **L23699** [OPEN] web_app.py CSRF check (X-Requested-With / method allowlist) applies globally via before_request, no per-route gap found
- **L23711** [OPEN] Admin accuracy-circuit-breaker override (cluster M) confirmed CLI-only, not dashboard-reachable
- **L23724** [OPEN] trade_cycle.py's STRONG/MED placement-gate mirror uses a different None-fallback for net_edge than the real validate() gate it mirrors
- **L23736** [OPEN] log_prediction's UTC-based days_out fallback remains for callers that do not supply analysis days_out
- **L23748** [OPEN] ml_bias.get_emos_status() mislabels a concurrent EMOS deactivation race as file corruption
- **L23756** [OPEN] ForecastCache disk snapshot is last-writer-wins across independent processes
- **L23768** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L23852** [OPEN] Verified: web_app.py's dev server does NOT block concurrent requests behind an open SSE stream, despite Werkzeug's own run_simple() defaulting threaded=False
- **L23863** [OPEN] ml_bias.py's HMAC sidecar write bypasses the codebase's atomic-write convention (but fails safe on a torn write anyway)
- **L23876** [OPEN] Misleading test docstring in test_full_exit_race_loss_does_not_crash_the_caller
- **L23888** [OPEN] Recent-commit test suite (2026-08-02..08-17) shows unusually high, self-critical test-writing discipline
- **L23899** [OPEN] metar.fetch_metar_daily_extreme docstring's caller list is stale after b0f4cad2 added a third caller
- **L23910** [OPEN] CI workflow runs the full pytest suite twice per run (coverage report + coverage gate as separate full runs)
- **L23917** [OPEN] pyproject.toml's `integration` marker relies entirely on tests self-skipping, not on pytest configuration excluding the marker
- **L23924** [OPEN] tests/conftest.py's mock_balance_1000 fixture does not actually isolate paper.DATA_PATH -- tests using it read/write the REAL production data/paper_trades.json
- **L23978** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
- **L24013** [OPEN] _exit_live_position's docstring claims a reduced gate; the code runs the FULL LiveTradingGate, silently disabling every protective exit once the daily-loss halt trips
- **L24047** [OPEN] 'unknown'-status live orders have no age cap or operator alert -- an unresolvable row is re-checked forever at 3 authenticated GETs per recovery pass with no visible escalation
- **L24089** [OPEN] _recover_pending_orders' unknown-row loop re-walks the account's full order history once PER unknown row instead of once per recovery pass
- **L24116** [OPEN] order_executor._poll_pending_orders' natural-settlement P&L formula applies the taker fee only on WINNING outcomes, so a taker-entered position that LOSES at settlement is recorded fee-free, understating that loss
- **L24519** [OPEN] ANALYZE_TRADE'S model_consensus CHECK (AND ORDER_EXECUTOR'S KELLY-HALVING) DOESN'T EXCLUDE A QUARANTINED ENSEMBLE MEMBER
- **L24555** [OPEN] QUARANTINE CHANGES THE LIVE ENSEMBLE BLEND'S SPREAD, BUT SPREAD-BASED CALIBRATIONS (EMOS, ANOMALY/BIMODAL GUARDS) ARE FIT ON THE UNFILTERED 3-MODEL BLEND
- **L24588** [OPEN] BRIER_SKILL_SCORE() HAS NO CONDITION_TYPE FILTER
- **L24615** [OPEN] CALIBRATION.PY/ML_BIAS.PY/MAIN.PY STILL HAVE THE STATIC HARDCODED BRIER CONDITION_TYPE EXCLUSION TUPLE
- **L24672** [OPEN] MORE BRIER-FAMILY FUNCTIONS WITH NO CONDITION_TYPE FILTER, FOUND VIA ADJACENCY
- **L24910** [OPEN] order_id also flows unvalidated into REST path segments in cancel_order/amend_order/get_order, same path-interpolation pattern AUD-0076 fixed for ticker/series_ticker
- **L24919** [OPEN] kalshi_ws.py's WebSocket URL is hardcoded to the PROD host with no KALSHI_ENV read at all -- in demo mode, order_executor's reprice/chase and flash-crash circuit breaker still consume real production market prices
- **L24928** [OPEN] 6 synthetic "TK_*" test-fixture tickers (TK_CHI, TK_Chicago, TK_LA, TK_Miami, TK_Dallas, TK_NYC) are present in the production predictions.db's price_improvement table
- **L25707** [OPEN] cmd_today's interactive "[P] Place" flow books the actual paper trade at the bid-ask MID, not the realistic ask/no_ask price
- **L25744** [OPEN] weather app site V_3 (3)/src/tabs/SignalsTab.jsx carries the same wrong-side-price bugs batch-26 item 1 just fixed in the real frontend, unfixed
- **L25764** [OPEN] trade_cycle.py's pre-scan CB-health check, web_app.py's circuit-breaker status dashboard, AND cron.py's own newly-opened-circuit detector don't know about the new _ensemble_precip_multiday_cb
- **L26177** [OPEN] record_live_exit_fill never charges the ENTRY-side taker fee for a taker-entered position that closes via an early exit (stop-loss/breakeven/model-exit/manual sell) rather than natural settlement
- **L26470** [OPEN] weather_markets._metar_lock_in's above/below branch has no monotonic-safety veto for HIGH markets, unlike the existing `_is_low_mkt` veto for LOW markets -- the OKC/SATX incident's core shape (a running value well inside "not yet confirmed" territory at 14:00-15:00, before the day's true extreme has occurred) is still fully reachable with entirely legitimate, correctly-dated same-day data
- **L26529** [OPEN] settlement_monitor. check_city_settlement never checks a market's own event date against the METAR observation date -- only whether the OBSERVATION is from today, never whether the MARKET itself is a today-settling market
