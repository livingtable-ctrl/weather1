# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**96 unfinished entries** as of generation time.

- **L4** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L1947** [OPEN] KALSHI_ENV=prod STARTUP BANNER WRONGLY CLAIMS ONLY `watch --auto --live` CAN PLACE LIVE ORDERS -- FALSE FOR cmd_order (buy/sell) ONCE LIVE_TRADING_ENABLED=true
- **L2746** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L3495** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L4550** [OPEN] HURRICANE NEXT-EVENT MODEL'S OCCURRED_THIS_SEASON SIGNAL IS SEASON-SCOPED, NOT ISSUANCE-SCOPED (KALSHI CONFIRMS ROLLOVER)
- **L6262** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L6881** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L7941** [OPEN] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L8352** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L9437** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10026** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L10177** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L10566** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L11126** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L11350** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L11796** [OPEN] CITY-LOCAL AFTERNOON SAME-DAY SWEEP
- **L12462** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L12768** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L12818** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L12869** [OPEN] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L14384** [PARTIALLY RESOLVED] check_model_exits/_check_live_model_exits/ _check_early_exits STILL SOURCE POSITIONS AS RAW DICTS, NOT THE NEW Position READ-MODEL -- AND THEIR EXIT-PRICE CONVENTIONS HAVE ALREADY DIVERGED
- **L14650** [PARTIALLY RESOLVED] SIGNAL GRADUATION IS A CONVENTION, NOT A MECHANISM -- SIGNAL #3 REBUILDS RUN-TREND'S PLUMBING
- **L15063** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L17145** [PARTIALLY RESOLVED] main.py's FROZEN `from utils import ...` COULD REPEAT THE SAME IDENTITY-DIVERGENCE BUG -- utils IS RELOADED BY 4 TESTS
- **L19062** [OPEN] main.py's _rating() CLI TABLE IS A 4TH, STILL-TEXT-DERIVED STAR LADDER, NOW CONTRADICTING THE FIXED ONES
- **L20113** [OPEN] BRIER_SCORE()'S PAPER_TRADES.DB FALLBACK HAS NO CONDITION_TYPE FILTER, AND IS NOW MORE REACHABLE THAN BEFORE
- **L20150** [OPEN] BRIER-FAMILY CONDITION_TYPE EXCLUSION LIST HAS NO COUPLING TO RAIN/SNOW/HURRICANE GRADUATION GATES
- **L20182** [OPEN] SEVERAL BRIER-FAMILY FUNCTIONS STILL HAVE NO CONDITION_TYPE FILTER -- brier_score_rolling_with_n AND brier_score_rolling NOW DISAGREE
- **L20785** [OPEN] LIVE_TRADING_RUNBOOK.md falsely claims only `watch --auto --live` can place live orders
- **L20887** [OPEN] KALSHI_ENV=prod startup banner wrongly claims only `watch --auto --live` can place live orders
- **L20898** [OPEN] KalshiClient env comparison fails open to PROD_BASE for any non-exact 'demo' string
- **L20911** [OPEN] Between-bucket settlement lock-in can lock YES off the instantaneous METAR reading despite a lower authoritative daily-high, contradicting its own documented invariant
- **L20923** [OPEN] _target_date_due() still compares city-local target_date against UTC-today, missed by the 0100bffe/6364b38b fix sweep
- **L20935** [OPEN] .env.example's DASHBOARD_PASSWORD comment ('leave empty to disable auth') is stale — code now refuses to start the dashboard instead
- **L20942** [OPEN] _compute_persistence_prob's same-day daily-extreme fix (b0f4cad2) covers only var="max" (HIGH markets); var="min" (LOW markets) still uses the instantaneous METAR reading instead of the true running daily low
- **L20954** [OPEN] _log_shadow_predictions() re-opens the paper ledger and a fresh SQLite connection once per shadow ticker per cron cycle instead of once per batch
- **L20966** [OPEN] Shadow-only far-tail rain-blend signal shares the global _ensemble_cb circuit breaker with the live temperature trading blend's prewarm fetch
- **L20978** [OPEN] Accuracy-circuit-breaker admin override has no test proving it actually lifts the LIVE trading gate
- **L20991** [OPEN] No automated reconciliation between execution_log's internally-tracked live positions and Kalshi's real /portfolio/positions
- **L21037** [OPEN] Settlement-lag force-close signal is wired to paper positions only, never live
- **L21080** [OPEN] emos-train/emos-deactivate check cron-in-flight before an unbounded human confirmation prompt, not immediately before the write
- **L21122** [OPEN] e5331a8d's two self-disclosed follow-up gaps remain open, and the startup banner is wrong about a third live-capable command path
- **L21134** [OPEN] LIVE_TRADING_RUNBOOK.md's Appendix gate list is incomplete — omits TRADING_PAUSED and kill switch, miscounts 'seven' gates
- **L21141** [OPEN] LIVE_TRADING_RUNBOOK.md incorrectly states KELLY_CAP is 'hardcoded, not env-configurable'
- **L21148** [OPEN] README.md documents `override set/clear` command syntax that doesn't exist — real CLI is `pause/unpause`
- **L21155** [OPEN] web_app.py's CSRF header enforcement (X-Requested-With) has no test that proves it is actually enforced
- **L21168** [OPEN] METAR settlement-lag force-close gate (cron.py's >=0.80 threshold) is mathematically unreachable under the currently fitted calibration model
- **L21179** [OPEN] Live cmd_order sell only closes the oldest of multiple tracked live positions sharing the same ticker+side (operator-warned, not auto-fixed)
- **L21190** [OPEN] METAR-calibrated T-ticker settlement force-close gate's 'currently dormant' status is a coefficient-snapshot fact, not a durable invariant, given weekly auto-retrain
- **L21273** [OPEN] cmd_order accepts and forwards an out-of-range order price with no local validation before hitting the live exchange
- **L21306** [OPEN] Far-tail rain blend's logged n_members metadata doesn't reflect the deterministic cross-product's true effective sample size
- **L21318** [OPEN] tracker.py Previous-Runs-API helpers still use UTC-anchored day arithmetic against a city-local target_date, with stale comments asserting the opposite
- **L21330** [OPEN] cmd_forecast CLI display starts its 7-day range from UTC-today instead of city-local today
- **L21342** [OPEN] web_app.py dashboard forecast endpoints label per-city forecasts using UTC-today, and their own justifying comment is now stale
- **L21354** [OPEN] settlement_monitor.py logs per-city polling failures at DEBUG only, invisible on console for an unattended daily task
- **L21366** [OPEN] execution_log.py and tracker.py never explicitly close SQLite connections
- **L21397** [OPEN] trade_cycle.py's prewarm ThreadPoolExecutor tasks are not cancelled on timeout and outlive the phase that spawned them
- **L21489** [OPEN] tracker.count_settled_signal_rows() builds SQL via f-string interpolation of column/json_key parameters
- **L21502** [OPEN] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L21514** [OPEN] frontend authHeader() has no unit test asserting the CSRF header is present in its output
- **L21527** [OPEN] cmd_order's multi-open-live-position-per-ticker sell branch is untested
- **L21636** [OPEN] METAR calibration production-file write isolation relies on per-test monkeypatches, not an autouse structural guard
- **L21648** [OPEN] No test coverage for check_position_limits() vs execution_log interaction
- **L21660** [OPEN] schema_validator.py's validate_market/validate_forecast/validate_nws_response return values are discarded everywhere they're called
- **L21672** [OPEN] README.md's environment-variable table omits the 6 shadow-only *_TRADING_ENABLED feature flags, 3 added within this audit's commit window
- **L21679** [OPEN] README.md's 'bot only trades temperature and precipitation' claim is stale given hurricane market support added this window
- **L21686** [OPEN] README.md's EMOS activation row count ('~25 rows') contradicts the actual 40-row floor added this window and contradicts COMMANDS.md
- **L21693** [OPEN] cmd_schedule()'s docstring claims 'auto-scan every hour' but registers a 3-hourly task
- **L21700** [OPEN] README.md's NOTIFY_CHANNELS default ('desktop,discord') doesn't match the actual code default
- **L21707** [OPEN] kalshi_client.py docstring claims 'no live caller uses IOC/FOK today' — now false after e5331a8d
- **L21718** [OPEN] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L21730** [OPEN] cmd_watch and cron confirmed to share one effective safety-gate chain via run_trade_cycle (no residual divergence found)
- **L21741** [OPEN] safe_io bare os.replace() migration is complete repo-wide (cluster J fully resolved for os.replace specifically)
- **L21752** [OPEN] web_app.py CSRF check (X-Requested-With / method allowlist) applies globally via before_request, no per-route gap found
- **L21764** [OPEN] Admin accuracy-circuit-breaker override (cluster M) confirmed CLI-only, not dashboard-reachable
- **L21777** [OPEN] trade_cycle.py's STRONG/MED placement-gate mirror uses a different None-fallback for net_edge than the real validate() gate it mirrors
- **L21789** [OPEN] log_prediction's UTC-based days_out fallback remains for callers that do not supply analysis days_out
- **L21801** [OPEN] ml_bias.get_emos_status() mislabels a concurrent EMOS deactivation race as file corruption
- **L21809** [OPEN] ForecastCache disk snapshot is last-writer-wins across independent processes
- **L21821** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L21834** [OPEN] Kalshi API ticker values flow unvalidated into URL path segments
- **L21847** [OPEN] Verified: web_app.py's dev server does NOT block concurrent requests behind an open SSE stream, despite Werkzeug's own run_simple() defaulting threaded=False
- **L21858** [OPEN] ml_bias.py's HMAC sidecar write bypasses the codebase's atomic-write convention (but fails safe on a torn write anyway)
- **L21871** [OPEN] Misleading test docstring in test_full_exit_race_loss_does_not_crash_the_caller
- **L21883** [OPEN] Recent-commit test suite (2026-08-02..08-17) shows unusually high, self-critical test-writing discipline
- **L21894** [OPEN] metar.fetch_metar_daily_extreme docstring's caller list is stale after b0f4cad2 added a third caller
- **L21905** [OPEN] CI workflow runs the full pytest suite twice per run (coverage report + coverage gate as separate full runs)
- **L21912** [OPEN] pyproject.toml's `integration` marker relies entirely on tests self-skipping, not on pytest configuration excluding the marker
- **L21919** [OPEN] tests/conftest.py's mock_balance_1000 fixture does not actually isolate paper.DATA_PATH -- tests using it read/write the REAL production data/paper_trades.json
- **L21973** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
- **L22008** [OPEN] _exit_live_position's docstring claims a reduced gate; the code runs the FULL LiveTradingGate, silently disabling every protective exit once the daily-loss halt trips
- **L22042** [OPEN] 'unknown'-status live orders have no age cap or operator alert -- an unresolvable row is re-checked forever at 3 authenticated GETs per recovery pass with no visible escalation
- **L22084** [OPEN] _recover_pending_orders' unknown-row loop re-walks the account's full order history once PER unknown row instead of once per recovery pass
- **L22111** [OPEN] order_executor._poll_pending_orders' natural-settlement P&L formula applies the taker fee only on WINNING outcomes, so a taker-entered position that LOSES at settlement is recorded fee-free, understating that loss
- **L22514** [OPEN] ANALYZE_TRADE'S model_consensus CHECK (AND ORDER_EXECUTOR'S KELLY-HALVING) DOESN'T EXCLUDE A QUARANTINED ENSEMBLE MEMBER
- **L22550** [OPEN] QUARANTINE CHANGES THE LIVE ENSEMBLE BLEND'S SPREAD, BUT SPREAD-BASED CALIBRATIONS (EMOS, ANOMALY/BIMODAL GUARDS) ARE FIT ON THE UNFILTERED 3-MODEL BLEND
