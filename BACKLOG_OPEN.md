# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**51 unfinished entries** as of generation time.

- **L4** [OPEN] record_live_partial_exit'S `>=` GUARD LETS AN EXACT-MATCH PARTIAL WRITE fill_quantity=0, WHICH RESURRECTS THE POSITION AT ITS ORIGINAL SIZE
- **L56** [OPEN] TWO HRRR analyze_trade TESTS ARE DATE-DEPENDENT AND NOW FAIL -- tests/test_weather_markets.py
- **L102** [OPEN] _settle_recovered_exit_order STILL CLOSES ONLY THE OLDEST MATCHING LIVE POSITION -- THE ASYNC TWIN OF THE cmd_order SELL CASCADE BATCH-60 FIXED
- **L534** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L3382** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L4140** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L6990** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L7609** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L8669** [PARTIALLY RESOLVED] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L9210** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L10314** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10903** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L11065** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L11469** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L12060** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L12294** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L13521** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L13847** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L13905** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L14032** [PARTIALLY RESOLVED] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L16689** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L24216** [PARTIALLY RESOLVED] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L24682** [PARTIALLY RESOLVED] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L24920** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L25373** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
- **L26191** [OPEN] QUARANTINE CHANGES THE LIVE ENSEMBLE BLEND'S SPREAD, BUT SPREAD-BASED CALIBRATIONS (EMOS, ANOMALY/BIMODAL GUARDS) ARE FIT ON THE UNFILTERED 3-MODEL BLEND
- **L26246** [OPEN] BRIER_SKILL_SCORE() HAS NO CONDITION_TYPE FILTER
- **L27042** [OPEN] 6 synthetic "TK_*" test-fixture tickers (TK_CHI, TK_Chicago, TK_LA, TK_Miami, TK_Dallas, TK_NYC) are present in the production predictions.db's price_improvement table
- **L30767** [OPEN] PAPER_MIN_EDGE's entire soft-override scale (config.py + param_sweep.py + backtest.py's walk-forward thresholds) may be set below net_edge's real operating floor, making the edge gate a near no-op
- **L30949** [OPEN] "below" condition-type predictions show directional bias strong enough to repeatedly hit ml_bias._fit_T's T-upper-bound, blocking any T-scaling refit
- **L32002** [OPEN] Queue-position data now exists (execution_log.queue_positions) but nothing reads it back into a reprice/chase decision
- **L32152** [OPEN] GRADUATE HRRR (ncep_hrrr_conus) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L32177** [OPEN] no operator path exists to close an open paper position while the kill switch or TRADING_PAUSED is engaged batch-41 (audit-M-9) added kill-switch/TRADING_PAUSED gates to web_app.py's /api/close-position route, mirroring /api/paper-order's existing gates exactly, per that batch's own explicit directive. This closes a real gap (the route previously had neither gate, unlike its order-placement sibling) but has a side effect worth its own follow-up: cron.py (the automated exit path -- settlement-lag closes, model-reversal exits) already aborts its entire run under either gate, and main.py has no standalone CLI command to manually close an arbitrary open paper position (`undo` only reverses a just-placed trade within a short window, not a general close). With both the dashboard and cron now blocked and no CLI alternative, there is currently NO operator-facing way to close an open position at all while the kill switch or TRADING_PAUSED is engaged -- which matters specifically because closing a position is a risk-REDUCING action, the opposite of what a kill switch is meant to block. Not fixed as part of batch-41 (which only mirrored the existing sibling-route pattern per its own directive, correctly -- adding an operator override for the close path specifically is a separate design decision with its own tradeoffs, not a mechanical parity fix). Noted in LIVE_TRADING_RUNBOOK.md's kill- switch section as the batch-41 note under "Immediate halt (emergency)". What it would look like: either (a) a new `main.py close <trade_id> [exit_price]` CLI command that bypasses the two gates entirely (closing is not itself a live-order-placement action, so the same LiveTradingGate reasoning that blocks new orders doesn't obviously apply), or (b) carve /api/close- position's gate check out with its own justification (e.g. only block if manual=false and a live quote exists, since a kill-switch scenario is exactly when an operator might need to exit at a stale/manual price). Needs a real decision, not a default -- surface via AskUserQuestion when picked up. Source: opus review of audit/handoff_prompts/batches/batch-41.md's implementation, LOW-9.
- **L32744** [OPEN] HURRICANE NEXT-EVENT MODEL LOGS ZERO PREDICTIONS -- GENERIC trade_cycle.py GATES FILTER EVERY REAL CANDIDATE, NOT A ROUTING/DB BUG
- **L33147** [OPEN] Miami hourly pools into the shared T_hourly calibration + hourly gate sample count despite a mis-referenced model
- **L33188** [OPEN] Miami hourly settlement-instant/aggregation methodology unconfirmed
- **L33223** [OPEN] No data-freshness gate on order actions (Approve/Close) -- batch-47's visibility-gated polling made a pre-existing gap materially wider
- **L33406** [OPEN] MEASURE BRIER SKILL CONDITIONED ON THE SIZE OF OUR DISAGREEMENT WITH THE PRICE
- **L33736** [OPEN] The other 5 hourly cities settle on THE WEATHER COMPANY, not METAR -- same mis-reference class batch-52 found for Miami, never measured
- **L33803** [OPEN] TRAIN_ALL_TEMPERATURE_SCALING NEVER FITS THE 'BETWEEN' T ITS OWN DOCSTRING PROMISES
- **L33854** [OPEN] CMD_WALKFORWARD'S PER-CONDITION BRIER SILENTLY DROPS 'BETWEEN'
- **L33883** [OPEN] METAR-LOCKED PATH'S RECOMMENDED SIDE CAN CONTRADICT THE LOCK'S OWN OUTCOME -- AND THE DIVERGENCE GATE IS SKIPPED THERE
- **L33941** [OPEN] _var_from_ticker_prefix RETURNS None FOR KXHOLIDAYTMIN, SO THE WHOLE DAILY PATH TREATS A DAILY-MINIMUM MARKET AS A DAILY-MAXIMUM ONE
- **L33988** [OPEN] TWO HRRR TESTS IN test_weather_markets.py FAIL IN A FULL-FILE RUN BUT PASS IN ISOLATION -- ORDER-DEPENDENT STATE LEAK
- **L34020** [OPEN] BUILD A PER-STORM HURRICANE FORMATION-DATE SIGNAL SO THE NEXT-EVENT MODEL CAN CONFIRM A STORM FELL INSIDE A MARKET'S OWN WINDOW
- **L34073** [OPEN] GRAPHIFY'S OBSIDIAN VAULT AND graph.html WERE GENERATED FROM THE EDGE-DIRECTION-CORRUPTED GRAPH AND ARE NOW STALE
- **L34115** [OPEN] useData.js's apiFetch has no request timeout, so a HUNG backend freezes the whole dashboard silently instead of degrading
- **L34172** [OPEN] the `graphify update` CLI path still rebuilds the WHOLE repo even though manifest.json is now portable, because the AST cache is keyed by content hash rather than by path -- so a scoped step-29 refresh is only reachable via the manual extract workaround, never the documented command
- **L34233** [OPEN] tests/test_trade_improvements.py::test_trades_placed_below_cap has never exercised the 20-position cap it is named for
- **L34471** [OPEN] Autouse fixture setup is 85% of test wall time; two fixtures account for 99.6% of it
- **L34529** [OPEN] Intermittent single-test failure (~1 in 11) when the paper/lock/isolation test files run in parallel processes
