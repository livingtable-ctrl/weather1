# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**73 unfinished entries** as of generation time.

- **L138** [OPEN] place_paper_order()'S NEW STALE-TARGET_DATE GUARD HAS NO UPPER BOUND
- **L163** [OPEN] LIVE cmd_order BUY NEVER VALIDATES target_date FRESHNESS BEFORE PLACING A REAL ORDER
- **L185** [OPEN] HURRICANE-NEXT-EVENT TICKERS GET THE WRONG _date FROM enrich_with_forecast (SHARED SEASON-REFERENCE SEGMENT, NOT THE REAL THRESHOLD DATE)
- **L208** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L3021** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L3779** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L4834** [OPEN] HURRICANE NEXT-EVENT MODEL'S OCCURRED_THIS_SEASON SIGNAL IS SEASON-SCOPED, NOT ISSUANCE-SCOPED (KALSHI CONFIRMS ROLLOVER)
- **L6546** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L7165** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L8225** [PARTIALLY RESOLVED] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L8766** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L9870** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10459** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L10621** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L11025** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L11616** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L11850** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L13077** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L13403** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L13461** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L13588** [PARTIALLY RESOLVED] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L16245** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L18327** [PARTIALLY RESOLVED] main.py's FROZEN `from utils import ...` COULD REPEAT THE SAME IDENTITY-DIVERGENCE BUG -- utils IS RELOADED BY 4 TESTS
- **L22845** [OPEN] _log_shadow_predictions() re-opens the paper ledger and a fresh SQLite connection once per shadow ticker per cron cycle instead of once per batch
- **L22893** [OPEN] No automated reconciliation between execution_log's internally-tracked live positions and Kalshi's real /portfolio/positions
- **L22990** [OPEN] emos-train/emos-deactivate check cron-in-flight before an unbounded human confirmation prompt, not immediately before the write
- **L23161** [OPEN] Live cmd_order sell only closes the oldest of multiple tracked live positions sharing the same ticker+side (operator-warned, not auto-fixed)
- **L23502** [OPEN] execution_log.py and tracker.py never explicitly close SQLite connections
- **L23533** [OPEN] trade_cycle.py's prewarm ThreadPoolExecutor tasks are not cancelled on timeout and outlive the phase that spawned them
- **L23643** [OPEN] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L23826** [PARTIALLY RESOLVED] schema_validator.py's validate_market/validate_forecast/validate_nws_response return values are discarded everywhere they're called
- **L23844** [OPEN] README.md's environment-variable table omits the 6 shadow-only *_TRADING_ENABLED feature flags, 3 added within this audit's commit window
- **L23851** [OPEN] README.md's 'bot only trades temperature and precipitation' claim is stale given hurricane market support added this window
- **L23858** [OPEN] README.md's EMOS activation row count ('~25 rows') contradicts the actual 40-row floor added this window and contradicts COMMANDS.md
- **L23865** [OPEN] cmd_schedule()'s docstring claims 'auto-scan every hour' but registers a 3-hourly task
- **L23872** [OPEN] README.md's NOTIFY_CHANNELS default ('desktop,discord') doesn't match the actual code default
- **L23896** [OPEN] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L23908** [OPEN] cmd_watch and cron confirmed to share one effective safety-gate chain via run_trade_cycle (no residual divergence found)
- **L23919** [OPEN] safe_io bare os.replace() migration is complete repo-wide (cluster J fully resolved for os.replace specifically)
- **L23930** [OPEN] web_app.py CSRF check (X-Requested-With / method allowlist) applies globally via before_request, no per-route gap found
- **L23942** [OPEN] Admin accuracy-circuit-breaker override (cluster M) confirmed CLI-only, not dashboard-reachable
- **L23955** [OPEN] trade_cycle.py's STRONG/MED placement-gate mirror uses a different None-fallback for net_edge than the real validate() gate it mirrors
- **L23967** [OPEN] log_prediction's UTC-based days_out fallback remains for callers that do not supply analysis days_out
- **L23979** [OPEN] ml_bias.get_emos_status() mislabels a concurrent EMOS deactivation race as file corruption
- **L23987** [OPEN] ForecastCache disk snapshot is last-writer-wins across independent processes
- **L23999** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L24083** [OPEN] Verified: web_app.py's dev server does NOT block concurrent requests behind an open SSE stream, despite Werkzeug's own run_simple() defaulting threaded=False
- **L24094** [OPEN] ml_bias.py's HMAC sidecar write bypasses the codebase's atomic-write convention (but fails safe on a torn write anyway)
- **L24107** [OPEN] Misleading test docstring in test_full_exit_race_loss_does_not_crash_the_caller
- **L24119** [OPEN] Recent-commit test suite (2026-08-02..08-17) shows unusually high, self-critical test-writing discipline
- **L24146** [OPEN] CI workflow runs the full pytest suite twice per run (coverage report + coverage gate as separate full runs)
- **L24153** [OPEN] pyproject.toml's `integration` marker relies entirely on tests self-skipping, not on pytest configuration excluding the marker
- **L24160** [OPEN] tests/conftest.py's mock_balance_1000 fixture does not actually isolate paper.DATA_PATH -- tests using it read/write the REAL production data/paper_trades.json
- **L24214** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
- **L24249** [OPEN] _exit_live_position's docstring claims a reduced gate; the code runs the FULL LiveTradingGate, silently disabling every protective exit once the daily-loss halt trips
- **L24283** [OPEN] 'unknown'-status live orders have no age cap or operator alert -- an unresolvable row is re-checked forever at 3 authenticated GETs per recovery pass with no visible escalation
- **L24325** [OPEN] _recover_pending_orders' unknown-row loop re-walks the account's full order history once PER unknown row instead of once per recovery pass
- **L24771** [OPEN] ANALYZE_TRADE'S model_consensus CHECK (AND ORDER_EXECUTOR'S KELLY-HALVING) DOESN'T EXCLUDE A QUARANTINED ENSEMBLE MEMBER
- **L24807** [OPEN] QUARANTINE CHANGES THE LIVE ENSEMBLE BLEND'S SPREAD, BUT SPREAD-BASED CALIBRATIONS (EMOS, ANOMALY/BIMODAL GUARDS) ARE FIT ON THE UNFILTERED 3-MODEL BLEND
- **L24840** [OPEN] BRIER_SKILL_SCORE() HAS NO CONDITION_TYPE FILTER
- **L24867** [OPEN] CALIBRATION.PY/ML_BIAS.PY/MAIN.PY STILL HAVE THE STATIC HARDCODED BRIER CONDITION_TYPE EXCLUSION TUPLE
- **L24924** [OPEN] MORE BRIER-FAMILY FUNCTIONS WITH NO CONDITION_TYPE FILTER, FOUND VIA ADJACENCY
- **L25162** [OPEN] order_id also flows unvalidated into REST path segments in cancel_order/amend_order/get_order, same path-interpolation pattern AUD-0076 fixed for ticker/series_ticker
- **L25171** [OPEN] kalshi_ws.py's WebSocket URL is hardcoded to the PROD host with no KALSHI_ENV read at all -- in demo mode, order_executor's reprice/chase and flash-crash circuit breaker still consume real production market prices
- **L25180** [OPEN] 6 synthetic "TK_*" test-fixture tickers (TK_CHI, TK_Chicago, TK_LA, TK_Miami, TK_Dallas, TK_NYC) are present in the production predictions.db's price_improvement table
- **L25959** [OPEN] cmd_today's interactive "[P] Place" flow books the actual paper trade at the bid-ask MID, not the realistic ask/no_ask price
- **L25996** [OPEN] weather app site V_3 (3)/src/tabs/SignalsTab.jsx carries the same wrong-side-price bugs batch-26 item 1 just fixed in the real frontend, unfixed
- **L26016** [OPEN] trade_cycle.py's pre-scan CB-health check, web_app.py's circuit-breaker status dashboard, AND cron.py's own newly-opened-circuit detector don't know about the new _ensemble_precip_multiday_cb
- **L26429** [OPEN] record_live_exit_fill never charges the ENTRY-side taker fee for a taker-entered position that closes via an early exit (stop-loss/breakeven/model-exit/manual sell) rather than natural settlement
- **L26722** [OPEN] weather_markets._metar_lock_in's above/below branch has no monotonic-safety veto for HIGH markets, unlike the existing `_is_low_mkt` veto for LOW markets -- the OKC/SATX incident's core shape (a running value well inside "not yet confirmed" territory at 14:00-15:00, before the day's true extreme has occurred) is still fully reachable with entirely legitimate, correctly-dated same-day data
- **L26781** [OPEN] settlement_monitor. check_city_settlement never checks a market's own event date against the METAR observation date -- only whether the OBSERVATION is from today, never whether the MARKET itself is a today-settling market
- **L27170** [OPEN] _amend_live_order's log_order_result(row_id=replaces_order_id, status="amended") call is bare, same bug class as L-10(a)
- **L27492** [OPEN] mos.fetch_mos()'s GFS/NAM row filter compares a UTC ftime against a city-local target date
