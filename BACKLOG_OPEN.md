# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**84 unfinished entries** as of generation time.

- **L4** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L2786** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L3535** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L4590** [OPEN] HURRICANE NEXT-EVENT MODEL'S OCCURRED_THIS_SEASON SIGNAL IS SEASON-SCOPED, NOT ISSUANCE-SCOPED (KALSHI CONFIRMS ROLLOVER)
- **L6302** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L6921** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L7981** [OPEN] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L8392** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L9477** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10066** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L10217** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L10606** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L11166** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L11390** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L11836** [OPEN] CITY-LOCAL AFTERNOON SAME-DAY SWEEP
- **L12502** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L12808** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L12858** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L12909** [OPEN] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L14424** [PARTIALLY RESOLVED] check_model_exits/_check_live_model_exits/ _check_early_exits STILL SOURCE POSITIONS AS RAW DICTS, NOT THE NEW Position READ-MODEL -- AND THEIR EXIT-PRICE CONVENTIONS HAVE ALREADY DIVERGED
- **L14690** [PARTIALLY RESOLVED] SIGNAL GRADUATION IS A CONVENTION, NOT A MECHANISM -- SIGNAL #3 REBUILDS RUN-TREND'S PLUMBING
- **L15103** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L17185** [PARTIALLY RESOLVED] main.py's FROZEN `from utils import ...` COULD REPEAT THE SAME IDENTITY-DIVERGENCE BUG -- utils IS RELOADED BY 4 TESTS
- **L19102** [OPEN] main.py's _rating() CLI TABLE IS A 4TH, STILL-TEXT-DERIVED STAR LADDER, NOW CONTRADICTING THE FIXED ONES
- **L21234** [OPEN] KalshiClient env comparison fails open to PROD_BASE for any non-exact 'demo' string
- **L21247** [OPEN] Between-bucket settlement lock-in can lock YES off the instantaneous METAR reading despite a lower authoritative daily-high, contradicting its own documented invariant
- **L21277** [OPEN] .env.example's DASHBOARD_PASSWORD comment ('leave empty to disable auth') is stale — code now refuses to start the dashboard instead
- **L21284** [OPEN] _compute_persistence_prob's same-day daily-extreme fix (b0f4cad2) covers only var="max" (HIGH markets); var="min" (LOW markets) still uses the instantaneous METAR reading instead of the true running daily low
- **L21296** [OPEN] _log_shadow_predictions() re-opens the paper ledger and a fresh SQLite connection once per shadow ticker per cron cycle instead of once per batch
- **L21308** [OPEN] Shadow-only far-tail rain-blend signal shares the global _ensemble_cb circuit breaker with the live temperature trading blend's prewarm fetch
- **L21320** [OPEN] Accuracy-circuit-breaker admin override has no test proving it actually lifts the LIVE trading gate
- **L21333** [OPEN] No automated reconciliation between execution_log's internally-tracked live positions and Kalshi's real /portfolio/positions
- **L21379** [OPEN] Settlement-lag force-close signal is wired to paper positions only, never live
- **L21422** [OPEN] emos-train/emos-deactivate check cron-in-flight before an unbounded human confirmation prompt, not immediately before the write
- **L21546** [OPEN] web_app.py's CSRF header enforcement (X-Requested-With) has no test that proves it is actually enforced
- **L21559** [OPEN] METAR settlement-lag force-close gate (cron.py's >=0.80 threshold) is mathematically unreachable under the currently fitted calibration model
- **L21570** [OPEN] Live cmd_order sell only closes the oldest of multiple tracked live positions sharing the same ticker+side (operator-warned, not auto-fixed)
- **L21581** [OPEN] METAR-calibrated T-ticker settlement force-close gate's 'currently dormant' status is a coefficient-snapshot fact, not a durable invariant, given weekly auto-retrain
- **L21664** [OPEN] cmd_order accepts and forwards an out-of-range order price with no local validation before hitting the live exchange
- **L21697** [OPEN] Far-tail rain blend's logged n_members metadata doesn't reflect the deterministic cross-product's true effective sample size
- **L21781** [OPEN] settlement_monitor.py logs per-city polling failures at DEBUG only, invisible on console for an unattended daily task
- **L21793** [OPEN] execution_log.py and tracker.py never explicitly close SQLite connections
- **L21824** [OPEN] trade_cycle.py's prewarm ThreadPoolExecutor tasks are not cancelled on timeout and outlive the phase that spawned them
- **L21916** [OPEN] tracker.count_settled_signal_rows() builds SQL via f-string interpolation of column/json_key parameters
- **L21929** [OPEN] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L21941** [OPEN] frontend authHeader() has no unit test asserting the CSRF header is present in its output
- **L21954** [OPEN] cmd_order's multi-open-live-position-per-ticker sell branch is untested
- **L22063** [OPEN] METAR calibration production-file write isolation relies on per-test monkeypatches, not an autouse structural guard
- **L22075** [OPEN] No test coverage for check_position_limits() vs execution_log interaction
- **L22087** [OPEN] schema_validator.py's validate_market/validate_forecast/validate_nws_response return values are discarded everywhere they're called
- **L22099** [OPEN] README.md's environment-variable table omits the 6 shadow-only *_TRADING_ENABLED feature flags, 3 added within this audit's commit window
- **L22106** [OPEN] README.md's 'bot only trades temperature and precipitation' claim is stale given hurricane market support added this window
- **L22113** [OPEN] README.md's EMOS activation row count ('~25 rows') contradicts the actual 40-row floor added this window and contradicts COMMANDS.md
- **L22120** [OPEN] cmd_schedule()'s docstring claims 'auto-scan every hour' but registers a 3-hourly task
- **L22127** [OPEN] README.md's NOTIFY_CHANNELS default ('desktop,discord') doesn't match the actual code default
- **L22151** [OPEN] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L22163** [OPEN] cmd_watch and cron confirmed to share one effective safety-gate chain via run_trade_cycle (no residual divergence found)
- **L22174** [OPEN] safe_io bare os.replace() migration is complete repo-wide (cluster J fully resolved for os.replace specifically)
- **L22185** [OPEN] web_app.py CSRF check (X-Requested-With / method allowlist) applies globally via before_request, no per-route gap found
- **L22197** [OPEN] Admin accuracy-circuit-breaker override (cluster M) confirmed CLI-only, not dashboard-reachable
- **L22210** [OPEN] trade_cycle.py's STRONG/MED placement-gate mirror uses a different None-fallback for net_edge than the real validate() gate it mirrors
- **L22222** [OPEN] log_prediction's UTC-based days_out fallback remains for callers that do not supply analysis days_out
- **L22234** [OPEN] ml_bias.get_emos_status() mislabels a concurrent EMOS deactivation race as file corruption
- **L22242** [OPEN] ForecastCache disk snapshot is last-writer-wins across independent processes
- **L22254** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L22267** [OPEN] Kalshi API ticker values flow unvalidated into URL path segments
- **L22280** [OPEN] Verified: web_app.py's dev server does NOT block concurrent requests behind an open SSE stream, despite Werkzeug's own run_simple() defaulting threaded=False
- **L22291** [OPEN] ml_bias.py's HMAC sidecar write bypasses the codebase's atomic-write convention (but fails safe on a torn write anyway)
- **L22304** [OPEN] Misleading test docstring in test_full_exit_race_loss_does_not_crash_the_caller
- **L22316** [OPEN] Recent-commit test suite (2026-08-02..08-17) shows unusually high, self-critical test-writing discipline
- **L22327** [OPEN] metar.fetch_metar_daily_extreme docstring's caller list is stale after b0f4cad2 added a third caller
- **L22338** [OPEN] CI workflow runs the full pytest suite twice per run (coverage report + coverage gate as separate full runs)
- **L22345** [OPEN] pyproject.toml's `integration` marker relies entirely on tests self-skipping, not on pytest configuration excluding the marker
- **L22352** [OPEN] tests/conftest.py's mock_balance_1000 fixture does not actually isolate paper.DATA_PATH -- tests using it read/write the REAL production data/paper_trades.json
- **L22406** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
- **L22441** [OPEN] _exit_live_position's docstring claims a reduced gate; the code runs the FULL LiveTradingGate, silently disabling every protective exit once the daily-loss halt trips
- **L22475** [OPEN] 'unknown'-status live orders have no age cap or operator alert -- an unresolvable row is re-checked forever at 3 authenticated GETs per recovery pass with no visible escalation
- **L22517** [OPEN] _recover_pending_orders' unknown-row loop re-walks the account's full order history once PER unknown row instead of once per recovery pass
- **L22544** [OPEN] order_executor._poll_pending_orders' natural-settlement P&L formula applies the taker fee only on WINNING outcomes, so a taker-entered position that LOSES at settlement is recorded fee-free, understating that loss
- **L22947** [OPEN] ANALYZE_TRADE'S model_consensus CHECK (AND ORDER_EXECUTOR'S KELLY-HALVING) DOESN'T EXCLUDE A QUARANTINED ENSEMBLE MEMBER
- **L22983** [OPEN] QUARANTINE CHANGES THE LIVE ENSEMBLE BLEND'S SPREAD, BUT SPREAD-BASED CALIBRATIONS (EMOS, ANOMALY/BIMODAL GUARDS) ARE FIT ON THE UNFILTERED 3-MODEL BLEND
- **L23016** [OPEN] BRIER_SKILL_SCORE() HAS NO CONDITION_TYPE FILTER
- **L23043** [OPEN] CALIBRATION.PY/ML_BIAS.PY/MAIN.PY STILL HAVE THE STATIC HARDCODED BRIER CONDITION_TYPE EXCLUSION TUPLE
- **L23100** [OPEN] MORE BRIER-FAMILY FUNCTIONS WITH NO CONDITION_TYPE FILTER, FOUND VIA ADJACENCY
