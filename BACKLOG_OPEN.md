# Backlog — Unfinished Items (auto-generated)

Generated by `python backlog_index.py` from `backlog.txt`. This file is a disposable index, not a source of truth -- edit backlog.txt itself, then re-run the script to refresh this list. RESOLVED and CLOSED entries (finished either way) are excluded.

**104 unfinished entries** as of generation time.

- **L4** [OPEN] METAR SETTLEMENT-LAG CALIBRATION MAKES CRON.PY'S >=0.80 FORCE-CLOSE GATE MATHEMATICALLY UNREACHABLE UNDER THE CURRENT FITTED MODEL -- ALSO, THE MODEL WAS FIT ON A DIFFERENT POPULATION THAN IT'S APPLIED TO HERE
- **L1947** [OPEN] KALSHI_ENV=prod STARTUP BANNER WRONGLY CLAIMS ONLY `watch --auto --live` CAN PLACE LIVE ORDERS -- FALSE FOR cmd_order (buy/sell) ONCE LIVE_TRADING_ENABLED=true
- **L2746** [PARTIALLY RESOLVED] EMOS CALIBRATION STAYS DISABLED UNTIL THE ens_var-POPULATED TRAINING SET CLEARS 40 ROWS
- **L3495** [PARTIALLY RESOLVED] RAIN MARKETS -- MONTHLY MODEL HAS NO DAY-SPECIFIC FORECAST SIGNAL
- **L4550** [OPEN] HURRICANE NEXT-EVENT MODEL'S OCCURRED_THIS_SEASON SIGNAL IS SEASON-SCOPED, NOT ISSUANCE-SCOPED (KALSHI CONFIRMS ROLLOVER)
- **L6262** [OPEN] DEMO_BASE SMOKE TEST -- LIVE ORDER-EXECUTION PATH HAS NEVER BEEN FIRED AT A REAL EXCHANGE
- **L6881** [PARTIALLY RESOLVED] RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE
- **L7941** [OPEN] RAIN ARBITRAGE-CHECK SHADOW SIGNAL HAS NO GRADUATION DECISION YET
- **L8352** [PARTIALLY RESOLVED] 3-WAY MODEL_CONSENSUS CHECK -- ECMWF_AIFS025_ENSEMBLE'S PROBABILITY ISN'T WIRED IN YET
- **L9437** [OPEN] MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
- **L10026** [PARTIALLY RESOLVED] FORECAST RUN-TO-RUN TREND SIGNAL
- **L10177** [PARTIALLY RESOLVED] RICHER ML CALIBRATION FEATURES
- **L10566** [PARTIALLY RESOLVED] GRADUATE GEM/UKMO (AND FUTURE TRACKING-ONLY MODELS) FROM TRACK-ONLY INTO THE LIVE BLEND
- **L11126** [OPEN] DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH
- **L11350** [OPEN] PUBLIC TRADE-FLOW SIGNAL
- **L11796** [OPEN] CITY-LOCAL AFTERNOON SAME-DAY SWEEP
- **L12462** [PARTIALLY RESOLVED] PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT
- **L12768** [PARTIALLY RESOLVED] FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ
- **L12818** [PARTIALLY RESOLVED] CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE
- **L12869** [OPEN] MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS
- **L14384** [PARTIALLY RESOLVED] check_model_exits/_check_live_model_exits/ _check_early_exits STILL SOURCE POSITIONS AS RAW DICTS, NOT THE NEW Position READ-MODEL -- AND THEIR EXIT-PRICE CONVENTIONS HAVE ALREADY DIVERGED
- **L14650** [PARTIALLY RESOLVED] SIGNAL GRADUATION IS A CONVENTION, NOT A MECHANISM -- SIGNAL #3 REBUILDS RUN-TREND'S PLUMBING
- **L15063** [OPEN] ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS
- **L17145** [PARTIALLY RESOLVED] main.py's FROZEN `from utils import ...` COULD REPEAT THE SAME IDENTITY-DIVERGENCE BUG -- utils IS RELOADED BY 4 TESTS
- **L19062** [OPEN] main.py's _rating() CLI TABLE IS A 4TH, STILL-TEXT-DERIVED STAR LADDER, NOW CONTRADICTING THE FIXED ONES
- **L20113** [OPEN] BRIER_SCORE()'S PAPER_TRADES.DB FALLBACK HAS NO CONDITION_TYPE FILTER, AND IS NOW MORE REACHABLE THAN BEFORE
- **L20150** [OPEN] BRIER-FAMILY CONDITION_TYPE EXCLUSION LIST HAS NO COUPLING TO RAIN/SNOW/HURRICANE GRADUATION GATES
- **L20182** [OPEN] SEVERAL BRIER-FAMILY FUNCTIONS STILL HAVE NO CONDITION_TYPE FILTER -- brier_score_rolling_with_n AND brier_score_rolling NOW DISAGREE
- **L20485** [OPEN] Settlement P&L for taker (IOC)-filled live orders is computed with the $0 maker fee, not Kalshi's real taker fee
- **L20525** [OPEN] cron._acquire_cron_lock() has a TOCTOU race with no OS-level exclusive-create primitive — reproduced
- **L20538** [OPEN] Ambiguous place_order() failure can leave a real live position permanently untracked, unprotected, and re-orderable
- **L20550** [OPEN] cmd_watch --live position-protection block has zero exception handling; a single failure kills the entire persistent watch process
- **L20590** [OPEN] _quick_paper_buy()'s maker-order branch places real live orders but never records the fill in execution_log
- **L20603** [OPEN] LIVE_TRADING_RUNBOOK.md falsely claims only `watch --auto --live` can place live orders
- **L20632** [OPEN] cmd_watch --auto --live has no standalone _recover_pending_orders() call; crash-window phantom 'pending' live rows stay invisible to live position protection whenever the cron lock is contended
- **L20644** [OPEN] KALSHI_ENV=prod startup banner wrongly claims only `watch --auto --live` can place live orders
- **L20655** [OPEN] KalshiClient env comparison fails open to PROD_BASE for any non-exact 'demo' string
- **L20668** [OPEN] Between-bucket settlement lock-in can lock YES off the instantaneous METAR reading despite a lower authoritative daily-high, contradicting its own documented invariant
- **L20680** [OPEN] _target_date_due() still compares city-local target_date against UTC-today, missed by the 0100bffe/6364b38b fix sweep
- **L20692** [OPEN] .env.example's DASHBOARD_PASSWORD comment ('leave empty to disable auth') is stale — code now refuses to start the dashboard instead
- **L20699** [OPEN] _compute_persistence_prob's same-day daily-extreme fix (b0f4cad2) covers only var="max" (HIGH markets); var="min" (LOW markets) still uses the instantaneous METAR reading instead of the true running daily low
- **L20711** [OPEN] _log_shadow_predictions() re-opens the paper ledger and a fresh SQLite connection once per shadow ticker per cron cycle instead of once per batch
- **L20723** [OPEN] Shadow-only far-tail rain-blend signal shares the global _ensemble_cb circuit breaker with the live temperature trading blend's prewarm fetch
- **L20735** [OPEN] Accuracy-circuit-breaker admin override has no test proving it actually lifts the LIVE trading gate
- **L20748** [OPEN] No automated reconciliation between execution_log's internally-tracked live positions and Kalshi's real /portfolio/positions
- **L20760** [OPEN] cmd_order's unmatched-sell settlement fallback can leave the exact phantom-position shape its own fix was designed to prevent
- **L20772** [OPEN] Settlement-lag force-close signal is wired to paper positions only, never live
- **L20784** [OPEN] cmd_order's manual partial live-sell never settles its own execution_log row, reproducing the 'aggregate-only P&L' bug the same day's earlier commit fixed for the automated path
- **L20793** [OPEN] emos-train/emos-deactivate check cron-in-flight before an unbounded human confirmation prompt, not immediately before the write
- **L20805** [OPEN] paper._CrossProcessDataLock silently fails OPEN after 10 seconds of sustained contention
- **L20817** [OPEN] e5331a8d's two self-disclosed follow-up gaps remain open, and the startup banner is wrong about a third live-capable command path
- **L20829** [OPEN] LIVE_TRADING_RUNBOOK.md's Appendix gate list is incomplete — omits TRADING_PAUSED and kill switch, miscounts 'seven' gates
- **L20836** [OPEN] LIVE_TRADING_RUNBOOK.md incorrectly states KELLY_CAP is 'hardcoded, not env-configurable'
- **L20843** [OPEN] README.md documents `override set/clear` command syntax that doesn't exist — real CLI is `pause/unpause`
- **L20850** [OPEN] web_app.py's CSRF header enforcement (X-Requested-With) has no test that proves it is actually enforced
- **L20863** [OPEN] METAR settlement-lag force-close gate (cron.py's >=0.80 threshold) is mathematically unreachable under the currently fitted calibration model
- **L20874** [OPEN] Live cmd_order sell only closes the oldest of multiple tracked live positions sharing the same ticker+side (operator-warned, not auto-fixed)
- **L20885** [OPEN] METAR-calibrated T-ticker settlement force-close gate's 'currently dormant' status is a coefficient-snapshot fact, not a durable invariant, given weekly auto-retrain
- **L20897** [OPEN] Kill-switch override rename race can crash cmd_cron with uncaught FileExistsError (Path.rename vs os.replace semantics gap)
- **L20909** [OPEN] cmd_order accepts and forwards an out-of-range order price with no local validation before hitting the live exchange
- **L20921** [OPEN] cmd_order writes 'buy'/'sell' into execution_log's order_type column, which every other write site (and the schema's own comment) uses for 'market'/'limit'
- **L20933** [OPEN] Far-tail rain blend's logged n_members metadata doesn't reflect the deterministic cross-product's true effective sample size
- **L20945** [OPEN] tracker.py Previous-Runs-API helpers still use UTC-anchored day arithmetic against a city-local target_date, with stale comments asserting the opposite
- **L20957** [OPEN] cmd_forecast CLI display starts its 7-day range from UTC-today instead of city-local today
- **L20969** [OPEN] web_app.py dashboard forecast endpoints label per-city forecasts using UTC-today, and their own justifying comment is now stale
- **L20981** [OPEN] settlement_monitor.py logs per-city polling failures at DEBUG only, invisible on console for an unattended daily task
- **L20993** [OPEN] execution_log.py and tracker.py never explicitly close SQLite connections
- **L21005** [OPEN] was_traded_today/was_recently_ordered/was_ordered_recently already treat any fill (entry OR exit) on a ticker+side as blocking re-entry -- pre-existing design, not changed by the closes_position_id-adding commits
- **L21013** [OPEN] trade_cycle.py's prewarm ThreadPoolExecutor tasks are not cancelled on timeout and outlive the phase that spawned them
- **L21025** [OPEN] settlement_monitor.py has no application-level overlap guard; relies entirely on unverified Task Scheduler default policy
- **L21037** [OPEN] tracker.count_settled_signal_rows() builds SQL via f-string interpolation of column/json_key parameters
- **L21050** [OPEN] web_app.py's /api/trades route loads the entire paper ledger twice in one request, with no caching across the ~7 routes (plus the /api/stream SSE loop) that each independently call get_open_trades()/get_all_trades()
- **L21062** [OPEN] frontend authHeader() has no unit test asserting the CSRF header is present in its output
- **L21075** [OPEN] cmd_order's multi-open-live-position-per-ticker sell branch is untested
- **L21087** [OPEN] cmd_order's unmatched-live-sell placeholder pnl=0.0 is indistinguishable from a real zero-P&L outcome in tax/P&L exports
- **L21098** [OPEN] METAR calibration production-file write isolation relies on per-test monkeypatches, not an autouse structural guard
- **L21110** [OPEN] No test coverage for check_position_limits() vs execution_log interaction
- **L21122** [OPEN] schema_validator.py's validate_market/validate_forecast/validate_nws_response return values are discarded everywhere they're called
- **L21134** [OPEN] README.md's environment-variable table omits the 6 shadow-only *_TRADING_ENABLED feature flags, 3 added within this audit's commit window
- **L21141** [OPEN] README.md's 'bot only trades temperature and precipitation' claim is stale given hurricane market support added this window
- **L21148** [OPEN] README.md's EMOS activation row count ('~25 rows') contradicts the actual 40-row floor added this window and contradicts COMMANDS.md
- **L21155** [OPEN] cmd_schedule()'s docstring claims 'auto-scan every hour' but registers a 3-hourly task
- **L21162** [OPEN] README.md's NOTIFY_CHANNELS default ('desktop,discord') doesn't match the actual code default
- **L21169** [OPEN] kalshi_client.py docstring claims 'no live caller uses IOC/FOK today' — now false after e5331a8d
- **L21180** [OPEN] Far-tail rain climatology blend's additive dry-tilt shift is floor-clipped at 0.0, under-applying the correction for near-zero precipitation distributions
- **L21192** [OPEN] cmd_watch and cron confirmed to share one effective safety-gate chain via run_trade_cycle (no residual divergence found)
- **L21203** [OPEN] safe_io bare os.replace() migration is complete repo-wide (cluster J fully resolved for os.replace specifically)
- **L21214** [OPEN] web_app.py CSRF check (X-Requested-With / method allowlist) applies globally via before_request, no per-route gap found
- **L21226** [OPEN] Admin accuracy-circuit-breaker override (cluster M) confirmed CLI-only, not dashboard-reachable
- **L21239** [OPEN] trade_cycle.py's STRONG/MED placement-gate mirror uses a different None-fallback for net_edge than the real validate() gate it mirrors
- **L21251** [OPEN] log_prediction's UTC-based days_out fallback remains for callers that do not supply analysis days_out
- **L21263** [OPEN] ml_bias.get_emos_status() mislabels a concurrent EMOS deactivation race as file corruption
- **L21271** [OPEN] ForecastCache disk snapshot is last-writer-wins across independent processes
- **L21283** [OPEN] web_app.py: two @_require_auth route decorators remain despite before_request comment claiming they were removed
- **L21296** [OPEN] Kalshi API ticker values flow unvalidated into URL path segments
- **L21309** [OPEN] Verified: web_app.py's dev server does NOT block concurrent requests behind an open SSE stream, despite Werkzeug's own run_simple() defaulting threaded=False
- **L21320** [OPEN] ml_bias.py's HMAC sidecar write bypasses the codebase's atomic-write convention (but fails safe on a torn write anyway)
- **L21333** [OPEN] Misleading test docstring in test_full_exit_race_loss_does_not_crash_the_caller
- **L21345** [OPEN] Recent-commit test suite (2026-08-02..08-17) shows unusually high, self-critical test-writing discipline
- **L21356** [OPEN] metar.fetch_metar_daily_extreme docstring's caller list is stale after b0f4cad2 added a third caller
- **L21367** [OPEN] CI workflow runs the full pytest suite twice per run (coverage report + coverage gate as separate full runs)
- **L21374** [OPEN] pyproject.toml's `integration` marker relies entirely on tests self-skipping, not on pytest configuration excluding the marker
- **L21381** [OPEN] tests/conftest.py's mock_balance_1000 fixture does not actually isolate paper.DATA_PATH -- tests using it read/write the REAL production data/paper_trades.json
- **L21435** [OPEN] execution_log.get_live_pnl_summary()'s own open_count metric has the identical status='pending'-only undercounting bug AUD-0009 fixed for _count_open_live_orders
