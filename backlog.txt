BACKLOG — future improvements to implement when ready
======================================================

[CLOSED 2026-07-12 -- see below] KALSHI ORDER-WRITE ENDPOINTS MOVING TO /portfolio/events/orders
Priority: was High -- verify before resuming live trading (~2026-07-30 return), not a
  someday item. Found 2026-07-10 via Kalshi's own API changelog; refined
  2026-07-10 via a second research pass (changelog re-fetch + independent
  third-party SDK docs cross-check, both agree).

RESOLVED 2026-07-12: Migrated place_order()/cancel_order() to POST/DELETE
  /portfolio/events/orders per the plan below (commit bc7e10c), then a
  second-opinion review found and fixed one real HIGH bug -- a successful
  POST followed by a failing get_order() follow-up could strand a live
  order under status="failed" with no poller ever picking it up again
  (commit 58a4e3b). 156+ tests pass, lint/mypy clean. This item is done;
  kept below for historical context on the migration decisions.

Problem:
  kalshi_client.py's live order-placement and order-cancellation code
  targets endpoints Kalshi is migrating off of. The split is "reads stay,
  writes move," not a wholesale change:
    - GET /portfolio/orders (kalshi_client.py:313, list resting/executed
      orders) and GET /portfolio/orders/{order_id} (kalshi_client.py:423,
      get single order) are UNAFFECTED -- reads stay on this path
      permanently per Kalshi's own docs.
    - POST /portfolio/orders (kalshi_client.py:364, place_order) and
      DELETE /portfolio/orders/{order_id} (kalshi_client.py:427,
      cancel_order) are the legacy WRITE endpoints being deprecated. The
      new V2 write endpoints are POST /portfolio/events/orders and
      DELETE /portfolio/events/orders/{order_id} (confirmed via both
      Kalshi's changelog and an independent third-party SDK
      -- github.com/TexasCoding/kalshi-python-sdk -- whose docs describe
      the identical old-path/new-path split).
  This is NOT just a URL rename: Kalshi's changelog describes the new V2
  write endpoints as using "a bid/ask single-book shape with fixed-point
  dollar prices and lightweight responses" -- a different request/response
  shape. The third-party SDK's docs show the new response includes
  order_id, remaining_count, fill_count, reduced_by, ts_ms -- diff this
  against what kalshi_client.py currently parses out of the old response
  before assuming the migration is small.
  Timeline: legacy write rate-limit costs escalated to 10x V2-equivalent by
  2026-06-04; deprecation was announced for "sometime between June 18 and
  June 25, 2026." No changelog entry confirms the deprecation was actually
  enforced/completed -- legacy writes may still work today, just at 10x
  rate-limit cost, with no confirmed grace period remaining. No real order
  has ever been placed by this bot yet (ENABLE_MICRO_LIVE=False), so this
  has not caused a live incident.

What the fix looks like:
  1. Quick check first: hit place_order/cancel_order against DEMO_BASE
     (https://demo-api.kalshi.co/trade-api/v2) and see whether they still
     succeed, return a deprecation error, or already 404/reject.
  2. Migrate kalshi_client.py's place_order (line 364) and cancel_order
     (line 427) to POST/DELETE /portfolio/events/orders[/{order_id}].
     Read Kalshi's live API reference for the exact current request body
     (fixed-point dollar prices, bid/ask single-book shape) and response
     field names before writing the migration -- do not assume the old
     request/response dict shape carries over unchanged.
  3. Leave the two GET /portfolio/orders* call sites (line 313, 423) alone
     -- they are not affected by this migration.
  4. While in there, evaluate the new orderbook_delta WebSocket
     get_snapshot action (see FEATURE CANDIDATES section below) since it's
     adjacent surface area.

Why not now (only reason to defer, not to skip):
  - TRADING_PAUSED=true and ENABLE_MICRO_LIVE=False mean nothing is
    currently broken in production -- but this should be resolved before
    either flag flips, not discovered live when it does.

When to revisit:
  - Before the Belgium trip ends (~2026-07-30) and live trading is
    considered for resumption. This is a hard prerequisite, not a nice-to-have.

[RESOLVED 2026-07-12] MYSTERY REVERT -- COMMIT 24559a7 SILENTLY
  WIPED ~10 COMMITS OF CALIBRATION/SAFETY WORK UNDER AN UNRELATED COMMIT
  MESSAGE
Priority: Medium-High (a calibration-integrity safeguard has been silently
  missing for ~166 commits; not urgent since TRADING_PAUSED, but a real gap,
  not cosmetic)

PARTIALLY RESOLVED 2026-07-12: piece 1 of the restoration priority list below,
  disputed-outcome tracking, is done. Ported forward against current tracker.py
  (not a mechanical port of 31c7226's tracker.py diff -- the schema has grown
  from v23 to v38 since, with ~27 new calibration/Brier/bias/win-rate functions
  added that all needed the same treatment the original only gave to 3).
  Migration v38->v39 adds outcomes.disputed; mark_outcome_disputed()/
  get_disputed_count() added; audit_settlement() calls mark_outcome_disputed()
  on a genuine archive/Kalshi mismatch (matching the original). Surfaced a real
  scope question to the user first -- reproduce the original's 3-function scope
  faithfully, or audit and fix every current settled_yes-consuming function --
  user chose the full audit. Traced every JOIN outcomes / o.settled_yes site in
  tracker.py (33 total) and added disputed-row exclusion to all 30 that use
  settled_yes as a calibration/scoring ground truth; left unchanged (with
  reasoning, independently verified by the review below): the count_settled_*
  sample-size gates (matches the original's own unfiltered get_settled_count()
  precedent), get_history/get_outcome_for_ticker (display/lookup, not
  aggregate), backfill_emos_data/get_emos_training_data/
  get_recent_city_correlations (these train on settled_temp_f, the archived
  actual temperature -- the trusted value a dispute is raised AGAINST, not the
  suspect one), get_unselected_bias (different table, no disputed column), and
  the ensemble_member_scores-based model-accuracy functions (unrelated table).
  Also fixed two incidental dead-code-guard failures surfaced while running the
  full test sweep (unrelated to this piece but caught along the way): wired
  get_disputed_count() into web_app.py's /api/analytics dispatch, and
  allowlisted get_price_history() (from the prior candlesticks-capture session)
  with its own accurate deferred-consumer reason. ~380 lines of new tests
  (mechanism, audit_settlement wiring both paths, and a before/after
  disputed-exclusion test per touched function). Independently reviewed (Agent,
  opus) -- came back clean; the reviewer additionally mutation-tested by
  stripping all 30 filter lines from a scratch copy and confirming exactly 30
  test failures, proving the tests genuinely catch a missing filter rather than
  passing spuriously.

  Piece 2, climate-derived sigma, is also done (2026-07-12, same day).
  Restored climatology.py's compute_sigma_from_climate()/load_all_sigmas()
  against current code -- the original commit (ae86946) bundled this with an
  unrelated NBM-delta bias-correction change (piece 3, still open below);
  only the sigma half was ported this pass. get_historical_sigma() gained a
  var= parameter and now prefers the dynamic per-city/per-month value (from
  the already-cached 30yr climate archives) over the static _HISTORICAL_SIGMA
  table, closing the LV/NOLA sigma gap entry below as a side effect exactly
  as anticipated -- verified end-to-end against the real cached
  data/climate_LasVegas.json/data/climate_NewOrleans.json archives (both
  return sane per-month sigmas in the 1.5-5.5F range, no longer flat
  _DEFAULT_SIGMA=3.5). New autouse conftest.py fixture
  (isolate_dynamic_sigma) prevents the lazy module-global cache from writing
  to the real data/ directory during tests or breaking ~7 existing tests that
  assert exact static-table values -- defaults the dynamic path to
  unavailable per-test, matching this project's existing isolation-fixture
  pattern (neutral_temperature_scaling etc.). 22 new tests
  (tests/test_climatology.py + a new TestDynamicSigma class in
  test_gaussian_prob.py). Independently reviewed (Agent, opus) -- clean;
  reviewer additionally ran the restored function live against the real
  cached archives (not just mocked tests) to confirm the LV/NOLA payoff
  actually works, and confirmed no NBM-delta scope leaked in.

  Piece 3 (2026-07-12, same day) is now closed too -- all 4 of its
  sub-pieces got a real per-piece disposition rather than a batch port:
    - NBM-delta bias correction: CLOSED AS NOT NEEDED, not restored. Its
      entire premise ("GFS forecast minus NOAA's professionally
      bias-corrected NBM") is dead -- confirmed live against Open-Meteo
      (models=nbm and models=nbm_conus both 400 with "invalid String
      value") that NBM is no longer available there at all; the current
      fetch_temperature_nbm() already silently substitutes model="best_match"
      (an arbitrary, uncalibrated auto-selection), which would make the delta
      noise, not a bias signal. More importantly, it's superseded: this piece
      was built 2026-05-23, silently wiped 2026-05-26, and
      _get_combined_station_bias() (weather_markets.py) was built
      independently 2026-05-30 with no awareness NBM-delta had ever existed
      -- it corrects the bot's forecast toward real settled outcomes (dynamic
      METAR-learned bias, weight growing with sample count), a strictly
      better-grounded signal than correcting toward another model's guess
      ever was. User confirmed closing as not-needed rather than restoring.
    - Stop-loss accuracy tracking: DONE, but rebuilt against the current
      architecture, not ported mechanically. The original targeted
      tracker.live_fills, which today is a narrow slippage-only table
      (#P10.4) with zero rows ever, since no live order has been placed
      (ENABLE_MICRO_LIVE=False) -- auditing it would find nothing. The real
      settled stop-loss history lives in paper.py's trade ledger instead.
      close_paper_early() gained an optional reason= param, stored as
      exit_reason (separate from the existing outcome="early_exit" value so
      current semantics/tests are unchanged); cron.py's stop-loss call site
      (not its sibling breakeven-stop call site) now passes reason="stop_loss".
      tracker.get_stop_loss_accuracy(stop_loss_trades) takes pre-filtered
      paper trades and joins them against tracker.outcomes (excluding
      disputed rows, same as every other calibration consumer) -- the
      market still settles on Kalshi regardless of whether the bot's own
      position was closed early, so the real outcome is still recoverable.
      paper.get_stop_loss_accuracy() is a thin zero-arg wrapper (filters
      get_all_trades() to exit_reason=="stop_loss", hands them to tracker's
      join) wired into web_app.py's /api/analytics dispatch alongside its
      paper-module siblings (get_rolling_sharpe etc.) -- same
      wire-it-in-rather-than-leave-it-orphaned precedent as every other
      reporting function this session.
    - BETWEEN_FLOOR_MODEL_MAX env var: DONE. The 0.15 model-probability
      threshold was still hardcoded in weather_markets.py's between_floor
      gate; extracted to utils.BETWEEN_FLOOR_MODEL_MAX (default "0.15",
      no behavior change) and added to get_config_fingerprint() alongside
      every other env-configurable constant.
    - BETWEEN_FLOOR_MARKET_MIN env var: CLOSED, not applicable -- nothing
      left to extract. The gate's original "market > 0.30" condition (what
      this env var would have configured) was found buggy and deliberately
      redesigned away in a later fix, unrelated to this session (see the
      comment at weather_markets.py's between_floor gate: "The old condition
      (market > 0.30) was wrong... blocked profitable NO trades while never
      catching the suspicious YES case it was meant for"). The gate now
      compares against the live market price directly, not a hardcoded
      floor -- there's no static "0.30" left to make configurable.
    - Circuit-breaker threshold env-var overrides: CLOSED, not applicable --
      traced against the actual 24559a7 diff and found circuit_breaker.py
      was never one of the 9 files it touched at all. The only
      circuit-breaker-adjacent content that WAS lost (recovery_timeout=300
      settings on weather_markets.py's _ensemble_cb/_nbm_om_cb) is already
      present in current code. This backlog sub-item doesn't correspond to
      any real lost feature -- looks like a stale claim from the original
      research pass, not a genuine gap.

Problem:
  Found 2026-07-12 while investigating the LV/NOLA sigma backlog entry above.
  Commit 24559a7 (2026-05-26, "fix: handle str _date in signals_cache append
  (cron.py)") claims to be a 1-line cron.py fix. Its actual diff is
  -780/+103 lines across 9 files. Confirmed via `git diff <old-sha>:file
  24559a7:file`: weather_markets.py and climatology.py at 24559a7 are BYTE-
  IDENTICAL to their state at c348100~1 -- i.e. before commit c348100 (the
  "dynamic NBM-delta bias correction + climate-derived sigma" merge) and
  before 9 more legitimate commits that landed between c348100 and 24559a7's
  direct parent (cb14115). This has the signature of an accidental
  `git checkout <old-sha> -- <files>` baked into what was meant to be a small
  fix, not an intentional revert -- nothing in the commit message or history
  suggests the sigma/bias work was being deliberately rolled back.

  Checked each lost piece against current HEAD (166 commits past 24559a7, as
  of 2026-07-12) to separate what's genuinely still gone from what got
  independently rebuilt later:

  STILL MISSING AS OF THE ORIGINAL 2026-07-12 RESEARCH PASS (all resolved
  since -- see the piece 1/2/3 resolution notes above; kept for historical
  context on what was actually lost):
    - climatology.py's compute_sigma_from_climate()/load_all_sigmas() --
      derived per-city, per-month forecast sigma from a 30yr ERA5 reanalysis
      archive (clim std x 0.60 ~= NWS Day-3 RMSE), cached to
      data/forecast_sigma.json. Unlike a learned-from-settled-trades
      approach, this is independent of this bot's own trade history --
      it would cover LasVegas/NewOrleans immediately (see the LV/NOLA sigma
      entry above, which is otherwise blocked on settled-trade counts of
      2 and 0 respectively). Restoring this would resolve that entry as a
      side effect, the same way the LV/NOLA entry already anticipated a
      "data-driven sigma" fix might.
    - NBM-delta bias correction (GFS-minus-NBM) replacing hand-coded
      per-city degree offsets in weather_markets.py's bias-correction path.
    - Disputed-outcome tracking (tracker.py) -- a `disputed` column +
      mark_outcome_disputed(), with brier_score/brier_score_by_method/
      get_brier_by_days_out all excluding disputed rows so a corrupted
      settlement label can't silently pollute calibration scoring. This is
      the one piece with real safety/integrity implications, not just
      accuracy -- it's been absent with nobody aware it was ever built.
    - Stop-loss accuracy tracking (tracker.py) -- was_stop_loss/
      stop_loss_exit_price columns + get_stop_loss_accuracy(), an audit of
      whether stop-losses actually save money vs. prematurely exit winners.
    - BETWEEN_FLOOR_MODEL_MAX/BETWEEN_FLOOR_MARKET_MIN env-var
      configurability for the 0.15/0.30 between-probability floor magic
      numbers (back to hardcoded).
    - Circuit-breaker threshold env-var overrides -- note circuit_breaker.py
      was later fully rebuilt from scratch in a 2026-07-11 session (see
      project memory), but that rebuild does not have env-var-configurable
      thresholds either, so this specific piece is still absent even though
      the file it belonged to has since been substantially rewritten.
    - Sigma-cache validation (_validate_sigma_cache(), warns on values
      outside [1.0, 20.0]F) -- moot on its own since the cache system it
      validated is itself gone; would come back for free if the sigma cache
      is rebuilt.

  CONFIRMED INDEPENDENTLY REAPPLIED SINCE (false alarms, do not re-do):
    - `unretire`/`unretire-strategy` CLI command (main.py) -- present.
    - MIN_BIAS_CORRECTION_TRADES env var + learned-weights TTL-warning
      suppression below that threshold -- present (weather_markets.py:172-173).
    - Gaussian sigma 1.5F effective floor -- present (weather_markets.py:5599).

  NOT re-verified in this pass (lower-value/cosmetic per the original
  31c7226 commit message, worth a quick check before restoring the rest but
  not independently investigated): exact horizon-dependent sigma multipliers
  (Day-1 x0.50/Day-2 x0.70/Day-3+ x1.00 -- some horizon-discount concept
  exists at weather_markets.py:5634 but wasn't diffed against the original
  values), between_floor warning-message wording, cron.py's "portfolio is
  flat"/retirement-alert print statements, and a root-logger-to-module-logger
  cleanup in load/save_learned_weights.

What the fix looked like: [DONE, see resolution notes above for all 3 pieces]

Why not now: N/A -- fully resolved.

When to revisit:
  - The "NOT re-verified" list above (horizon-dependent sigma multipliers,
    between_floor warning-message wording, cron.py print statements, a
    root-logger cleanup) was deliberately left out of scope across all three
    restoration passes -- low-value/cosmetic per the original commit
    message. Worth a quick check if ever idle, not a real gap.
  - Worth a `git log --oneline -- <file>` sanity check on any other
    core file (paper.py, main.py, web_app.py, utils.py were also touched by
    24559a7 but not individually re-verified here beyond the grep checks
    above) if this revert pattern is ever suspected elsewhere.

[HOURLY-DIRECTIONAL TEMPERATURE MARKETS — KXTEMPxxxH]
Priority: Low/Medium (one city has real live volume right now; the rest don't)

Problem:
  Kalshi's "Climate and Weather" category also lists an "Hourly Directional
  [City] Temperature" market family, structurally invisible to this bot:
  check_series_drift() only ever diffs KXHIGH*/KXLOW* prefixed series, so
  this family never surfaces as a gap no matter how much volume it gets.
  Confirmed live 2026-07-08: KXTEMPMIAH, KXTEMPBOSH, KXTEMPLAXH, KXTEMPCHIH,
  KXTEMPAUSH, KXTEMPDCH, KXTEMPNYCH all exist; only KXTEMPNYCH currently has
  open markets (24 of them, e.g. KXTEMPNYCH-26JUL0804-T76.99 -- an hourly
  threshold, not a daily high/low). The other 6 cities are dormant right now.

What the fix looks like:
  This isn't a ticker-list fix -- it needs a per-hour threshold-crossing
  probability model instead of the daily high/low ensemble blend this bot
  runs today. The good news: the same-day nowcasting infra this would lean
  on (live METAR observation override, persistence baseline, HRRR same-day
  fetch -- see analyze_trade()'s days_out==0 path in weather_markets.py, and
  cron.py's "Same-day markets... re-enabled" same-day trading logic) already
  exists and is live for daily markets, not still-deferred as an earlier
  version of this backlog entry assumed. Still needs new work though: the
  existing pipeline reasons about "will today's high/low cross X," not
  "will the temp at hour H cross X" -- the per-hour target and the shorter
  settlement window are a real design difference, not just reused as-is.

  Calibration follows the same precedent already established for same-day
  vs multi-day: ml_bias.train_all_temperature_scaling() fits a dedicated
  'sameday' T from days_out=0 rows only, with apply_temperature_scaling()
  enforcing zero fallback to the global/multi-day T (their probability
  distributions are too different to share a scale -- see commit chain
  c773474 -> 9f5bb40, 2026-06-02/05). An hour-level model needs the same
  treatment: its own training pool and its own T, not folded into either
  the daily same-day pool or the multi-day pool. Don't assume the existing
  'sameday' calibration path could just absorb hourly rows -- it can't
  without the same care taken to keep the pools separate.

Why not now:
  - No existing model for the hour-level target specifically (only daily).
  - Only 1 of 7 cities in this family is currently even active.
  - Worth confirming KXTEMPxxxH volume is durable (not a one-off) before
    investing in an hour-level model.

When to revisit:
  - If KXTEMPxxxH activity broadens beyond just NYC, or NYC volume proves
    durable over a few weeks.
  - Given the reusable infra already exists, this is lower-effort than it
    looked when first logged -- re-scope before deprioritizing further.

[RAIN / SNOW / HURRICANE MARKETS — UNTOUCHED CATEGORY SURFACE]
Priority: Low (net-new product line, not a gap in the existing one)

Problem:
  "Climate and Weather" has 287 series total; this bot only ever looks at
  ~45 of them (KXHIGH*/KXLOW*, 20 tracked cities). Live-checked 2026-07-08:
  16 KXRAIN* city series exist, 2 currently with real open volume
  (KXRAINDENM -- Denver rain, 7 open markets; KXRAINSTPM, 7 open markets);
  ~23 snow series and ~40 hurricane series (path/category/landfall by city)
  exist but were all dormant/closed at check time. Separately, a handful of
  real weather markets (RAINNYC, KXRAINMIA, KXRAINNOSB) live entirely
  outside the "Climate and Weather" category (filed under "World"), so even
  widening check_series_drift's prefix filter within that one category
  wouldn't catch those.

What the fix looks like:
  Not a ticker-coverage fix -- each market type needs its own forecasting
  model with no shared architecture with the temperature-ensemble blend
  this bot runs today: precipitation-probability modeling for rain,
  snowfall-total modeling for snow, storm-track/intensity modeling for
  hurricanes. Treat as three separate potential product lines, not one
  task. If ever prioritized, rain is the most promising starting point --
  it already has real live volume (Denver/St. Paul) and precip-probability
  modeling is a smaller lift than storm-tracking.

Why not now:
  - No existing model architecture for any of the three.
  - Effort scales per market type, not incrementally with the current bot.
  - check_series_drift()'s KXHIGH*/KXLOW*-only filter is a deliberate
    simplification of an already-noisy check; widening it without picking
    a market type to actually act on would just add more unactionable noise.

When to revisit:
  - If there's real appetite to add a new market type (not just "cover more
    tickers") -- start with rain given current live volume.

[TRACK ECMWF FORECAST ACCURACY]
Priority: Low (ECMWF already gets a reasonable static/seasonal weight; this is
  about making that weight data-driven like ICON/GFS already are)

Problem:
  ECMWF's per-model forecast accuracy is never captured anywhere, so it can
  never earn a learned weight the way icon_seamless/gfs_seamless can.
  paper.py's _score_ensemble_members() only ever logs icon_forecast_mean and
  gfs_forecast_mean to tracker.ensemble_member_scores (see the comment left
  in model_means there) -- no ecmwf_forecast_mean field exists anywhere in
  the trade-entry -> settlement -> scoring pipeline. _forecast_model_weights()
  and _model_weights() (weather_markets.py) both correctly fall back to a
  static seasonal default for ECMWF forever, since the tracker data that
  would let them do better structurally can't exist yet.

  Note there are TWO separate ECMWF products in play, tracked independently:
  ecmwf_ifs025 (deterministic, used by the daily 3-model blend in
  get_weather_forecast()) and ecmwf_aifs025_ensemble (ML ensemble, used by
  get_ensemble_temps()'s probability blend). Fixing one does NOT give the
  other a learned weight -- they're unrelated model runs on different
  Open-Meteo endpoints (FORECAST_BASE vs ENSEMBLE_BASE).

What the fix looks like:
  1. Decide which ECMWF product to instrument first. Recommend ecmwf_ifs025
     (the daily-blend one) since that's the one that was actually silently
     dropped from fetches until the 2026-07-07 fix (commit 5a3d80b) -- it's
     the more consequential of the two for real trade decisions.
  2. Add an ecmwf_forecast_mean field and thread it through the same path
     icon_forecast_mean/gfs_forecast_mean already take: weather_markets.py's
     analyze_trade() (near _get_consensus_probs(), ~line 4478) -> the analysis
     dict returned to callers -> order_executor.py/main.py's calls into
     paper.open_paper_trade(...) -> paper.py's trade schema -> finally added
     to _score_ensemble_members()'s model_means dict (~paper.py:1153 as of
     2026-07-10).
  3. _get_consensus_probs() (weather_markets.py ~line 4274 as of
     2026-07-10) currently only
     calls its generic, model-agnostic _model_prob_and_mean() helper for
     "icon_seamless" and "gfs_seamless". Extending it for
     ecmwf_aifs025_ensemble would be mechanically easy (same ENSEMBLE_BASE/
     _ensemble_cache infrastructure already used there). Extending it for
     ecmwf_ifs025 instead requires NEW plumbing mirroring this pattern
     against FORECAST_BASE, since that's a different endpoint entirely --
     don't assume this is a one-line change once ecmwf_ifs025 is picked.
  4. Redesign the currently-binary model_consensus check
     (abs(icon_p - gfs_p) > 0.12) for 3 models -- decide between an
     all-pairwise-agreement check or a max-min-spread check. This is a real
     design decision, not just plumbing -- don't rush it.
  5. Once the field exists, add "ecmwf_ifs025": trade.get("ecmwf_forecast_mean")
     to _score_ensemble_members()'s model_means dict.

Why not now:
  - The urgent bug (ECMWF silently excluded from live fetches) is already
    fixed and does not depend on this -- ECMWF now always gets fetched with
    a reasonable seasonal-default weight regardless.
  - This is new instrumentation across ~4 files, not a bug fix -- meaningfully
    more invasive than the fetch-list fix.
  - Won't pay off for weeks even after shipping: needs 10+ fresh settled
    trades per city (same MIN_OBSERVATIONS/min_n gates icon/gfs already use)
    before the learned weight would ever activate.
  - Requires a real design decision (which ECMWF product, how to redesign the
    3-way consensus check) that deserves its own scoping pass, not a
    rushed addition to an unrelated bug-fix session.

When to revisit:
  - Have bandwidth for a multi-file instrumentation change, not a quick fix.
  - Decide up front whether ecmwf_ifs025 (daily blend, drives the probability
    calc real trades actually use) or ecmwf_aifs025_ensemble (ensemble
    consensus data) matters more before starting -- see note above, they are
    not interchangeable.

[RESOLVED 2026-07-12] LAS VEGAS / NEW ORLEANS MISSING FROM _HISTORICAL_SIGMA
Priority: was Low-Medium (silently degraded signal quality for 2 of 20 cities,
  not a crash risk)

RESOLVED 2026-07-12: resolved as a side effect of restoring climatology.py's
  climate-derived sigma (see the [MYSTERY REVERT -- COMMIT 24559a7] entry's
  piece 2, same day) exactly as this entry's 2026-07-12 update anticipated.
  get_historical_sigma() now tries a per-city/per-month dynamic sigma (from
  the already-cached 30yr climate archive) before falling back to the static
  _HISTORICAL_SIGMA table -- LasVegas/NewOrleans get a real value from
  data/climate_LasVegas.json/data/climate_NewOrleans.json (both already
  cached on disk) without needing a _HISTORICAL_SIGMA table entry or any
  settled-trade history at all. Verified against the real cached archives
  (not just mocked): LasVegas max 3.19-5.44F, min 2.77-4.30F; NewOrleans max
  1.94-5.54F, min 1.50-5.47F -- both sane, both no longer the flat
  _DEFAULT_SIGMA=3.5 every LV/NOLA trade used before. Resolves for all 20
  cities, not just these 2 (any city later added to CITY_COORDS without a
  static table entry now gets a real sigma too, once its climate archive is
  cached).

UPDATED 2026-07-10: the correlation-groups half of this entry is DONE --
  _CORRELATED_CITY_GROUPS (now paper.py:283-287) includes LasVegas (desert-
  Southwest group) and NewOrleans (Gulf-Coast group), with explicit pair
  correlations in _CITY_PAIR_CORR (paper.py:313-332). Only the sigma half
  remains open.

Problem (remaining):
  _HISTORICAL_SIGMA (weather_markets.py:2067-2086) has exactly 18 keys,
  missing LasVegas/NewOrleans. get_historical_sigma()
  (weather_markets.py:2097-2104) silently falls back to _DEFAULT_SIGMA=3.5
  for these two, feeding directly into gaussian_probability() for every
  trade analyzed -- i.e. every LV/NOLA trade uses a generic sigma instead
  of a city-tuned one.

What the fix looks like:
  Add LasVegas/NewOrleans entries to _HISTORICAL_SIGMA (compute from
  historical data the same way the other 18 were derived, not just copy the
  default). Note the [DATA-DRIVEN SIGMA FROM SETTLED HISTORY] feature
  candidate below solves this as a side effect -- if that gets built, this
  entry closes with it.

Why not now:
  - Signal degradation, not a crash or a live-money-losing bug -- the
    generic sigma is a safe-side default, not an obviously wrong one.

UPDATE 2026-07-12 (research pass, no code changed): queried data/predictions.db
  directly (outcomes joined to predictions on settled_temp_f IS NOT NULL).
  NewOrleans has 0 settled trades; LasVegas has 2. Codebase precedent for a
  sample floor before trusting a per-city statistic (ACCURACY_MIN_SAMPLE=20,
  SPRT_MIN_TRADES=20, MIN_BIAS_CORRECTION_TRADES=50) is nowhere close to
  cleared -- not fixable with real settled-trade data right now regardless of
  approach. Separately, and more importantly: found while investigating this
  entry that a working, settled-trade-INDEPENDENT sigma source
  (climatology.py's compute_sigma_from_climate()/load_all_sigmas(), derived
  from a 30yr ERA5 archive per city/month) was built 2026-05-23 and then
  silently deleted 3 days later by an apparently-accidental commit -- see the
  new [MYSTERY REVERT -- COMMIT 24559a7] entry near the top of this file.
  Restoring that mechanism would resolve this entry as a side effect,
  immediately, without needing any settled-trade history for LV/NOLA at all
  -- a stronger and faster fix path than either "wait for settled trades" or
  the DATA-DRIVEN SIGMA FROM SETTLED HISTORY feature candidate below (which
  IS settled-trade-dependent and would still be blocked). Prioritize the
  24559a7 entry's restoration over either of those two paths when this is
  next picked up.

When to revisit:
  - Restoring climatology.py's dynamic sigma (see 24559a7 entry) is now the
    recommended path -- resolves this immediately for all 20 cities, not
    just LV/NOLA. Fall back to "once LV/NOLA have enough settled trade
    history" only if that restoration is deprioritized.

[CLOSED 2026-07-12 -- evaluated live, not switching] OPEN-METEO ENSEMBLE MEAN
  API -- SIMPLER/CHEAPER THAN CURRENT MEMBER-BY-MEMBER FETCH
Priority: was Medium (efficiency + data-quality improvement, not a bug fix)

CLOSED 2026-07-12: made live test calls to the actual endpoint (not just docs)
  and found three real blockers, not one:
  1. Daily spread does not exist as a variable at all -- requesting
     temperature_2m_max_spread (or any daily _spread variant) 400s with
     "Cannot initialize ForecastVariableDaily from invalid String value."
     Spread is HOURLY-ONLY. Since this bot needs daily-high/low uncertainty,
     using this API would mean fetching hourly spread and picking the value
     near the forecast daily max/min hour -- reintroducing complexity, not
     eliminating it. Not the "get uncertainty for free" win it looked like.
  2. Spread comes back in Kelvin-degrees (hourly_units reports
     "temperature_2m_spread":"K") even when temperature_unit=fahrenheit is
     explicitly requested -- the mean value correctly converts to F, spread
     does not. A Kelvin-degree spread is NOT numerically equal to a
     Fahrenheit-degree spread; using it unconverted as sigma in
     gaussian_probability() would silently understate uncertainty by ~44%
     (missing x1.8 factor). Easy to get wrong silently.
  3. ECMWF AIFS mean (ecmwf_aifs025_ensemble_mean) returned null for daily
     max/min at the one live test point (NYC) -- same failure mode already
     documented in this repo's fetch_temperature_ecmwf() comments for the
     deterministic endpoint.
  Also re-confirmed and corrected the model-name guess yet again on live
  verification: ICON mean is dwd_icon_eps_ensemble_mean_seamless, GFS mean is
  ncep_gefs_ensemble_mean_seamless (NOT gfs_ensemble_seamless_mean -- GFS's
  ensemble is branded GEFS at Open-Meteo, guessing wrong 400s), ECMWF AIFS
  mean is ecmwf_aifs025_ensemble_mean (NOT ecmwf_aifs_025_ensemble_mean).
  Verdict: not a clean drop-in win as originally framed. Closing rather than
  leaving open-ended -- if ever revisited, it needs to solve the
  hourly-only-spread problem first, which is real added complexity, not a
  simplification.

Problem:
  Open-Meteo launched an Ensemble Mean API in March 2026 that returns mean
  and spread (standard deviation) directly per ensemble model, computed
  server-side from individual members. This bot's current approach
  (weather_markets.py's _model_prob_and_mean()/get_ensemble_temps(), used
  for icon_seamless/gfs_seamless and, per the TRACK ECMWF entry above,
  potentially ecmwf_aifs025_ensemble) fetches every individual ensemble
  member and computes mean/spread itself. Found 2026-07-10 via web search,
  not yet verified against the live Open-Meteo endpoint or this repo's
  actual fetch code in detail.

UPDATE 2026-07-12 (research pass, no code changed):
  Re-confirmed weather_markets.py's current fetch approach is still
  accurate as described above -- get_ensemble_temps() (weather_markets.py:2933)
  still calls _fetch_model_ensemble() per model and builds all_temps itself;
  no change since 2026-07-10.
  Confirmed via 2 independent fetches of open-meteo.com/en/docs/ensemble-mean-api
  that the endpoint is real:
  base URL https://ensemble-api.open-meteo.com/v1/ensemble, daily variables
  temperature_2m_max/temperature_2m_min are supported, response shape is
  {variable} for mean + {variable}_spread for std-dev, data retained since
  March 2026 (individual members only kept 3 days, so the mean/spread
  series is also the only way to get real historical ensemble data beyond
  that window -- a second reason to care about this beyond raw efficiency).
  IMPORTANT correction to the original note: the `models` parameter uses
  DIFFERENT string values than the raw per-member API this bot calls today
  -- e.g. `ecmwf_aifs_025_ensemble_mean` (mean API) vs this bot's
  `ecmwf_aifs025_ensemble` (raw API, note both the underscore placement and
  the "_mean" suffix differ), and `gfs_ensemble_seamless_mean` vs
  `gfs_seamless`. This is not a drop-in endpoint swap with the same model
  identifiers -- confirm the exact spelling with one live test call before
  writing any code, not from docs-summary text alone (same caution that
  mattered for the Kalshi V2 order-endpoint migration this session).

(Original "what the fix looks like" section removed 2026-07-12 -- superseded
  by the CLOSED verdict above, which found 3 real blockers a simple
  model-name fix wouldn't address.)

[CLOSED 2026-07-12 -- not currently achievable, see below] CLIMATE INDEX
  TABLES (AO/NAO/ENSO) MISSING 10 OF 20 CITIES
Priority: was Low (signal degradation, safe-side default fallback already in
  place)

CLOSED 2026-07-12: two findings that together make this not a boundedly-
  scoped task right now, not just a low-priority one.
  First, git-archaeology on climate_indices.py (git log -p on the file, only
  7 commits total) found the ao_sens/nao_sens/enso_sens dicts for the
  ORIGINAL 10 cities appear once, in the very first commit, and have never
  been modified since. No docstring, comment, docs/ file, or commit message
  anywhere references a study, NOAA regression, or any data source -- unlike
  _PDO_TEMP_COEFF/_PNA_TEMP_COEFF's explicit scoping comments nearby. These
  read as plausible hand-authored estimates from initial repo creation, not
  values derived from any real analysis -- so "follow whatever methodology
  produced the first 10" (the fix this entry originally proposed) has no
  methodology to follow.
  Second, even if a real regression were attempted from scratch: queried
  data/predictions.db's outcomes table for settled_temp_f coverage -- 231
  total rows, spanning only 2026-05-19 to 2026-07-09 (under 8 weeks, one
  season, ZERO winter data). AO/NAO effects are largest in winter, which is
  exactly the regime this window can't see. Per-city counts are 3-14 rows
  even for the best-covered cities. A regression against monthly AO/NAO/ONI
  values with this little same-season data would be statistically
  meaningless -- and wouldn't even be reproducible for the original 10
  cities using this repo's own data, let alone extendable to 10 more.
  Verdict: a real, data-backed fix needs decades of external per-city
  historical daily temperature (NOAA GHCN/CLI archives) regressed against
  NOAA CPC's public AO/NAO/ONI series -- genuine external research, not
  something derivable from what's in this repo today. The only thing
  achievable right now is extending the same unscientific hand-authored-
  estimate pattern the original 10 already use, which would be internally
  consistent with the file's actual origin but shouldn't be presented as a
  data-driven fix. Closing rather than leaving open-ended; revisit only if
  someone is willing to do the external research this actually requires.

Problem:
  climate_indices.py's ao_sens/nao_sens/enso_sens dicts inside
  temperature_adjustment() (climate_indices.py:212-251) only have the
  original 10 cities (NYC, Boston, Chicago, Miami, LA, Dallas, Phoenix,
  Seattle, Denver, Atlanta) -- none of the 10 cities added later to
  CITY_COORDS (Austin, Washington, Philadelphia, OklahomaCity, SanFrancisco,
  Minneapolis, Houston, SanAntonio, LasVegas, NewOrleans) appear. Confirmed
  this isn't an intentionally-scoped subset the way _PDO_TEMP_COEFF/
  _PNA_TEMP_COEFF (climate_indices.py:354-367) explicitly are (those have
  inline comments restricting them to "west-coast cities" / "central and
  eastern US", and the caller documents the scoping) -- no such comment
  exists for ao_sens/nao_sens/enso_sens. temperature_adjustment() is called
  unconditionally for every city in analyze_trade() (weather_markets.py:5550),
  silently falling back to generic sensitivities (.get(city, 0.5) etc.,
  climate_indices.py:253-255) for the 10 newer cities -- half the tracked
  roster gets untuned AO/NAO/ENSO adjustments.

(Original "what the fix looks like" section removed 2026-07-12 -- superseded
  by the CLOSED verdict above, which found no real methodology exists to
  follow and no data currently exists to build one from scratch.)

[RESOLVED 2026-07-12] MORE INERT config.py FIELDS -- SAME DIVERGENCE PATTERN, ROUND 4
Priority: Low-Medium (silent no-op env vars, same already-recognized bug
  class)

RESOLVED 2026-07-12 (second pass, same day): all 6 fields now have a real
  decision, not just documentation. max_city_date_exposure, method_kelly_gate,
  and min_arb_edge were wired up for real -- the hardcoded shadow constants
  in paper.py (MAX_CITY_DATE_EXPOSURE=0.25, _METHOD_MIN_SAMPLES=50) and
  main.py (guaranteed_edge < 0.05) were moved into utils.py as real
  os.getenv-backed constants (matching each's CURRENT live value exactly, so
  no behavior change), paper.py/main.py now import and use them, and
  config.py's fields derive their defaults from utils.py via new
  _live_max_city_date_exposure()/_live_method_kelly_gate()/_live_min_arb_edge()
  functions (same pattern as _live_max_days_out). kelly_cap was similarly
  consolidated (_live_kelly_cap()) so its two independently-hardcoded "0.25"
  literals can't diverge later, even though it was already safety-netted.
  min_kelly_fraction and partial_exit_pct -- confirmed still zero consumers
  anywhere -- were deleted from BotConfig entirely (plus partial_exit_pct's
  validate() check), rather than inventing unspecified trading semantics.
  _DEAD_FIELD_ALLOWLIST in tests/test_config_divergence_guard.py updated to
  match: the 3 "misleading" entries became "enforced via utils.X" entries
  (like breakeven_trigger_pct), and the 2 unimplemented entries were removed
  along with their fields. Independently reviewed (Agent, opus) -- no bugs
  found, all three live-value literals confirmed exact matches to prior
  hardcoded behavior. Pushed together with the dead-code-scan work below.

UPDATE 2026-07-12 (documented + guarded, NOT wired/deleted): re-verified all
  5 fields live while building tests/test_config_divergence_guard.py (see
  the NO AUTOMATED GUARD entry above). One correction to the KELLY_CAP claim
  below: it's stale -- utils.py's KELLY_CAP is now os.getenv-backed (fixed
  in an earlier 2026-07-11 session, `float(os.getenv("KELLY_CAP", "0.25"))`)
  -- so it's no longer "not even os.getenv-backed", it's just still a
  SEPARATE env read from config.py's own kelly_cap field (same default,
  coincidentally, so no live divergence -- but still two independently
  hardcoded "0.25" literals that could diverge later). All 5 fields (plus a
  6th found the same way: max_days_out, which WAS a live divergence --
  "3" vs "5" -- fixed same session) are now individually documented with a
  concrete reason in _DEAD_FIELD_ALLOWLIST, and the new guard test would
  catch either field silently going MORE wrong (a value mismatch) even
  though it doesn't fix the "config field lies about being configurable"
  problem itself. The actual "wire up or delete" decision for all 5 is
  still not made -- still needs the dedicated pass described below.

Problem:
  Following the same config.py/utils.py divergence pattern already found
  and fixed 3 times in this project (BREAKEVEN_TRIGGER_PCT,
  MAX_SAME_DAY_SPEND, PAPER_MIN_EDGE), grepped every BotConfig field name
  across the whole codebase (excluding config.py/utils.py/tests/) and found
  5 more fields that are parsed from env but never actually read by real
  trading logic:
    - KELLY_CAP (config.py:201) -- the real cap enforced everywhere
      (paper.py:634,1532; weather_markets.py:4360) is utils.py:64's
      hardcoded KELLY_CAP=0.25, which isn't even os.getenv-backed (its own
      comment says "Operative production cap is 0.25"). The env var is
      fully inert on top of an already-inert config field.
    - MAX_CITY_DATE_EXPOSURE (config.py:216-218) -- the real cap enforced
      in paper.py:1390-1408 is a separate hardcoded module constant of the
      same name, paper.py:161 (=0.25). Setting the env var has zero effect.
    - METHOD_KELLY_GATE (config.py:213-215) -- the real per-method Kelly
      gate is a hardcoded local _METHOD_MIN_SAMPLES=50 in paper.py:517-527,
      unrelated to this field/env var.
    - MIN_ARB_EDGE (config.py:226-228) -- STALE CLAIM CORRECTED 2026-07-10:
      arb auto-placement HAS since been built (main.py's _analyze_once arb
      block, ~main.py:1542-1660, paper-only, with volume gates + $25
      per-city exposure cap + live-gate check) -- but it gates on a
      HARDCODED `v.guaranteed_edge < 0.05` (main.py:1602), not the config
      field. So MIN_ARB_EDGE (default 0.03) is still fully inert, and the
      fix is now trivial: replace the hardcoded 0.05 with the config field
      (deciding deliberately whether 0.03 or 0.05 is the right default).
    - min_kelly_fraction (config.py:202-204) -- no Kelly-sizing code
      anywhere references a minimum-fraction floor.

What the fix looks like:
  For each: either wire the config field into the real enforcement point
  (replacing the hardcoded constant), or delete the dead field/env var if
  it's not actually wanted. MIN_ARB_EDGE and min_kelly_fraction may be
  genuinely unimplemented features rather than divergence bugs (no
  hardcoded shadow value exists for them, unlike the other 3) -- worth
  distinguishing "should wire this up" from "should delete this" per field
  rather than treating all 5 identically.

Why not now:
  - Not urgent: the real values enforced today (utils.py's KELLY_CAP=0.25,
    paper.py's MAX_CITY_DATE_EXPOSURE=0.25) are reasonable production
    values -- this is a "the env var lies about being configurable"
    problem, not a "wrong value in production" problem.
  - 5 fields is enough scope to warrant a dedicated pass rather than a
    drive-by fix, similar to how PAPER_MIN_EDGE's fix turned into a
    multi-session effort once the full blast radius was traced.

When to revisit:
  - Next time someone tries to actually change one of these 5 values via
    .env and is confused why it has no effect -- that's the actual trigger
    that surfaced the first 3 instances of this pattern too.

[metar.get_station_bias() IS A PERMANENT NO-OP STUB]
Priority: Low (safe no-op today, real feature never finished)

Problem:
  metar.py:437-461 get_station_bias() does real filtering/threshold work
  (station lookup, proxy-observation exclusion, _MIN_OBS_FOR_MODEL/
  20-per-month gates) and then unconditionally returns None
  (metar.py:459-461, with its own TODO: "implement per-station bias model
  once forecast_high is stored alongside observations. Until then, return
  None so callers skip this correction safely."). Independently corroborated
  by this repo's own AUDIT_PLAN.md (P4-5) and docs/audit_findings.md (C21),
  both still open.

What the fix looks like:
  Per the function's own TODO: needs forecast_high stored alongside
  observations before a real per-station bias model can be computed. Scope
  that storage change first, then implement the actual bias model.

Why not now:
  - Currently safe (callers correctly skip the correction when None is
    returned) -- this is a missing feature, not a live bug.
  - Needs the forecast_high storage prerequisite before the real work can
    start.

UPDATE 2026-07-12 (research pass, no code changed): re-verified still
  correctly blocked. get_station_bias() (now metar.py:437-461) still ends in
  an unconditional `return None` with the same TODO comment. Grepped the
  whole codebase for "forecast_high" -- confirmed it does not exist as a
  stored column/field anywhere (metar.py's own record_observation() only
  writes station_id/date/high_f/low_f/proxy; tracker.py's predictions table
  stores our_prob/market_prob/edge, not a forecast temperature, and has no
  join key to METAR observations by station+date). The named prerequisite is
  fully absent, not partially built. AUDIT_PLAN.md P4-5 and
  docs/audit_findings.md C21 both still open with the same (now slightly
  stale-worded, e.g. C21 cites old line numbers and says "returns 0.0" when
  it's actually "returns None") but substantively correct finding. If ever
  picked up: needs record_observation() to accept a new forecast_high
  parameter, AND the harder part -- plumbing the forecast value from wherever
  the trade decision is made through to wherever settlement recording calls
  record_observation() (likely paper.py around its settlement path), which is
  currently out of scope at that call site.

When to revisit:
  - When forecast_high is stored alongside observations for some other
    reason, or if a specific city's METAR-based lock-in confidence looks
    wrong in a way this correction would address.

[RESOLVED 2026-07-12] ORPHANED FUNCTIONS -- BUILT, NEVER WIRED IN OR CALLED
Priority: Low (cleanup / decide-and-close, not urgent)

RESOLVED 2026-07-12: all 4 got a real per-function decision, not a batch
  fix. slippage_kelly_scale was DELETED -- paper.py already has a wired-in,
  actively-used slippage mechanism that adjusts entry PRICE instead
  (estimate_slippage/slippage_adjusted_price, #50), making this a redundant/
  superseded alternate approach to the same illiquidity problem, not a real
  gap. _fetch_ensemble_members_historical was DELETED -- its only plausible
  caller, backfill_emos_data(), has a docstring explicitly stating ens_var is
  deliberately left NULL because "individual ensemble members are only
  stored for 3 days and no consistent-scale proxy exists," confirming the
  codebase already decided against the historical-members approach this
  function supported. get_calibration_by_season was WIRED UP -- added to
  web_app.py's /api/analytics getattr-dispatch tuple alongside its sibling
  get_model_calibration_buckets (read-only reporting, zero trading risk).
  undo_last_trade was WIRED UP -- new main.py cmd_undo() + `undo` CLI
  dispatch entry, manually smoke-tested (`python main.py undo` runs clean).
  A 5th orphaned function was found independently while building the
  dead-code scan (see below): is_weather_market (+ its now-also-dead
  WEATHER_KEYWORDS set) was DELETED -- a comment already in weather_markets.py
  documents that the global open-market scan it supported was removed in
  favor of KNOWN_WEATHER_SERIES direct queries, the same
  "superseded-by-a-different-approach" shape as slippage_kelly_scale. Two
  vestigial monkeypatch lines referencing it in tests/test_p1_remaining.py
  and tests/test_regression.py were removed (patching a function nothing
  calls is a no-op; confirmed via re-run that both tests' actual behavior is
  unchanged). Independently reviewed (Agent) -- see the dead-code-scan
  entry below for the combined review result. Pushed together with the
  config-field work above.

  The scan built for the guard (below) surfaced ~19 MORE tested-but-
  unreachable functions beyond these 5 -- NOT fixed today, deliberately
  scoped out (see that entry for the list and reasoning).

RESOLVED 2026-07-12 (follow-up session, same day): the ~19-function
  follow-up triage backlog above was fully worked through (actually 20 --
  one more, tracker.get_unselected_bias, was found independently while
  fixing a real scan bug, see below). Every one got a real per-function
  wire-up-or-delete decision:
    DELETED as superseded by an already-live different approach (11, plus 2
    cascading orphans created by these deletions): paper.portfolio_kelly
    (self-documented dead), paper.simulate_fill + simulate_partial_fill
    (abandoned early partial-fill design, superseded by the simpler
    Gaussian-noise approach actually in place_paper_order), paper.
    calc_trade_pnl (implemented the REJECTED settlement approach --
    settle_paper_trade's own P1-8 comment explicitly says actual_fill_price
    must NOT affect P&L, the opposite of what this function did),
    paper.estimate_slippage (cascading orphan once simulate_fill was
    deleted -- slippage_adjusted_price is the real, different-formula
    function actually wired into place_paper_order), tracker.
    analyze_all_markets (batch_log_analysis_attempts, called from cron.py,
    already does this job), weather_markets.apply_station_bias
    (_get_combined_station_bias is a strictly more complete version --
    static table + dynamic METAR blend -- already live in the real
    forecast pipeline), weather_markets._compute_ensemble_mean (an early
    Phase-C plan's simple-mean approach, superseded by the much richer
    live EMOS/ens_stats pipeline), weather_markets.
    _blend_with_circuit_fallback (the same circuit-breaker-exclusion logic
    is already inlined directly in analyze_trade), weather_markets.
    _blend_probabilities (superseded wrapper -- the real blend uses
    _confidence_scaled_blend_weights inline; _blend_weights itself, which
    it wrapped, is still real and has 3 live callers), weather_markets.
    bayesian_kelly_fraction + cascading _normal_quantile (production Kelly
    sizing calls kelly_fraction()/portfolio_kelly_fraction() directly; the
    live mechanism for "shrink Kelly under statistical uncertainty" is a
    completely different, already-wired one -- paper.py's Brier-based
    _method_kelly_multiplier/METHOD_KELLY_GATE -- wiring this in too would
    have double-corrected sizing), weather_markets.analyze_markets_parallel
    (cron.py's own _enrich_and_analyze + ThreadPoolExecutor is the real,
    more complete parallel-analysis path, with timeout/dedup/circuit-probe
    suppression this standalone version lacked).
    WIRED UP (7): tracker.get_brier_by_tier, brier_skill_score,
    get_model_brier_scores, get_optimal_threshold, and get_analysis_bias
    were added to web_app.py's /api/analytics getattr-dispatch tuple
    (read-only reporting, zero trading risk, same pattern as
    get_calibration_by_season's earlier wiring). weather_markets.is_stale
    (zero volume/OI + closing within 60 minutes -- "meaningless edge
    calculations" per its own docstring) was wired into cron.py's REAL
    live scan loop (_cmd_cron_body, the actual production path -- not
    main.py's _analyze_once, which only backs the interactive analyze/watch
    CLI display) as a pure risk-reducing exclusion filter, confirmed via a
    new integration test that the WS ticker subscription (which feeds the
    stop-loss path for any already-open position) still uses the
    unfiltered market list, so nothing downstream is affected by the
    exclusion. tracker.bayesian_confidence_interval (a correct, tested,
    #57 utility with no natural single call site) got a purpose-built
    minimal wrapper, tracker.get_rolling_win_rate_ci(), pairing it with
    get_rolling_win_rate's real (win_rate, count) data and wired into the
    same dashboard endpoint.
    KEPT UNWIRED, documented accurately rather than guessed at (3): each
    has its own in-code comment (not a guess made this session) citing a
    real, unmet prerequisite -- weather_markets._fetch_hrrr_temp ("NOT
    wired into analyze_trade yet -- that happens once HRRR data has been
    validated against settled same-day trades", not done),
    weather_markets.censoring_correction (correctly implements its #23
    spec, but wiring an ensemble-consensus probability correction into the
    live forecast pipeline needs backtesting to confirm it actually helps,
    not something to do blind), and tracker.get_unselected_bias (found
    independently this session -- see below -- whose own docstring says a
    ~2,000-extra-Kalshi-API-call settlement sweep is a real prerequisite
    that "has zero production callers today. Build that sweep before
    wiring this into anything real").
  Found and fixed a real bug in the scan itself while doing this triage:
  a full-line `#` comment that merely DISCUSSES a function -- e.g.
  "# ...and get_unselected_bias() always returned 0.0" -- matched the same
  \bname\s*\( regex as a genuine call, producing a false negative (the scan
  read get_unselected_bias as "has a caller" purely because of a comment
  mentioning it). Fixed by stripping full-comment lines before matching;
  re-running the corrected scan surfaced get_unselected_bias as newly
  dead-but-real, handled above, and confirmed no other false negatives of
  this shape existed. tests/test_dead_code_scan.py's _DEAD_CODE_ALLOWLIST
  now holds only the 3 deliberately-kept entries (down from ~19) -- the
  guard's docstring explicitly notes a large allowlist growing back would
  mean the guard isn't doing its job.
  Also added a `TestCmdUndo` test (main.py's cmd_undo CLI wrapper had no
  direct test until a second-opinion review from the PRIOR session flagged
  the gap) and a new cron.py integration test proving the is_stale wiring
  actually excludes a stale market from the real enrich_with_forecast
  call path (not just a unit test of is_stale in isolation).
  Independently reviewed as two tiers: HIGH (Agent, opus) for the one
  real live-trading-path change (is_stale into cron.py's scan loop) --
  came back clean, traced every downstream use of the filtered vs.
  unfiltered market list, confirmed no false-positive risk in is_stale's
  edge-case handling, one cosmetic docstring nit fixed (didn't mention the
  open-interest condition). MEDIUM (Agent) for the 11 deletions + 7
  wirings + test rewrites -- came back clean, independently re-derived
  each deletion's justification against current code (not just trusting
  this session's claims) and ran all affected test files directly
  (577 passed, 4 pre-existing skips, 0 failures). 677 tests pass across
  the full combined touched scope; lint/mypy clean; TRADING_PAUSED
  confirmed true throughout, never touched.

Problem:
  4 functions in core trading files are fully implemented but have zero
  callers anywhere in the codebase (including tests):
    - paper.slippage_kelly_scale() (now paper.py:1843) -- complete 4-tier
      volume/OI-based Kelly multiplier. Referenced only in a docs plan
      (docs/superpowers/plans/2026-04-14-p7-market-realism.md:8) as if it
      reduces Kelly sizing, but paper.py's actual sizing path never calls
      it (re-verified 2026-07-10, still zero production callers). Not to be
      confused with estimate_slippage/slippage_adjusted_price (fill-price
      slippage), which ARE wired in -- this is a different, unused
      mechanism. See also the [LIQUIDITY-AWARE SIZING] feature candidate
      below, which would wire this in.
    - paper.undo_last_trade() (now paper.py:2849) -- complete admin/undo
      implementation (refunds cost, recomputes peak_balance by replaying
      remaining trades). UPDATE 2026-07-10: now HAS tests
      (tests/test_paper.py:1806+, added in the 2026-07-09/10 review
      batches) but is still unreachable in production: no CLI command in
      main.py, no dashboard route in web_app.py.
    - tracker.get_calibration_by_season() (now tracker.py:1562) -- full
      seasonal Brier/bias breakdown, zero callers, no test, no dashboard
      endpoint (sibling get_calibration_by_city IS wired into the
      dashboard). Re-verified 2026-07-10.
    - tracker._fetch_ensemble_members_historical() (now tracker.py:2271) --
      full Open-Meteo archive-ensemble fetcher mirroring
      _fetch_previous_run_daily (which IS called from
      backfill_emos_data), zero callers. Re-verified 2026-07-10.

What the fix looks like:
  For each: either wire it into a real call site (CLI command / dashboard
  route / calling function) or delete it. Don't leave working code
  unreachable indefinitely -- per this repo's own convention (see how
  KNOWN_WEATHER_SERIES renames get commented, not silently dropped), a real
  decision beats silent dead weight.

Why not now:
  - None of these are broken or risky as-is -- pure cleanup, no urgency.
  - slippage_kelly_scale and undo_last_trade in particular look like they
    were meant to be used (a docs plan references the former; the latter
    is a reasonable admin tool) -- worth a real "wire up or delete"
    decision per function rather than batch-deleting.

When to revisit:
  - Low priority housekeeping -- next general cleanup pass, or when
    picking up the docs plan that references slippage_kelly_scale.

[MOSTLY RESOLVED 2026-07-12] MIXED-TIMESTAMP-FORMAT BUG CLASS -- RECURRING, NO ROOT-CAUSE FIX
Priority: was Low-Medium (individually low-impact so far, but a well-established
  recurring pattern -- fifth occurrence would not be surprising)

RESOLVED 2026-07-12 (mostly): get_brier_over_time (tracker.py:1471) already
  uses the correct strftime('%Y-%m-%d %H:%M:%S') cutoff (not .isoformat()) --
  fixed sometime before this date, this entry's "queued for its own fix
  pass" note is stale. Separately, added utils.sql_normalize_iso_column()
  as the shared "centralize all cutoffs through one helper" fix this entry
  asked for, and used it to consolidate the 3 remaining hand-duplicated
  instances of the underlying strftime/replace normalization expression
  (execution_log.py's was_recently_ordered/was_ordered_recently, tracker.py's
  v21->v22 settled_at migration) -- pure refactor, no behavior change,
  pushed af39a67. A full audit at that time found no remaining unfixed
  instances of the raw bug (mismatched-format comparison) anywhere in
  tracker.py/execution_log.py. NOT built: the "cheap regression test that
  greps for .isoformat() feeding a SQL bind parameter" lint-style guard --
  the shared helper reduces the chance of a NEW instance (nothing left to
  copy-paste wrong), but doesn't structurally prevent someone writing a
  fresh ad-hoc .isoformat() cutoff by hand. Worth adding if a fifth instance
  ever appears despite the helper existing.

Problem:
  A 2026-07-10 Fable adversarial review of tracker.py found the *identical*
  bug three separate times in one pass: get_brier_over_time,
  get_model_brier_scores, and get_recent_city_correlations each built a
  Python .isoformat() cutoff ('...T...+00:00') and compared it
  lexicographically against a column written by SQLite's datetime('now')
  ('YYYY-MM-DD HH:MM:SS') -- silently dropping the entire boundary day every
  time, since ' ' (0x20) < 'T' (0x54) in a TEXT comparison. This is the exact
  same bug class the v22 migration already fixed once for
  outcomes.settled_at (see the migration's own comment: "Mixed formats
  corrupt date-range queries that rely on lexicographic order"). Four
  independent occurrences of one mistake in the same file means a fifth is
  a when, not an if, absent a structural fix. (get_recent_city_correlations
  and get_model_brier_scores were fixed in the same 2026-07-10 session;
  get_brier_over_time's cutoff bug was found MEDIUM-confirmed and is queued
  for its own fix pass.)

What the fix looks like:
  Either centralize all "N days/weeks ago" cutoffs through one helper
  (e.g. _sql_cutoff(days) returning SQLite-format text, or pushing the
  cutoff into SQL via datetime('now', ? || ' days') the way
  get_member_accuracy/get_model_weights already do) instead of hand-rolling
  (datetime.now(UTC) - timedelta(...)).isoformat() at each call site, or add
  a cheap regression test that greps tracker.py for .isoformat() feeding a
  SQL bind parameter and fails the build/pre-commit hook.

Why not now:
  - Each individual instance found so far has been LOW/MEDIUM severity, not
    a live-trading-critical bug -- this is about stopping the pattern, not
    an urgent fix.
  - A real fix (the shared-helper version) touches every date-cutoff query
    in the file -- worth its own careful pass with tests, not a drive-by.

When to revisit:
  - Next time tracker.py is opened for other reasons, or the next time this
    exact bug shape is found a fifth time -- at that point the case for the
    structural fix (not just another one-off patch) becomes hard to ignore.

[RESOLVED 2026-07-12] NO AUTOMATED GUARD AGAINST CONFIG-DIVERGENCE / ORPHANED-RESOURCE DRIFT
Priority: was Low-Medium (tooling investment; every instance so far has been
  caught manually, after the fact)

RESOLVED 2026-07-12 (part b, same day as part a): built
  tests/test_dead_code_scan.py -- a regex+AST scan over paper.py/tracker.py/
  weather_markets.py (no vulture dependency added; a custom scan matches
  test_config_divergence_guard.py's existing style and needs no new
  requirements.txt entry). Two real wrinkles found and handled while
  building it: (1) this codebase's dominant style for function-local
  cross-module imports is `from module import name as _name` -- a naive
  `\bname\s*\(` search alone missed the large majority of real call sites
  until alias-resolution was added; (2) web_app.py's analytics endpoint
  dispatches some functions dynamically via `getattr(tracker, fn_name)()`
  over a tuple of string literals, which a pure call-pattern search can't
  prove reachable or dead -- added a third "possible dynamic dispatch"
  bucket (not asserted on, just excluded from the confident dead-code
  lists) after this exact pattern produced one false positive
  (get_model_calibration_buckets) during construction. The scan correctly
  reproduced all 4 originally-known candidates (categorized right: 3 fully
  dead, 1 tested-but-unreachable) plus found is_weather_market as a 5th
  fully-dead instance (see the ORPHANED FUNCTIONS entry above) -- all 5 are
  now fixed, not just documented. The scan also surfaced ~19 MORE
  tested-but-unreachable functions beyond the original 4 (e.g.
  paper.portfolio_kelly -- which paper.py's own nearby comment already
  admits is "dead in production, the live path uses portfolio_kelly_fraction
  instead" -- weather_markets.apply_station_bias, .bayesian_kelly_fraction,
  tracker.analyze_all_markets, and 16 others) -- these are allowlisted with
  a "needs follow-up triage" reason in _DEAD_CODE_ALLOWLIST, deliberately
  NOT fixed today (investigating and deciding wire-up-vs-delete for 19
  functions individually is out of scope for this pass -- exactly the same
  judgment call the original config-divergence guard made for its own
  first-pass findings). Two pytest tests wired in
  (test_no_new_dead_code_outside_allowlist, mirroring the config guard's
  pattern) so a NEW orphaned function can't land silently going forward,
  even though the current backlog isn't fully cleared. Independently
  reviewed (Agent) alongside the ORPHANED FUNCTIONS fixes above.

  apply_station_bias is worth flagging specifically: it's a fully-tested
  station-bias temperature correction that appears to have zero production
  callers anywhere in the forecast pipeline -- unlike the others in the
  allowlist, this one is plausibly a real gap (a forecast-accuracy
  correction that was built and tested but never actually wired in), not
  obviously-safe cleanup. Worth prioritizing first whenever the follow-up
  triage pass happens.

RESOLVED 2026-07-12 (part a only): built tests/test_config_divergence_guard.py
  -- exactly the two guards described below, plus a third (env-var
  hardcoded-default-literal divergence scan, which immediately found a real,
  live, previously-unknown instance: config.py's max_days_out defaulted to
  "3" while utils.py's MAX_DAYS_OUT defaulted to "5", masked only because
  .env sets it explicitly -- fixed via config._live_max_days_out(), commit
  5245269). The BotConfig field call-site guard surfaced ~19 fields with no
  real call site outside config.py -- categorized honestly into an
  allowlist (13 enforced elsewhere via a matching value, 3 misleading/
  shadowed by an unrelated hardcoded constant elsewhere -- see "MORE INERT
  config.py FIELDS" below, which this pass also corrected -- and 2
  genuinely unimplemented) rather than fixed/deleted; the guard's job is to
  stop a NEW instance from landing silently, not retroactively resolve the
  existing ones. Pushed 5245269 + 48016fa (a same-day correction after
  cross-checking prior backlog.txt research this session had initially
  missed).
  NOT done: part (b), the vulture-style dead-code CI scan for orphaned
  functions (paper.py/tracker.py/weather_markets.py) -- the [ORPHANED
  FUNCTIONS] entry below (slippage_kelly_scale, undo_last_trade,
  get_calibration_by_season, _fetch_ensemble_members_historical) is still
  fully manual-detection-only. Genuinely separate, larger scope from the
  config-field guard -- still open.

Problem:
  Between the "MORE INERT config.py FIELDS" entry above (4 confirmed
  config.py/utils.py divergence bugs: BREAKEVEN_TRIGGER_PCT,
  MAX_SAME_DAY_SPEND, PAPER_MIN_EDGE, and the current round of 5 more) and
  the duplicate-dict pattern documented across mos.py/settlement_monitor.py/
  weather_markets.py (4 instances, some already consolidated), this project
  has hit the same "one shared resource, many consumers, one gets missed"
  bug class seven-plus times. Every single instance was caught by a manual
  grep sweep during an unrelated session, never by CI or a standing test.

What the fix looks like:
  A small standalone test module that (a) walks every BotConfig field
  (config.py) and asserts it has at least one real call site outside
  config.py/utils.py/tests/ -- failing loudly (naming the field) the moment
  a new field is added without being wired in, mirroring the manual grep
  that has caught every instance so far; and (b) runs a dead-code scan
  (e.g. vulture) scoped to the core trading files (paper.py, tracker.py,
  weather_markets.py) as a CI/pre-commit check rather than a periodic
  manual sweep, to catch the next orphaned-function instance automatically.

Why not now:
  - Tooling investment, not a bug fix -- no live behavior changes.
  - Needs care to avoid false positives (some BotConfig fields are
    legitimately read only by config.py's own validation, e.g. the "should
    this be wired up or deleted" undecided fields from the round-4 entry
    above) -- the test needs an explicit allowlist for those, not a blind
    call-site count.

When to revisit:
  - Cheap relative to how many sessions this exact pattern has already
    eaten -- a reasonable candidate for the next slow/low-stakes session,
    not something to wait indefinitely on.

[RESOLVED 2026-07-12] TEST COVERAGE GAPS -- alerts.py, PAPER.PY METRICS,
  calibration.py
Priority: was Low-Medium (matches an already-recognized "shipped with zero
  correctness tests" pattern in this project)

RESOLVED 2026-07-12: 37 new correctness tests added (not smoke tests --
  construct known inputs, assert the actual computed value) across all 10
  functions below. No bugs found -- every function behaved correctly against
  real assertions.
  calibration.load_condition_weights: 3 tests added to the existing
  TestLoadWeights class in tests/test_calibration.py, mirroring its two
  siblings' exact missing/valid/corrupt-file pattern.
  alerts.py: new tests/test_alerts.py, 22 tests across 5 classes covering
  add_alert (field construction, ticker uppercasing, persistence/ID
  increment, invalid direction/target_price), remove_alert, get_alerts'
  cooldown re-arm math (untriggered/within-cooldown/after-cooldown/
  cooldown=0 cases), mark_triggered, and check_alerts' above/below direction
  logic including the exact boundary at target price.
  paper.py: 15 tests appended to tests/test_paper.py --
  validate_paper_trades_integrity (clean state vs. a deliberately corrupted
  balance), get_rolling_sharpe (known 5-day P&L fixture, exact value
  verified independently, plus <5-day and zero-variance guard cases),
  get_factor_exposure (YES-heavy/NO-heavy/both exact 0.6-0.4 boundary
  cases), get_unrealized_pnl_paper (yes-side/no-side/mixed sign logic).
  517 tests pass across the full combined touched scope (this entry plus
  the MINOR CLEANUP entry below, fixed same session); lint/mypy clean.
  Pushed together with the MINOR CLEANUP fixes as commit c97a71b.

Problem:
  Three areas of real, live-used logic have zero tests asserting correct
  behavior (not just "doesn't crash") -- the same pattern already found
  once in this project (the rolling directional-accuracy guard shipped
  with zero tests on its actual computed value):
    - alerts.py's entire price-alert subsystem (add_alert:51,
      remove_alert:90, get_alerts:101, check_alerts:137, mark_triggered:183)
      -- zero test references despite being wired into cron
      (main.py:2697-2709) and the interactive dashboard menu
      (main.py:4342-4418). Untested logic includes get_alerts()'s
      cooldown-based re-arm math (alerts.py:101-134) and check_alerts()'s
      above/below direction comparison (alerts.py:137-180).
    - paper.py's get_rolling_sharpe (paper.py:2770-2806, annualized Sharpe
      via daily P&L grouping), get_factor_exposure (paper.py:2844-2890,
      YES/NO-heavy classification via a 0.6/0.4 threshold -- untested
      boundary), get_unrealized_pnl_paper (paper.py:2920-2973, mark-to-
      market formula with different sign logic for yes vs no side), and
      validate_paper_trades_integrity (paper.py:1051-1083, balance-drift
      corruption detector used by web_app.py:1841's health-check endpoint)
      -- all zero tests on actual computed output.
    - calibration.py's load_condition_weights (calibration.py:364-379) is
      the untested outlier among 3 structurally-identical loaders --
      load_seasonal_weights and load_city_weights both get dedicated
      missing/valid/corrupt-file test classes (tests/test_calibration.py:
      166-213), load_condition_weights doesn't, despite being called at
      MODULE IMPORT TIME (weather_markets.py:29,695) to populate the global
      _CONDITION_WEIGHTS dict that feeds every trade's probability blend --
      a corrupt/missing file bug here would silently zero out condition
      weighting with nothing to catch it.

What the fix looks like:
  Write correctness tests (construct known inputs, assert the actual
  computed value) for each function above -- not just smoke tests. For
  load_condition_weights specifically, mirror the exact
  missing/valid/corrupt test pattern already used for its two siblings.

Why not now:
  - No known bugs in the underlying logic (all read as correct on
    inspection) -- this is a coverage gap, not a fix for broken behavior.
  - Meaningful scope (3 files, ~9 functions) -- worth a dedicated
    test-writing pass rather than squeezing in alongside unrelated work.

When to revisit:
  - Before the next time any of these functions' logic is touched/
    refactored (tests should exist before the change, not after); or as a
    standalone test-coverage session.

[RESOLVED 2026-07-12] MINOR CLEANUP -- DEAD ASSIGNMENT, STALE COMMENTS,
  UNBUILT PLANNED FEATURES
Priority: was Low (cosmetic / documentation only)

RESOLVED 2026-07-12: 5 of 6 sub-items fixed, 1 deliberately left with an
  explanatory comment instead of a forced fix.
  Dead _confidence_boost assignment (weather_markets.py) -- deleted.
  metar.py's "(Phase 4 stub)" docstring -- rewritten to describe actual
  current functionality (plausibility/staleness gates, dynamic lock-in
  confidence, station observation/bias recording); dropped the stub framing.
  code_review_plan.md's Phase 5 -- annotated with a "NOT BUILT -- see
  backlog.txt" note rather than left reading as planned/in-progress; the
  feature specs themselves were left in place, not deleted.
  _CITY_TZ (weather_markets.py) -- now derived from CITY_COORDS via a
  one-line dict comprehension instead of hand-duplicated; verified all 5
  call sites (weather_markets.py x4, settlement_monitor.py x1) still get the
  same dict shape/keys. Can no longer drift even once dynamic city loading
  is eventually added.
  cron.py's duplicate 3-model list -- deliberately left as-is: no clean
  existing constant in weather_markets.py exactly matches
  (ENSEMBLE_MODELS is missing ecmwf_ifs025, ENSEMBLE_MODELS_EXTENDED swaps
  in different models) -- forcing an import would have been an awkward fit
  for a cosmetic, log-line-only duplication. Added a comment explaining why
  and pointing at both near-miss constants instead.
  backtest.py/tracker.py's duplicate Previous-Runs model-name structures --
  added a regression test (TestPrevRunModelsMatchTracker in
  tests/test_backtest.py) asserting backtest's flat list is a subset of
  tracker's map values, after verifying that's the actual real relationship
  between the two shapes. A future silent divergence now fails a test
  instead of going unnoticed; the two structures themselves were not
  unified (that would have been a bigger refactor than this pass's scope).
  517 tests pass across the full combined touched scope (this entry plus
  the TEST COVERAGE GAPS entry above, fixed same session); lint/mypy clean.
  Pushed together as commit c97a71b.

Problem:
  Several small, low-impact items surfaced during the same sweep, not worth
  individual entries but worth tracking together:
    - weather_markets.py:6144 assigns _confidence_boost = 1.0 inside the
      METAR-lock branch, but it's unconditionally overwritten at line 6193
      before ever being read (first real read is line 6533) -- dead
      assignment, no functional effect, just confusing to read.
      (Line numbers re-verified 2026-07-10; previously 6032/6081/6421.)
    - code_review_plan.md's "Phase 5" features (cross-market arbitrage
      auto-execution, an intraday METAR 2pm sweep, dynamic edge threshold
      by market liquidity) were planned but none were ever built -- worth
      either building or explicitly marking the plan doc as not-done so it
      doesn't read as already-shipped.
    - weather_markets.py's _CITY_TZ (weather_markets.py:337-358) duplicates
      the timezone already embedded in each CITY_COORDS tuple. All 20
      values currently agree, and data/cities.json (the dynamic-loading
      mechanism CITY_COORDS supports) doesn't exist yet -- so this hasn't
      drifted yet, but the moment a city is added via that mechanism,
      CITY_COORDS picks it up automatically while _CITY_TZ (used in 5
      places including settlement_monitor.py:65) would not.
    - RESOLVED 2026-07-10: the stale "18 traded cities" comments are gone
      -- paper.py's was removed outright (the rewritten group comment at
      paper.py:278-282 correctly describes LV/NOLA), and
      settlement_monitor.py:62's was reworded to acknowledge the LV/NOLA
      additions. Nothing left to do on this item.
    - metar.py's module docstring (metar.py:2) still says "(Phase 4 stub)"
      despite the module having grown real staleness/plausibility checks,
      dynamic lock-in confidence, and ensemble sanity checks since.
      (Re-verified still stale 2026-07-10.)
    - cron.py:1146,1169 (was 1070,1093) hardcodes a second copy of the
      3-model list that weather_markets.py already defines -- confirmed
      cosmetic only (only affects a log line's model count, never passed
      into the actual fetch functions).
    - backtest.py:32 (_PREV_RUN_MODELS) and tracker.py:2327
      (_PREVIOUS_RUN_MODEL_MAP) each independently hardcode the "Previous
      Runs API" model names -- no test protects them, no shared source.
      (Note 2026-07-10: tracker's copy is now a dict keyed by ensemble
      model, backtest's is a flat list -- shapes differ, drift between
      them would be even harder to spot now.)

What the fix looks like:
  Batch these into a single small cleanup pass: delete the dead assignment,
  update the 2 stale city-count comments and the metar.py docstring, and
  either derive _CITY_TZ from CITY_COORDS or delete it in favor of always
  reading the coords tuple. Decide the code_review_plan.md Phase 5 items are
  either being built or being marked as abandoned/not-happening.

Why not now:
  - None of these have any live functional impact today -- purely
    opportunistic cleanup, bundle into whatever session next touches these
    files anyway.

When to revisit:
  - Whenever weather_markets.py/paper.py/metar.py is next touched for other
    reasons -- fold in as a drive-by cleanup rather than a dedicated
    session.

[MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM]
Priority: Medium (not a code bug, but the user's stated auto-run reliability
  problem is real and this is the actual fix for it)

Problem:
  This bot's scheduled runs (cron.py, invoked via Windows Task Scheduler on
  a laptop) have caused reliability issues per the user directly -- a
  travel laptop closing its lid, losing wifi, or just not being on defeats
  the whole point of unattended scheduled scanning. cron.py itself is a
  genuine one-shot process (confirmed while building the [RESTING EXIT
  ORDERS] entry above -- no `while True` loop, no persistent state between
  invocations) that depends entirely on an external scheduler staying
  alive, which a travel laptop structurally can't guarantee.

  Original plan (per the user, "a long time ago") was AWS. Re-evaluated
  from scratch 2026-07-13 rather than assuming that's still the best fit.

What the fix looks like:
  Move scheduling off the laptop onto a small always-on VM. This bot's
  entire persistence model (SQLite DBs in tracker.py/execution_log.py,
  JSON stores for paper trades/WS orderbook cache/sigma cache/config hash,
  the Kalshi private key file) assumes durable local disk between runs --
  that rules out pure serverless (Lambda-style, ephemeral filesystem) and
  GitHub Actions (ephemeral runners; would need a git-commit-back or
  Artifacts hack to fake persistence) without a real storage migration
  first. A plain VPS keeps the current architecture untouched: just swap
  Windows Task Scheduler for real Unix cron or a systemd timer running the
  same `python cron.py` invocation.

  11 hosting options researched and ranked 2026-07-13 (full comparison
  with current pricing/reliability sources in that session's transcript,
  not reproduced here in full):
    - RECOMMENDED: Hetzner Cloud or DigitalOcean, ~$5-12/mo. Cheapest real
      VPS tier with no vendor-reliability red flags. DO has free built-in
      monitoring/alerting as a bonus; Hetzner is cheaper at equivalent
      specs. Linode/Vultr are close seconds, no compelling reason to pick
      them over Hetzner/DO specifically.
    - AWS EC2 (t4g.micro) and Azure (B1s) both priced out higher than
      Hetzner/DO with no functional benefit for a workload this small --
      note for AWS specifically: t4g.micro is NOT covered by the standard
      12-month free tier (that's t3.micro/t2.micro, or a t4g.small promo
      through end of 2026 only) -- budget for cost from day one if AWS is
      ever picked instead.
    - AVOID despite being $0/mo, specifically because the user's problem is
      unpredictable auto-run failures: Oracle Cloud "Always Free" (2026
      reports of account-approval opacity, a June 2026 capacity halving --
      4 OCPU/24GB -> 2 OCPU/12GB -- with zero notice, and "idle
      reclamation" that can throttle/kill a low-traffic instance) and
      GitHub Actions scheduled workflows (silently disables a workflow
      after 60 days of repo inactivity, on top of the persistent-disk
      mismatch above -- the same "silently stops working" failure category
      the user is trying to escape, not a fix for it).
    - Google Cloud e2-micro Always Free is a genuine $0/mo dark horse (no
      Oracle-style complaints found), but has no SLA on free-tier
      resources and only 1GB RAM -- tight if this box is also meant to run
      continuously (see sizing note below).
    - Render/Railway/Fly.io (PaaS with built-in cron+volumes) work
      architecturally but cost more than a plain VPS for no benefit here;
      Fly.io specifically has a reputation for surprise bills from
      compounding metrics (volume storage, snapshots, inter-region
      traffic all billed separately).
    - Home Raspberry Pi + Tailscale is a legitimate $0/mo-recurring option
      that sidesteps cloud-vendor risk entirely (stays online at home
      while the user travels, which is the actual scenario being solved)
      but shifts the single point of failure to home internet/power, with
      the user as the only support line if hardware fails.

  User's answers when asked (2026-07-13, via AskUserQuestion): pay ~$5/mo
  and prioritize reliability over $0 (rules out Oracle/GCP free tier);
  size the box for eventually running `watch --auto --live` continuously
  plus the deferred always-on-watcher piece from the [RESTING EXIT ORDERS]
  entry above, not just periodic cron.py -- pushes toward a 2GB-RAM tier
  (Hetzner CX22-equivalent, or DigitalOcean's ~$12/mo droplet) rather than
  the cheapest 1GB option; starting fresh, no existing cloud accounts to
  factor into the decision.

  Setup shape once started (not yet done): Ubuntu, SSH key-only access, a
  free stateful firewall (Hetzner) or DO's built-in cloud firewall
  (inbound SSH only from the user's IP, everything else outbound-only,
  which this bot needs freely for Kalshi/Open-Meteo/NWS/notification
  webhooks), clone via a read-only deploy key (not a personal SSH key),
  scp over .env and the Kalshi private key file (never committed), real
  cron/systemd timer for the schedule, drop "desktop" from NOTIFY_CHANNELS
  (won't fire headless -- Pushover/Discord/email already work and need no
  code changes, just env vars), periodic snapshot plus an independent
  rsync/scp of the SQLite DBs back to the user's own storage (don't rely
  solely on the provider's snapshot).

Why not now:
  - Explicitly parked by the user ("we are not doing anything now") --
    the comparison and decision are done, nothing has been executed. No VM
    created, no code changed.

When to revisit:
  - Whenever the user is ready to actually execute. Use the decision
    above (Hetzner or DigitalOcean, ~$12/mo tier, sized for continuous
    operation) rather than re-running the hosting comparison from scratch
    unless something material has changed (a provider's pricing/free-tier
    terms, or the user's requirements).
  - UPDATE 2026-07-16 (user clarification, no code changed): the trigger
    is live trading actually going live (or becoming concretely imminent),
    not just the ~2026-07-30 Belgium-trip-end date passing. Don't resurface
    this as ready-to-execute purely because the trip has ended -- confirm
    live trading is genuinely about to start first.

[RESOLVED 2026-07-13] RESTING EXIT ORDERS + OCO ORDER GROUPS
Priority: was High (live risk-management hole, not just an optimization)

RESOLVED 2026-07-13: resolved as "event-driven protective exits," a
  materially different design than the "OCO with a resting take-profit +
  resting stop-loss" plan below -- two research findings during design
  invalidated that plan's core mechanics, surfaced to and confirmed with the
  user before writing any code:
    1. Kalshi has no stop/conditional order type at all (confirmed directly
       against Kalshi's own V2 create-order API docs -- time_in_force is
       only fill_or_kill/good_till_canceled/immediate_or_cancel). A resting
       LIMIT sell fills when price RISES to meet it -- the opposite of what
       protects a falling position. A "resting stop-loss order" cannot work
       mechanically; a real stop-loss must cross the spread immediately the
       instant it triggers, not rest passively.
    2. Take-profit isn't a real concept in this bot today. paper.py's
       exit_target field (the take-profit price this plan assumed existed)
       was found to be fully dead -- threaded through place_paper_order's
       signature and checked by check_exit_targets(), but grepping every
       real call site in the repo, none of them ever actually sets it. This
       bot's actual exit strategy is 100% signal-triggered (price
       stop-loss, breakeven-stop, model-flip/edge-gone), not a fixed price
       target -- there was nothing to "rest a GTC sell at."
  User chose (via AskUserQuestion) to build event-driven protective exits
  only, explicitly declining to also invent a take-profit mechanism from
  scratch: doing so would risk cutting winning trades short before their
  statistical edge fully resolves (this bot trades multi-day probabilistic
  edge, not price momentum), with no settled-trade volume yet to calibrate
  a target against.

  What shipped: live positions previously had ZERO automated exit
  management of any kind (not "poll-based," genuinely none -- verified by
  exhaustive grep: execution_log.py's orders table only ever transitioned a
  filled live order through to natural Kalshi settlement, no live-position
  tracker existed, and paper.py's check_stop_losses/check_breakeven_stops/
  check_model_exits only ever operated on paper.get_open_trades()).
  execution_log.py gained 4 columns (peak_profit_pct, exit_reason,
  exit_price, entry_prob) + record_live_early_exit()/
  update_live_peak_profit(); order_executor.py gained
  _get_live_open_positions() (adapts execution_log rows into
  check-function-compatible dicts), _update_live_peak_profits() (peak math
  replicated from paper.update_peak_profits rather than calling it -- that
  function ignores its own parameter and re-reads paper's own JSON store
  internally, confirmed by an independent review reading its body),
  _check_live_position_exits() (reuses paper.check_stop_losses()/
  check_breakeven_stops() completely unmodified -- both verified
  storage-agnostic, operating purely on their passed-in list), and
  _check_live_model_exits() (mirrors the existing paper-only
  _check_early_exits()'s exact gates -- 12h minimum hold, 24h
  pre-settlement skip, 25pp forecast-shift threshold -- against live
  positions instead). Every triggered exit fires via _exit_live_position():
  an immediate_or_cancel sell at the guaranteed-cross price
  (paper._liquidation_price, traced against kalshi_client's V2 side/price
  mapping to confirm it actually crosses for both yes and no), re-running
  only the kill-switch/trading-paused gate (not daily-loss/spend/
  max-open-position -- closing a position reduces exposure, matching the
  same reasoning already applied to _replace_live_order and the manual sell
  path in main.py's cmd_order). Wired into both main.py's cmd_watch --live
  loop and cron.py's _cmd_cron_body (right after the existing
  _recover_pending_orders crash-recovery call) -- cron.py itself still
  never places a live order (unrelated, pre-existing: only watch --auto
  --live does), so this wiring exists to protect a position opened during
  an earlier watch session that's still open when a later cron run fires,
  not because cron can create new live exposure.

  Deliberately deferred, not part of this resolution:
    - A truly always-on watcher that reacts between cron cycles (closing
      the multi-hour manual-cron-cadence gap this entry was originally
      about) -- these checks still only run once per cmd_watch/cron cycle.
      Discussed explicitly with the user: this is more an ops/deployment
      question (does a travel laptop even stay on? systemd service on a
      persistent host?) than application code, and there's no live position
      to protect yet anyway (ENABLE_MICRO_LIVE=False). Tie this to whatever
      comes of the separate "stop relying on local cron entirely" discussion
      -- a small always-on VM would double as the natural host for this
      watcher too.
    - WS `fill`-channel wiring for faster fill detection -- deferred
      alongside the watcher above, since the existing REST-based
      _poll_pending_orders poll already covers what a periodic-check design
      needs; the fill channel's real payoff only shows up once something
      is actually always-on. Confirmed via a live docs fetch that the
      channel is real, account-wide (no market_tickers filter required),
      with fields order_id/market_ticker/side/action/count_fp/
      yes_price_dollars/is_taker/ts_ms.
    - order_groups as a fill-rate safety cap -- still just the 15s
      rolling-window circuit breaker it was already corrected to be in the
      2026-07-12 update below, not used anywhere in this design.
    - Take-profit (see above -- declined, not just postponed, unless a
      future session has real settled-trade data to design one from).

  Two independent opus reviews given this is the only backlog item this
  session that adds NEW live order-placement code (not modifying existing
  paths). Round 1 (adversarial) found one real HIGH bug: the exit-price
  lookup read _get_current_book()'s REST-fallback shape (raw integer cents,
  or *_dollars-only keys) as if it were already a normalized dollar float --
  meaning stop-loss/breakeven protection would silently never fire on
  exactly the cron path this feature was built for (a fresh process always
  starts with an empty WS cache, so every cron run hits the REST fallback).
  Fixed by routing through the same _coalesce_cents_or_dollars helper the
  sibling model-exit check and pre-existing reprice code already used
  correctly. Also caught and fixed a real fee-formula inconsistency:
  the exit PnL calc applied the taker fee to gross proceeds unconditionally
  including on a loss, diverging from the established convention (fee only
  discounts a genuine WIN, verified across weather_markets.py/paper.py and
  the pre-existing natural-settlement code) -- fixed to the win/loss
  asymmetric form, independently re-derived as an exact generalization of
  the existing settlement formula (substituting exit_price=1.0 or 0.0
  reproduces the original win/loss branches exactly). Round 2 (verification)
  confirmed both fixes correct and complete, added a regression test using
  the genuine raw-cents shape (not another disguised dollar-float mock),
  and found one new narrow LOW issue -- multiple live positions sharing one
  ticker would collapse to one entry in a ticker-keyed dict, silently
  leaving the other unprotected for a cycle -- inherited from paper.py's
  own ticker-keyed check-function design (cron.py's paper stop-loss/
  breakeven handling has the identical `next(t for t in trades if
  t["ticker"]==ticker)` one-match pattern already live in production, so
  this was fixed narrowly in order_executor.py only rather than touching
  the shared, already-relied-upon paper.py functions): positions are now
  grouped by ticker (list per ticker, not one dict slot), and every
  position under a flagged ticker is exited, not just one -- erring toward
  over-inclusive (a healthy same-ticker position exits a cycle earlier than
  its own individual threshold required) rather than the failure mode this
  whole entry exists to prevent (a position silently left unprotected).
  118+ tests pass across the affected scope; ruff/mypy clean.

  Also fixed the same day, found while running the broader regression
  sweep (confirmed via git stash it failed identically on the prior commit
  with none of this session's changes applied, so genuinely unrelated to
  this backlog item): tests/test_paper.py's
  TestMonteCarloCholesky::test_past_date_trade_excluded_from_simulation was
  failing -- root cause was the TEST, not monte_carlo.py itself. It built
  its "past"/"future" fixture dates from local machine date.today(), but
  simulate_portfolio()'s actual skip logic compares against
  utils.utc_today(). On a machine whose local date is already a day ahead
  of UTC, "yesterday, local" lands on the same calendar day as "today,
  UTC" -- the skip condition (`_tdate < utc_today()`) never fires, and the
  trade wrongly falls through into the clamping path the test was trying
  to prove it never reaches. Fixed both this test and its sibling
  (test_past_date_only_portfolio_returns_empty_result, same pattern, not
  yet failing only because its 2-day margin happened to still clear the
  gap) to build fixture dates from utils.utc_today() instead, matching the
  System Under Test's actual reference frame.

Problem (original framing, superseded by the resolution above):
  All exits are poll-based: check_exit_targets()/check_model_exits()/
  stop-losses only run when a cron or watch cycle happens to execute. On a
  manual-cron cadence (e.g. ~3 runs/day during the Belgium trip) an open
  position can sit through its stop level for hours with zero protection.
  Kalshi's API supports order_groups (OCO — linked orders where one fill
  auto-cancels the others) and a WebSocket `fill` channel; the bot uses
  neither (kalshi_ws.py:301 subscribes only to ticker + orderbook_delta,
  fills are discovered by polling _poll_pending_orders()).

UPDATE 2026-07-12 (research pass, no code changed) -- IMPORTANT CORRECTION
to the order_groups claim above:
  Re-confirmed kalshi_ws.py still subscribes only to ticker + orderbook_delta
  (kalshi_ws.py:329) and kalshi_client.py has no order_group/fill support at
  all -- that part of the problem statement still holds.
  But fetched docs.kalshi.com/api-reference/order-groups/create-order-group
  directly and Kalshi's order_groups is NOT an OCO mechanism -- the original
  "linked orders where one fill auto-cancels the others" claim is wrong.
  It's actually a risk-management circuit breaker: contracts_limit sets a
  max matched-contracts count over a rolling 15-SECOND window; once hit,
  ALL orders in the group are cancelled and no new ones can be placed until
  reset. There is no field or documented behavior for "one order filling
  cancels the others" -- that's a self-built OCO concept this bot would
  still have to implement client-side (rest both legs, watch for either to
  fill via polling or the `fill` WS channel, cancel the other manually).
  order_groups' real use case is closer to protecting a market maker's
  resting quotes from a fast adverse-selection burst, not linking a
  take-profit and a stop-loss together -- still possibly useful for THIS
  bot's resting exits (cap how much can fill against a stale resting order
  in a 15s window) but as an extra safety layer, not as the OCO primitive
  the fix plan below assumed it would be.
  The WebSocket `fill` channel IS real and confirmed subscribable via
  `channels: ["fill"]` (docs.kalshi.com/websockets/websocket-connection) --
  that half of the plan holds.
  The market-maker rebate claim also needs correcting: Kalshi runs several
  DIFFERENT incentive programs, not one. Fetched
  help.kalshi.com/en/articles/13823851-liquidity-incentive-program directly:
  it pays a $10-$1,000/day share of a reward pool (Score / Total Participants'
  Scores * Reward Pool) for maintaining resting-order "Target Size" (100-
  20,000 contracts), NOT a flat percentage-of-fees rebate -- doesn't match
  the original "1%, capped $7,000/week" figure at all. Kalshi also has a
  separate Volume Incentive Program, Liquidity Provider Program, Combo
  Incentive Program, and a formal fee-rebate Market Maker Program referenced
  in CFTC filings (which may be where the original 1%/$7,000 figure came
  from, unconfirmed) -- these are genuinely different programs with
  different mechanics and eligibility, not one program with one number.
  Any real revenue-angle evaluation needs picking a specific program and
  reading its actual terms once this bot is resting real exit orders, not
  citing a single rebate percentage upfront.

What the fix looks like:
  When a live position opens, immediately rest a GTC sell at the take-profit
  price; implement OCO linking CLIENT-SIDE (order_groups does not do this
  for you -- see correction above): watch for either the take-profit or the
  stop leg to fill (via the `fill` WS channel or polling) and cancel the
  other manually. Subscribe to the WS fill channel to replace/(augment)
  _poll_pending_orders polling. Exits then execute continuously regardless
  of scan cadence, and resting exits get maker pricing instead of taker.
  order_groups' contracts_limit could still be layered in as an extra
  safety cap on a resting exit order's fill rate, separately from the OCO
  logic above.

  Note (revenue-angle, needs its own research pass once this ships -- see
  the multiple-distinct-programs correction above): don't assume a single
  rebate number; identify which Kalshi incentive program actually applies
  to this bot's real resting-order pattern and read its current terms
  directly before quoting a figure.
  Also see the endpoint-deprecation entry at the top of this file: Kalshi's
  order-mutation endpoints (used here for placing/canceling the resting
  exit orders) were migrated to /portfolio/events/orders this session (see
  kalshi_client.py's place_order/cancel_order) -- build any new order-group
  logic on top of that V2 path, not the old deprecated one.

Why not now: N/A -- resolved (as event-driven protective exits, see above).

When to revisit: N/A -- resolved. See the deliberately-deferred list above
  (always-on watcher, WS fill channel, take-profit) for what's still open.

[RESOLVED 2026-07-12] MAKER EXECUTION UPGRADES — REPRICING + MAKER-AWARE
  FEE MODEL
Priority: was High (systematically understated edge on every trade analyzed)

PARTIALLY RESOLVED 2026-07-12: problem 2/3 (maker-aware fee model) is done;
  problems 1/4 (repricing/chase logic, fill-quality instrumentation) are
  still open. Verified Kalshi's real fee formula directly against the
  primary source (kalshi.com/docs/kalshi-fee-schedule.pdf, effective July 7,
  2026, obtained and read in full) rather than trusting third-party
  summaries -- several independent secondary sources (pm.wiki, marketmath.io)
  claimed maker fee is universally 25% of taker (0.0175 x P x (1-P)), which
  is WRONG for this bot's markets specifically: the real schedule has a
  maker multiplier M that defaults to 0 for the general event-contract
  schedule, and is only nonzero (giving the 25%-of-taker rate) for ~50
  explicitly-listed non-standard series (KXNFL*, KXNBA*, KXCPI, KXFED, etc.)
  -- none of which are this bot's weather markets. So maker fee is genuinely
  and exactly $0 here, confirming utils.py's original "Maker orders pay 0%"
  comment was correct all along, and problem 3's formula-shape concern
  (0.07 x p x (1-p) vs flat-fraction-of-winnings) turned out to be moot in
  practice: 0 x anything = 0, so the shape mismatch doesn't matter while
  every fill is a maker fill.
  Added utils.KALSHI_MAKER_FEE_RATE (0.0, PDF-cited). Traced every
  KALSHI_FEE_RATE call site across the repo (not just kelly_fraction()/
  net_ev as originally scoped -- the same bug also lived in paper.py's
  settle_paper_trade()/get_attribution(), order_executor.py's live-
  settlement P&L feeding the daily-loss safety counter, and
  monte_carlo.py's portfolio VaR risk gate; scope was surfaced to and
  confirmed by the user before implementing) and switched every site that
  models this bot's own maker-executed trades to the maker rate, while
  deliberately leaving backtest.py's 3 naive-strategy comparison benchmarks
  and kelly_fraction()/bayesian_kelly()'s own generic default parameters at
  the taker rate. Independently reviewed (Agent, opus) -- traced the safety-
  gate-adjacent sites specifically (daily-loss counter, VaR gate) and
  confirmed the change doesn't remove a safety margin (loss-side P&L is
  unchanged; only previously-phantom fee on wins was removed); found one
  LOW gap (KALSHI_MAKER_FEE_RATE missing from get_config_fingerprint()'s
  drift-detection snapshot), fixed same session. 665 tests pass across the
  full affected scope (weather_markets.py, paper.py, order_executor.py,
  monte_carlo.py, backtest.py, web_app.py, main.py), 3 tests had hardcoded
  0.07-fee P&L expectations corrected to reflect the real $0 maker fee.

  UPDATE (same session, user-flagged): two display sites were still showing
  the stale taker-only fee after the first pass. Added BotConfig.
  kalshi_maker_fee_rate (mirrors kalshi_fee_rate's pattern, own validation)
  and wired it into /api/config (dashboard Settings tab) and the CLI
  settings menu, which previously only showed KALSHI_FEE_RATE with no
  visibility into the rate actually driving sizing. Separately, re-examined
  cmd_simulate()'s "Simulation Sandbox" (main.py) -- originally called out
  of scope as "a symmetric educational game" -- and found that framing was
  wrong: its "Model:" P&L explicitly claims to show what analyze_trade()'s
  own recommendation would have earned (it calls analyze_trade() directly),
  so recomputing that P&L with the taker rate while analyze_trade() itself
  now uses the maker rate was a real internal-consistency bug, not a
  self-contained toy. Fixed by splitting into _user_fee (taker -- a human
  manually taking the displayed price) and _model_fee (maker -- matches
  analyze_trade()'s real assumption). Added a regression test
  (tests/test_backtest.py) since none of the existing cmd_simulate tests
  exercised the interactive P&L path. All tests pass, lint/mypy clean.

  UPDATE (same session, requested full re-review): a second independent
  opus review of the accumulated diff (all 3 rounds) found the math correct
  everywhere but surfaced 3 more instances of the same underlying pattern --
  user-facing display TEXT still hardcoding "~7% fee"/"7% fees", not
  computation this time: main.py's cmd_market Edge line ("after ~7% fee"),
  cmd_backtest's Sim P&L caption ("7% fees"), and cmd_analyze's table
  legend ("after Kalshi's ~7% fee"). None affected sizing, P&L, or safety
  gates -- purely stale captions describing numbers that were already
  computed correctly. Fixed to generic "after fees"/"maker fees" wording
  (not hardcoding a specific percentage, since KALSHI_MAKER_FEE_RATE is
  itself env-overridable). No test pinned the stale strings. All tests
  pass, lint/mypy clean -- this closes the fee-model half of this backlog
  item.

RESOLVED 2026-07-12 (repricing half, separate later pass): problem 1
  (reprice-or-cancel logic) and problem 4 (fill-latency/adverse-selection
  instrumentation) are done, closing this backlog item in full. Scoped via
  two AskUserQuestion checkpoints before writing code -- depth source for
  reprice decisions (chose: WS ticker-tick cache, already streamed/proven
  via the flash-crash gate, over fixing the separately-broken
  orderbook_delta full-depth application, which top-of-book doesn't need
  for this bot's order sizes) and reprice cadence (chose: reuse the
  existing 5-min cmd_watch loop, not a new continuous watcher).

  Policy per pending live order, once per ~5-min cycle (order_executor.py's
  new _reprice_or_cancel_pending_orders, called from cmd_watch right after
  the existing fill-status poll): ticker not in this cycle's fresh scan ->
  leave alone; the existing _validate_trade_opportunity() gate no longer
  passes -> cancel outright, no age minimum; younger than 2 minutes ->
  leave alone; edge still clears the REAL taker fee (recomputed with
  KALSHI_FEE_RATE, not the maker rate analyze_trade() actually used) and
  rested >= 4 minutes -> cancel-verify-unfilled, replace as an
  immediate-or-cancel taker order at the current opposing price; fresh
  midpoint differs from the resting price by >= 1 cent -> cancel-verify-
  unfilled, replace as a new resting GTC order at the fresh midpoint (still
  $0 fee); otherwise leave resting. Every cancel-then-replace goes through
  a hardened _cancel_and_verify_safe_to_replace() (reuses the existing
  _finalize_cancel fill-check pattern) before ever placing a replacement --
  never risks a duplicate position. execution_log.py gained filled_at/
  market_mid_at_fill (captured once, at the moment a fill is first
  detected) and replaces_order_id (links a reprice chain) for the
  adverse-selection instrumentation ask.

  Independently reviewed TWICE given this is the highest-risk change of the
  whole backlog pass (Agent, opus both times) -- first pass found 2 MEDIUM
  + 1 LOW real issues, all fixed same session: (1) the WS-book usability
  check used `is not None`, but a one-sided book's missing side defaults to
  0.0 not None in kalshi_ws.py's own ticker parser, so a one-sided book was
  wrongly treated as usable -- fixed to require both sides > 0, and the
  test that had asserted the old behavior was corrected to mock the real
  0.0 sentinel instead of an unrealistic None; (2) a code comment claimed a
  same-cycle-placed order couldn't reprice-churn because placement and the
  reprice check read "the same scan data" -- traced and found that's false
  (placement re-fetches a fresh market via client.get_market() right before
  pricing; the reprice check reads the WS cache or older scan data --
  genuinely different snapshots) -- fixed with a real blanket minimum-age
  gate applied to both the reprice-improve and taker-cross branches, not
  just relying on the (wrong) assumption; (3) the cancel-safe-to-replace
  check only verified fill_count==0, not that the order was actually
  confirmed off the book -- _finalize_cancel collapses an unrecognized/
  in-flight status (e.g. "resting") to "canceled" for its two pre-existing
  callers, which don't need the distinction, but that meant a still-
  resting order could pass as "safe to replace" -- fixed by threading
  Kalshi's raw unmodified status through as a third return value and
  requiring that specifically. A second, narrower re-verification pass
  after these fixes confirmed all three were implemented correctly, traced
  every call site of the changed _finalize_cancel signature, and found two
  minor test-coverage gaps (fixed: a test whose docstring credited the
  wrong gate after fix #2, and a missing test for the age window between
  the two gates) -- no new bugs. One informational finding (taker-cross
  only fires for temperature markets today, since precip/snow analyze_trade
  paths don't store entry_price) documented, not fixed -- fails safe
  (reprice-improve still works for those markets). One pre-existing,
  narrow settlement-integrity gap unrelated to this feature's core
  correctness (an order that fills exactly as _finalize_cancel's post-
  cancel verification query itself fails could escape settlement
  reconciliation) was spawned as a separate follow-up task rather than
  fixed inline in an already-large diff.

Problem:
  The live path already places GTC limit orders at the midpoint
  (order_executor.py:626-643) — i.e. maker execution exists. But:
  1. No repricing/chase logic exists anywhere (verified: zero hits for
     reprice/amend/requote) — an unfilled order sits 24h (gtc_cancel_hours)
     then cancels. Midpoint resting orders suffer adverse selection: they
     fill preferentially when the market moves against the model.
  2. kelly_fraction() (weather_markets.py:4361-4377) charges the 7% taker
     fee on every trade's EV/sizing, while utils.py:59's own comment notes
     "Maker (limit) orders pay 0%." The bot understates its net edge and
     skips trades it could profitably take at maker pricing.
  3. Fee formula shape is also off (conservative direction): it models "7%
     of winnings, on wins"; Kalshi's real taker fee is 0.07 x p x (1-p)
     charged upfront win-or-lose. Compounds with (2).
  4. No fill-rate / time-to-fill / adverse-selection tracking exists, so
     none of the above can be tuned empirically yet.

What the fix looks like:
  [DONE, see resolution notes above] pass an execution-style-aware fee_rate
    into kelly_fraction()/net_ev instead of flat KALSHI_FEE_RATE.
  [DONE, see resolution notes above] Reprice-or-cancel logic driven by the
    existing kalshi_ws.py book feed (improve toward the ask as edge allows;
    cross as taker only when edge clears the taker fee); log fill latency
    and post-fill price drift per order to measure adverse selection.

Why not now: N/A -- fully resolved.

When to revisit: N/A -- fully resolved. Adverse-selection TUNING (as
  opposed to the instrumentation that now exists to support it) is a
  natural follow-up once enough live fill data accumulates.

[RESOLVED 2026-07-12] HISTORICAL MARKET-PRICE CAPTURE VIA CANDLESTICKS ENDPOINT
Priority: was Medium-High (unlocks execution research the two entries above need)

RESOLVED 2026-07-12: shipped the capture side (analysis passes deliberately
  deferred, per "When to revisit" below). Added kalshi_client.get_candlesticks()
  (GET /series/{series_ticker}/markets/{ticker}/candlesticks, verified against
  live Kalshi docs -- fixed-point "_dollars"/"_fp" response shape, not the
  legacy cents shape) and a new tracker.py price_history table (migration
  v37->v38) with log_price_candles()/get_price_history() helpers. Wired into
  sync_outcomes() as a backfill-on-settlement, not per-cron-cycle polling --
  it fires exactly once per market, right when that market's outcome is first
  recorded (pending query already excludes tickers with an existing outcome
  row), fetching the market's full OHLC history in one call via its
  open_time/close_time range. Backfill failure is isolated in its own
  try/except so it can never block outcome recording. Independently reviewed
  (Agent, opus): found and fixed one real MEDIUM issue before commit -- initial
  1-minute candle resolution over a multi-day open window risked silently
  under-capturing exactly the long-open weather markets this feature targets
  (Kalshi's undocumented-but-plausible per-request period cap), logged only at
  debug so the failure would have been invisible. Fixed by switching to hourly
  resolution (still plenty for the deferred edge-decay/adverse-selection
  analysis) and bumping the failure log to warning. 25 new tests, lint/mypy
  clean. Pushed ba3527e. TRADING_PAUSED untouched; no order-placement/gate
  code was touched.

Problem:
  Zero references to Kalshi's /markets/{ticker}/candlesticks endpoint (or
  any price-history capture) exist in the codebase. The bot cannot answer
  "does my edge decay as the market converges to the forecast — when is the
  optimal entry?", cannot benchmark against real price paths (the current
  "Follow market" backtest benchmark uses scan-time prices only), and
  cannot measure maker-fill adverse selection.

What the fix looks like:
  Log OHLC candles for tracked markets (fetch per cron cycle, or backfill
  on settlement); store in a new price_history table keyed by
  ticker/period. Then: entry-timing analysis (edge vs hours-to-close),
  real-price benchmark replay, and post-fill drift measurement.

Why not now:
  - Pure new instrumentation + storage; payoff is analytical, arrives only
    after weeks of accumulated candles.

When to revisit:
  - Ship the capture early (it's cheap and data accumulates only from when
    it ships); do the analysis passes once maker-execution work is picked
    up.
  - UPDATE 2026-07-16 (no code changed): concretizing this per the
    ENABLEMENT TRIGGER pattern introduced in the FORECAST RUN-TO-RUN TREND
    SIGNAL entry above -- check `SELECT COUNT(*) FROM price_history` (and
    ideally distinct ticker count) periodically; the analysis passes
    (entry-timing, real-price replay, drift measurement) are worth doing
    once there's a real multi-week window of candles, not tied strictly to
    maker-execution work being picked up if that stays deferred longer than
    the data does.

======================================================
FEATURE CANDIDATES — 2026-07-10 feature-scouting session.
Every "currently absent" claim below was verified against the code that day
(grep + reading the call sites), not assumed. Ordered roughly by expected
payoff within each group.
======================================================

[PARTIALLY RESOLVED 2026-07-16] FORECAST RUN-TO-RUN TREND SIGNAL
Priority: was Medium (new alpha source, mostly existing plumbing) -- now Low
  until the enablement trigger below fires; this is a data-collection phase,
  not an active development item.

PARTIALLY RESOLVED 2026-07-16: the LOGGING half is shipped; the BLEND half
  (directional lean and/or sigma-widening) is deliberately not built yet --
  scoped via AskUserQuestion before coding (data-source symmetry, series
  length, blend usage) and the user chose log-only for this pass, matching
  every other new-signal entry in this file's own precedent (candlestick
  capture, trade-flow signal, this entry's own original wording).
  Live-verified against the real Previous Runs API (2026-07-16) before
  writing code: a future target_date returns real non-null data when queried
  with forecast_days instead of past_days (the pre-existing
  _fetch_previous_run_daily, built only for backfilling PAST dates, returns
  None for a future one); the existing lead clamp of 1-7 is correct (lead=8
  confirmed all-null live); Open-Meteo accepts multiple previous_dayN
  hourly variables in one request, so a 4-point lookback costs the same 3
  HTTP calls (one per model) as a 2-point one would -- this is why 4 points
  were shipped instead of 2, at no extra API cost, so jumpiness (needed for
  the sigma-widening half, when it's built) doesn't require a second schema
  migration later.
  Shipped: tracker.get_forecast_run_trend(city, target_date, days_out, var) --
  weights the same 3 models as backfill_emos_data's EMOS ens_mean
  (_PREVIOUS_RUN_MODEL_MAP, via _model_weights) across leads N..N+3 (clamped
  to 1-7), so every point in the series uses identical apples-to-apples
  methodology, not a mismatch between a live ensemble mean and a single
  deterministic control run. Returns {"points": [...], "delta": ...,
  "jumpy": ...} or None (days_out<1 -- same-day markets use the METAR
  pipeline instead; unknown city; fewer than 2 usable leads). Cached
  (ForecastCache, 4h TTL, 30min TTL on a negative/failed result so a
  transient API hiccup doesn't blank the signal out for a full 4h) keyed on
  (city, target_date, days_out, var) -- var included because a HIGH and LOW
  market on the same city/date need independently cached series. Wired into
  tracker.log_prediction() as a new run_trend param (3 new columns:
  run_trend_points JSON, run_trend_delta/run_trend_jumpy scalars, schema
  v39->v42). 18 new tests (tests/test_tracker.py) -- fetch parsing/omission/
  failure modes, weighted-delta/jumpy computed from known per-model values
  with intentionally UNEQUAL weights (proves real weighting, not an average
  disguised as one), cache-hit-avoids-second-network-call, log_prediction
  round-trip, and the extraction-helper's field-mapping/malformed-input
  cases (see below). Mutation-tested the weighted-mean line (swapped to a
  plain average on a scratch edit, confirmed the delta/jumpy test fails with
  the exact expected wrong numbers, reverted) -- confirmed the test genuinely
  checks weighting rather than passing spuriously.

  Independently reviewed (Agent, opus) -- found the log-only guarantee held
  and the schema migration was safe, but ONE real MEDIUM issue: the initial
  design called get_forecast_run_trend() directly inside
  weather_markets.analyze_trade(), which put up to 3 sequential HTTP calls
  (up to ~60s worst case on a cache miss) on the live order-placement
  critical path -- analyze_trade's caller only places the order after it
  returns, so a slow fetch would delay an already-fully-decided trade's
  submission even though the fetch itself never touched blended_prob/kelly/
  edge. Fixed by moving the call entirely out of analyze_trade: new
  tracker.get_forecast_run_trend_from_analysis(analysis) extracts city/
  target_date/days_out/var from an analyze_trade()-shaped dict and is now
  the ONLY caller of get_forecast_run_trend, invoked from
  order_executor._prediction_kwargs_from_analysis and main.py's two direct
  log_prediction call sites (cmd_market, cmd_order) -- all of which run at
  LOG time, which for real trades already happens AFTER order placement
  (order_executor._auto_place_trades' own docstring), fully decoupling the
  fetch from fill timing. Side benefit: markets that get analyzed but never
  traded or shadow-logged now skip the fetch entirely, where the original
  design would have fetched for every scanned market regardless of outcome.
  Also fixed 2 LOW robustness gaps the same review surfaced:
  _fetch_previous_run_leads now checks the `hourly` field is actually a
  dict before indexing into it (a malformed API response could otherwise
  raise AttributeError outside the function's own try/except), and
  get_forecast_run_trend's model-weighting/statistics section is now
  wrapped in its own try/except so the function's "never raises" docstring
  claim is actually enforced by the function itself, not just inherited
  from whatever happened to call it. A second finding -- the real
  data/predictions.db had drifted to PRAGMA user_version=40 while already
  having all 3 new columns (caused by this session's own now-fixed
  _SCHEMA_VERSION=40 typo briefly existing while all 3 migration entries
  were already in _MIGRATIONS, and something triggering init_db() against
  the real DB during that window) -- confirmed harmless and self-healing
  (duplicate-column guard), but corrected immediately anyway by calling
  tracker._run_migrations() once by hand rather than leaving it for the
  bot's next natural run.

ENABLEMENT TRIGGER (the actual ask: when to turn this from log-only into a
  real blend input) -- do NOT wire it into blended_prob/sigma/kelly until:
  1. A meaningful number of settled trades have a non-NULL run_trend_delta/
     run_trend_jumpy (check: predictions JOIN outcomes WHERE
     run_trend_delta IS NOT NULL AND settled_temp_f IS NOT NULL). Apply the
     same sample-floor precedent already used elsewhere in this file before
     trusting a per-signal statistic (ACCURACY_MIN_SAMPLE=20,
     SPRT_MIN_TRADES=20, MIN_BIAS_CORRECTION_TRADES=50) -- don't invent a
     new, lower bar just because this signal is new.
  2. A real accuracy check has been run against that data: does a positive
     run_trend_delta (forecast trending warmer) actually correlate with the
     forecast being LOW (i.e. the true value came in even higher), the
     directional-persistence hypothesis this entry is built on? Does high
     run_trend_jumpy actually correlate with larger forecast error (the
     sigma-widening hypothesis)? Both are testable directly against
     settled_temp_f once #1 is satisfied -- don't wire in blend/sigma
     changes on the strength of the mechanism sounding plausible alone.
  3. Only then: revisit the original "What the fix looks like" below for
     the actual blend/sigma wiring, gated behind tracked accuracy like every
     other signal source in this file (see the ENSEMBLE spread bias, NWS
     weight decay, and dynamic bias-correction gates already live in
     weather_markets.py for the established pattern to match).
  This same trigger shape (accumulate -> check correlation against real
  settlement data -> only then wire into the blend) applies to every other
  log-only-shipped signal in this file -- see RICHER ML CALIBRATION
  FEATURES and GENERALIZED PER-MODEL ACCURACY TRACKING below, and the
  candlestick capture / public trade-flow signal entries, all of which have
  the same "ship the logging, evaluate later" shape and the same risk of
  being logged forever and never actually revisited without an explicit
  check-back condition like this one.

Problem (original framing, still accurate for the blend/sigma half):
  The Open-Meteo Previous Runs API is already integrated — but only in
  tracker.py (~2231) / backtest.py for EMOS backfill. The live path never
  compares the current model run against the previous run. Run-to-run
  revision carries signal: a forecast trending warmer across consecutive
  runs tends to keep trending while markets anchor to the older number, and
  high run-to-run jumpiness is a principled reason to widen sigma.

What the fix looks like (still open -- logging half only is done above):
  Feed the now-logged delta/jumpy series as (a) a small directional lean in
  the blend and/or (b) a sigma-widening term when jumpiness is high, once
  the ENABLEMENT TRIGGER above is satisfied.

Why not now:
  - The enablement trigger above hasn't fired yet -- zero settled trades
    have a run_trend_delta value as of 2026-07-16 (the column didn't exist
    until today). Needs real elapsed time + trading activity, not more
    engineering effort.

When to revisit:
  - Check the ENABLEMENT TRIGGER's sample-floor query periodically (e.g.
    whenever this file is next open for other reasons, or paired with the
    DATA-DRIVEN SIGMA FROM SETTLED HISTORY entry's own settled-count check
    below, since both are gated on the same kind of settled-trade volume
    growing over time).

[RICHER ML CALIBRATION FEATURES]
Priority: Medium (model architecture is fine; the feature vector is the
  bottleneck)

Problem:
  The GradientBoosting bias model trains on exactly [our_prob, month,
  days_out, 0.0] (ml_bias.py:204) — four features, the fourth literally
  always zero. Ensemble spread, ICON/GFS disagreement, data_quality score,
  condition_type, and sigma-used are all computed at trade time and
  discarded before training.

What the fix looks like:
  Thread 2-3 of the highest-value candidates (ensemble spread and model
  disagreement first) through the predictions table into the training rows;
  let the existing `features` importance command arbitrate which earn their
  place. Do NOT add many at once — the 200-samples-per-city training gate
  means feature count must stay small relative to data.

Why not now:
  - Needs schema addition + weeks of newly-logged rows before retraining
    can use them (existing rows lack the new fields).

When to revisit:
  - Ship the logging side early (same "data accumulates only from when it
    ships" logic as candlesticks); retrain when enough enriched rows exist.

[GENERALIZED PER-MODEL ACCURACY TRACKING + NEW AI MODEL SOURCES]
Priority: Medium (supersedes-and-extends [TRACK ECMWF FORECAST ACCURACY]
  above — read that entry first)

Problem:
  The existing entry covers instrumenting ECMWF specifically. The general
  version: per-model accuracy is stored as one hardcoded column per model
  (icon_forecast_mean/gfs_forecast_mean), so every new source requires the
  same multi-file plumbing again. Meanwhile free, unconsumed sources exist:
  GEM EPS and UKMO ensembles on the same Open-Meteo ensemble API already
  called, and Google WeatherNext (GraphCast/GenCast lineage) via BigQuery.
  (Verified 2026-07-10: zero code references to gem/ukmo/weathernext.)

What the fix looks like:
  Build the plumbing once as a generic model->forecast_mean mapping (new
  table or JSON column) instead of per-model columns; then adding any
  source is config + fetch only, and each self-calibrates via the existing
  learned-weight machinery. Note the research caveat: pure-AI models
  underperform physics models on record-breaking heat/cold — keep physics
  models in the blend and let learned weights arbitrate.

Why not now:
  - Same reasons as the ECMWF entry (multi-file instrumentation, weeks
    before learned weights activate) — plus a schema-design decision
    (generic table vs columns) that should be made once, deliberately.

When to revisit:
  - When picking up the ECMWF entry — decide then whether to do the generic
    version instead of the one-model version, since the marginal cost is
    small and it prevents a third round of this plumbing.

[LIQUIDITY-AWARE SIZING + DYNAMIC EDGE THRESHOLD]
Priority: Medium (consolidates two existing loose ends into one feature)

Problem:
  Two already-tracked fragments make one coherent feature:
  - paper.slippage_kelly_scale() (paper.py:1843) is a complete 4-tier
    volume/OI Kelly multiplier with zero callers (see [ORPHANED FUNCTIONS]).
  - "Dynamic edge threshold by market liquidity" was planned in
    code_review_plan.md Phase 5, never built (see [MINOR CLEANUP]).
  Net effect today: a signal in a 5-contract-deep book is sized and gated
  identically to one in a 5000-contract book.

What the fix looks like:
  Wire slippage_kelly_scale() into the sizing path (paper.py ~600-650);
  scale the effective min-edge requirement up as book depth/volume drops
  (thin books = your own fill moves the price = need more edge). Synergizes
  with maker-execution work: thin books are where maker fills are slowest
  and adverse selection worst.

Why not now:
  - Sizing-path changes need the usual careful blast-radius pass; better
    tuned once fill-quality instrumentation (above) exists.

When to revisit:
  - Alongside or after the maker-execution upgrades.

[DATA-DRIVEN SIGMA FROM SETTLED HISTORY + CLI-REPORT SETTLEMENT FETCH]
Priority: Medium (sharpens every probability the bot emits; unblocks a
  recorded deferral)

Problem:
  _HISTORICAL_SIGMA is a static 18-city table (missing LasVegas/NewOrleans
  -> generic 3.5 default; see the LV/NOLA entry above); sigma feeds
  gaussian_probability() on every trade. outcomes.settled_temp_f now exists
  with real history but its only consumer is EMOS training. Separately, a
  CLI-report-based settlement fetch (IEM afos API, city->WFO/pil table) was
  designed and proven feasible 2026-07-05 but deferred pending "a consumer
  for settled_temp_f accuracy" — this is that consumer. sigma_audit.py is
  diagnostic-only (its own docstring: "no behavior changes").

What the fix looks like:
  Compute per-city (and per-season, matching the existing table's shape)
  forecast-error sigma from settled_temp_f vs forecast_temp history; fall
  back to the static table below a sample-size floor. Build the CLI-report
  fetch (or the cheaper reactive variant: only on ASOS MISMATCH) so the
  training data uses Kalshi's real settlement source. Fixes the LV/NOLA
  sigma gap as a side effect.

Why not now:
  - Needs enough settled history per city/season to beat the static values
    (check counts before starting); CLI fetch carries the known per-city
    hardcoded-table risk documented in the 2026-07-05 deferral.

When to revisit:
  - Check settled_temp_f row counts per city; start when the biggest cities
    clear a reasonable floor (~30+ per season).

[JOINT FULL-LADDER BRACKET SELECTION]
Priority: Medium-Low (real EV improvement, real design work)

Problem:
  Market analysis is per-bracket independent; nothing chooses the optimal
  COMBINATION of sibling brackets for one city/date against the model's
  full temperature distribution. (The arb auto-trade block in main.py:1542+
  only fires on sum-to-1 violations — different mechanism.) Sequential
  per-market Kelly with position caps is not the same as jointly allocating
  across mutually-exclusive outcomes of one event.

What the fix looks like:
  Group candidate signals by event (city/date/series) before sizing; solve
  the joint Kelly allocation across the group (mutually-exclusive outcomes
  make this a clean simplex problem) instead of greedy per-market sizing.
  Smallest useful version: within an event group, cap total exposure and
  allocate proportionally to per-bracket edge rather than first-come.

Why not now:
  - Real sizing-math design work + touches the same order-placement path as
    several entries above; sequence it after the execution work stabilizes.

When to revisit:
  - After maker-execution + liquidity-aware sizing land, when the sizing
    path is next open anyway.

[WEBSOCKET orderbook_delta get_snapshot -- SIMPLER BOOK INITIALIZATION]
Priority: Low (nice-to-have simplification, not a bug)

Problem:
  Kalshi added a get_snapshot action to update_subscription on the
  orderbook_delta WebSocket channel (April 2026): it returns a one-off
  orderbook_snapshot response for requested markets without subscribing to
  them or affecting the existing delta stream. Found 2026-07-10 via web
  search, not yet checked against kalshi_ws.py's current book-init logic in
  detail.

UPDATE 2026-07-12 (research pass, no code changed) -- premise doesn't apply
as originally framed:
  Checked kalshi_ws.py's actual book-init path: there is no separate REST
  call for it at all. The only REST get_orderbook() call anywhere
  (kalshi_client.py:346) is used once, in main.py's `analyze` CLI command
  (main.py:1262), purely to print a human-readable orderbook table -- it has
  nothing to do with the live WS trading pipeline. Confirmed via
  docs.kalshi.com/websockets/orderbook-updates that subscribing to
  orderbook_delta for a ticker already triggers Kalshi's own automatic
  orderbook_snapshot message first, then incremental deltas -- and
  kalshi_ws.py's parse_message() already handles that exact
  "orderbook_snapshot" message type (kalshi_ws.py:88). So there's no
  REST-call redundancy to remove; book init is already snapshot-via-WS with
  zero REST involvement.
  get_snapshot's real, narrower value for this bot: a one-off book peek for
  a ticker that is NOT in the persistent per-cycle subscription list (e.g.
  previewing a candidate market's current book before deciding whether to
  add it to cron.py's per-cycle _ws.subscribe() call), without growing the
  subscription list or opening a second connection. That's a smaller,
  different use case than "simpler book initialization" as originally
  framed -- there's no current call site that needs it (this bot always
  subscribes up front to every scanned ticker), so it stays a nice-to-have
  pending a concrete use case, not a simplification of existing code.

What the fix looks like:
  No current call site needs this. If a future feature wants a one-off book
  check for a ticker outside the active subscription list (rather than
  simplifying anything that exists today), use update_subscription's
  get_snapshot action for that instead of adding a new REST call or growing
  the persistent subscription list.

Why not now:
  - Unverified against this repo's actual current book-init code path;
    purely a simplification if it applies, no functional gap being closed.
  - Re-verified 2026-07-12: it doesn't apply as a simplification (there's no
    existing REST-call redundancy to remove) -- only relevant if/when a
    concrete one-off-book-peek use case appears.

When to revisit:
  - Alongside the RESTING EXIT ORDERS + OCO entry above (same file,
    adjacent surface area) or whenever kalshi_ws.py is next touched.

[PUBLIC TRADE-FLOW SIGNAL]
Priority: Low-Medium (cheap to capture, speculative value)

Problem:
  kalshi_ws.py subscribes only to ticker + orderbook_delta (line 301);
  Kalshi's public `trade` channel (executed trades feed) is unused. Signed
  taker flow between scans would show when someone with a faster pipeline
  moved first on a model run — useful as a caution flag (widen required
  edge) or confirmation (market converging toward our stale signal).

What the fix looks like:
  Add the trade channel to the WS subscription, accumulate signed flow per
  ticker between scans, expose to analyze_trade() as a small adjustment or
  gate input. Evaluate on paper before giving it any weight.

Why not now:
  - Value unproven; WS client currently only runs during watch mode, so
    coverage between manual cron runs is zero anyway — worth more once the
    bot runs continuously.

When to revisit:
  - Once loop/watch runs continuously again post-Belgium.

[NWS AFD (AREA FORECAST DISCUSSION) PARSING]
Priority: Low (stretch/novel — highest differentiation, least proven)

Problem:
  Each WFO publishes an AFD where human forecasters state confidence and
  explicit disagreement with model guidance ("models overmixing, expect
  highs 2-3F below guidance") — exactly the correction the recurring
  NWS-bias investigations hunt for statistically, published free per city
  per day. Nothing consumes it.

What the fix looks like:
  Fetch AFDs via the same IEM afos API already scoped for the CLI-report
  fetch (shared city->WFO table — build once for both). LLM-extract a
  per-city confidence score + directional lean vs guidance; feed as a
  small bounded blend adjustment, gated behind tracked accuracy like any
  other source.

Why not now:
  - Adds an LLM dependency + per-day API cost to the pipeline; extraction
    quality needs its own evaluation before the signal can be trusted;
    depends on the same WFO table as the CLI-report fetch (build together).

When to revisit:
  - Alongside the CLI-report fetch (shared infrastructure), or as a fun
    standalone experiment scored in shadow mode first.

[CITY-LOCAL AFTERNOON SAME-DAY SWEEP]
Priority: Low (mostly covered by loop/watch when they run)

Problem:
  By early afternoon a day's high is substantially determined and METAR
  lock-in gets very sharp, but there is no scan timed to each city's local
  ~2pm. loop (every 4h) and watch (every 5min) cover this incidentally when
  running; the manual-cron cadence does not.

What the fix looks like:
  A lightweight `cron --sameday-only` mode (skip multi-day scan + batch
  prewarm; METAR + same-day markets only) cheap enough to run at each
  city's local afternoon window, or an extra loop-mode wake timed to the
  2pm ET / 2pm PT bands.

Why not now:
  - Marginal over a continuously-running loop; only meaningfully valuable
    during manual-cadence periods like the current trip.

When to revisit:
  - If manual cadence continues long-term, or as a small quality-of-life
    flag next time cron.py is open.

======================================================
2026-07-16 WHOLE-PROGRAM SCOUTING SESSION (feature half) -- three parallel
Fable 5 passes (feature/signal candidates, refactor/design-debt, architecture
-level structural gaps), each reading backlog.txt in full first so nothing
below duplicates an existing open/resolved/closed entry. This is the feature/
signal half; the architecture-level and refactor/design-debt findings from
the same session are in their own section further down (search "ARCHITECTURE
& DESIGN-DEBT CANDIDATES"). Reviewed in an HTML triage artifact before
filing; user approved merging into this file.
======================================================

[REAL NBM VIA IEM NBS STATION BULLETINS -- REPLACE THE SILENT best_match SUBSTITUTE]
Priority: Medium-High -- the bot believes it has an "nbm" ensemble member; it
  actually has an arbitrary auto-selected model, and the real thing is one
  parameter away on an API already integrated.

Problem:
  fetch_temperature_nbm() (weather_markets.py:1617) no longer fetches NBM at
  all -- Open-Meteo removed the model -- and the function now requests
  models: "best_match" (weather_markets.py:1655, documented at :1623), an
  uncalibrated auto-selection, yet still feeds model_temps["nbm"] in
  analyze_trade's Phase-C extended ensemble (weather_markets.py:5642) and
  _compute_ensemble_spread. The 2026-07-12 piece-3 closure rejected only the
  NBM-delta bias-correction premise ("correcting toward another model's
  guess"); it never addressed that the "nbm" blend member itself is now
  mislabeled. Meanwhile mos.py already integrates IEM's MOS API
  (mesonet.agron.iastate.edu/api/1/mos.json, mos.py:21) but only ever
  requests GFS/NAM (mos.py:83, 215). Live-verified 2026-07-16: the same
  endpoint with model=NBS returns current-day NBM station bulletins for
  KNYC -- real rows, hourly tmp plus n_x (the NBM day-max/night-min field),
  sky, gst, p12. That's genuine NBM at the exact ASOS settlement stations
  (mos.py's own docstring: "same ASOS stations Kalshi settles on"), better-
  targeted than the old Open-Meteo gridpoint NBM ever was.

What it would look like:
  Add model="NBS" support to mos.py's fetch (parse n_x for the daily
  max/min rather than scanning hourly tmp), and point
  fetch_temperature_nbm() at it (or add a sibling fetch_nbm_iem() and let
  model_temps["nbm"] consume that), keeping best_match only as fallback.
  Station map already exists (_MARKET_STATION_MAP, mos.py:32). Extend
  beyond the 6-city _MOS_CITIES subset -- NBM covers all ASOS stations, so
  all 20 cities work, unlike GFS/NAM MOS.

Why not now:
  - Needs a per-station parse of NBS's 12-hourly n_x semantics (which row
    is the max for the local calendar day -- same timezone care as CLI
    settlement); verify a few cities beyond KNYC return rows; decide
    whether the restored member also feeds _compute_ensemble_spread
    immediately or ships log-only first per house precedent.

When to revisit:
  - Pairs naturally with the NBM PROBABILISTIC QUANTILES entry below (same
    IEM/NBM surface) -- scope both together.

[RESOLVED 2026-07-17] DISCRETIZATION-CONSISTENT T-BUCKET THRESHOLDS -- HALF-DEGREE CONTINUITY
Priority: was Medium-High -- systematic, direction-consistent probability
  skew on every tail-bucket trade; bordered a correctness issue, not just a
  feature idea.

Problem (as originally scoped):
  _parse_market_condition already reasons explicitly about integer
  settlement for between-buckets -- B87.5 -> [86.5, 88.5], with a comment
  that brackets "must tile without gaps" (weather_markets.py:3719-3723).
  But T-buckets kept raw integer thresholds: T92 -> {"above", 92.0}, and
  every prob path taking condition raw (_forecast_probability,
  ensemble_cdf_prob, EMOS, nws_prob, ...) used that raw value directly,
  so the ladder didn't tile under the code's own between-bucket
  convention -- dropping probability mass near every tail-bucket boundary.

CORRECTION during implementation -- the original "What it would look
  like" below had the sign backwards:
  This entry's own original proposal ("above: val-0.5; below: val+0.5")
  was never live-verified and turned out to be wrong. Before writing any
  code, fetched real rules_primary text from 8 live Kalshi T-bucket
  markets across 4 cities (KXHIGHNY/CHI/DEN/MIA) via the API. Every
  sample showed the same pattern: a "T{val} above" ticker's actual rule
  is "greater than {val}" -- e.g. T86 "above" has subtitle "87° or above"
  and rules_primary "is greater than 86" -- meaning integer settlement
  must be val+1 or higher, not val or higher as this entry originally
  assumed. Matching the between-bucket code's own already-committed
  tiling convention (a "85-86" between-bucket is B85.5 -> [84.5, 86.5]),
  the correct continuous boundary is val+0.5 for above and val-0.5 for
  below -- the OPPOSITE sign from the original proposal. Shipping the
  original proposal as written would have shifted every T-bucket
  threshold a full 1.0°F in the wrong direction (turning a 0.5°F bug into
  a 1.0°F bug on the other side), not fixed it. Full ladder check around
  a boundary: T67-below (wins <=66) -> 66.5; B67.5 -> {67,68}; T68-above
  (wins >=69) -> 68.5. Integer 68 lands in exactly B67.5 and not in
  T68-above (68 > 68.5 is false) -- tiles with no gap or overlap.

What shipped:
  _parse_market_condition (weather_markets.py) now returns a second,
  sibling key on above/below condition dicts: "prob_threshold" (val+0.5 /
  val-0.5), alongside the existing "threshold" (raw ticker/rule value,
  UNCHANGED). Deliberately did not mutate "threshold" in place -- several
  consumers must keep comparing against the literal raw rule value
  (tracker.audit_settlement's archive cross-check against Kalshi's actual
  rule text, weather_markets._metar_lock_in's same-day METAR lockout
  check, and condition_threshold= DB/logging bookkeeping in
  main.py/order_executor.py) and mutating threshold would have silently
  corrupted all three. A single shared helper, utils.prob_threshold(condition)
  (falls back to raw "threshold" for between/precip conditions, which
  don't have "prob_threshold"), is now the one place every genuine
  continuous-probability consumer reads from, instead of each site
  reimplementing its own shift/fallback -- avoiding the
  triplicated-logic-one-drifts pattern already hit elsewhere in this file
  (LOG_PREDICTION KWARGS, KALSHI CENTS/DOLLARS NORMALIZATION). Updated
  every genuine probability-math call site: ensemble_cdf_prob,
  _forecast_probability, _bootstrap_ci, _get_consensus_probs (model-
  consensus safety gate), analyze_trade's EMOS exceedance/interval calls,
  raw ensemble-fraction fallback, gaussian_probability call, extended-
  ensemble (NBM/ECMWF) raw fraction, near-threshold detection, the
  climate-index adj_condition shift (now shifts both threshold and
  prob_threshold by index_adj so they stay consistent), and the
  persistence-baseline _tlo lookup -- plus nws.py's nws_prob and
  obs_prob, climatology.py's _climatological_prob_inner, and backtest.py
  (so backtest results keep matching live behavior). Confirmed
  audit_settlement, _metar_lock_in, settlement_monitor.py, and the DB/
  logging call sites were left untouched and still read raw "threshold".
  Tests (tests/test_weather.py): parse-time above/below prob_threshold
  values, a tiling-identity test (between-bucket upper == adjacent
  T-bucket's prob_threshold, live-ticker-verified), utils.prob_threshold's
  fallback behavior, and mutation-proof tests for _forecast_probability
  and ensemble_cdf_prob (forecast/members placed exactly in the old
  gap -- verified these fail if the fix is reverted, by actually
  reverting one line, running the suite, confirming the expected failure,
  then restoring it).

  Independently reviewed (Agent, opus) before commit: re-derived the
  sign/magnitude from the tiling argument independently (confirmed
  correct), audited every touched call site (confirmed each is a genuine
  probability computation, not a should-stay-raw site), searched for
  missed consumers, and checked the None-handling `or 0` idiom used at
  two sites for a silent-remap-of-a-real-0.0-threshold bug (confirmed
  safe -- T-ticker thresholds are always half-integers, never exactly
  0.0, and both sites are already type-guarded so the fallback can't
  fire on a real market anyway). No bugs found.

Known residual (not fixed here, correctly out of scope):
  - sigma_audit.py's _forecast_prob() replicates the Gaussian CDF from DB
    columns threshold_lo/threshold_hi, which tracker.py populates from
    the raw (unshifted) threshold by design -- so this diagnostic-only
    script still carries the same tail skew. Revisit if/when sigma_audit
    output is used for more than diagnostics.
  - nws.py's nws_prob_from_quantiles() is dead code (only referenced by
    tests/docs/backlog, not wired into analyze_trade or any live path,
    confirmed via the existing NBM PROBABILISTIC QUANTILES entry above).
    If it's ever wired in, its caller must pass prob_threshold, not raw
    threshold.
  - Calibration-reset interplay (per-city Platt/temperature scaling may
    have been partially absorbing the average skew this fixes) not
    separately audited post-ship -- watch get_calibration_by_city /
    condition-type Brier over the next batch of settled trades per this
    entry's original note.

[AMEND ORDER (V2) -- REPLACE CANCEL-VERIFY-REPLACE IN THE REPRICE LOOP]
Priority: Medium -- removes the exact client-side race window the reprice
  feature needed hardened plumbing to work around, and halves write-call
  count per reprice.

Problem:
  kalshi_client.py exposes only place/get/cancel (place_order :401,
  get_order :551, cancel_order :560) -- no amend. So
  _reprice_or_cancel_pending_orders (order_executor.py:779) does a
  3-step cancel -> verify-unfilled -> replace via
  _cancel_and_verify_safe_to_replace (order_executor.py:683), whose
  fill-race handling required threading raw API status through
  _finalize_cancel (the 2026-07-12 review's fix #3) and still leaves the
  "fills exactly as the post-cancel verification query fails" settlement
  gap that got spawned as its own follow-up. Verified via docs search
  2026-07-16: Kalshi has Amend Order (V2) -- POST
  /trade-api/v2/portfolio/events/orders/{order_id}/amend (same V2
  events-orders family this repo migrated to on 2026-07-12), body
  {ticker, side, price, count, client_order_id, updated_client_order_id},
  response {order_id, remaining_count, fill_count, ts_ms}; plus a
  separate Decrease Order endpoint. Amend is a single atomic exchange-side
  operation -- no client-side window where the order is gone-but-not-
  replaced or filled-mid-sequence.

What it would look like:
  Add amend_order() to kalshi_client.py (V2 fixed-point shape -- read the
  live docs page for exact field encoding before coding, same caution as
  the create/cancel migration); in _reprice_or_cancel_pending_orders, use
  amend for the reprice-improve branch (same order, new price) and keep
  cancel+IOC-replace only for the taker-cross branch (different order
  type). Log replaces_order_id chains the same way (execution_log already
  has the column).

Why not now:
  - Docs note queue position is preserved only on size decrease (price
    amends go to the back -- no queue-priority win, "only" atomicity +
    rate-limit savings); ENABLE_MICRO_LIVE=False means no live orders
    exist to reprice yet, so this naturally batches with the next
    order-path session. Verify amend's response/error shape against
    DEMO_BASE first.

When to revisit:
  - Whenever the order-execution path is next open for other reasons, or
    once live trading resumes and repricing actually runs.

[MARKET-IMPLIED TEMPERATURE DISTRIBUTION FROM THE FULL LADDER]
Priority: Medium -- turns data already fetched every scan into a per-event
  consensus signal; cheap, log-only ship.

Problem:
  Every market comparison is per-bracket: parse_market_price mid
  (weather_markets.py:3083-3128) vs model prob, with per-market anchors
  _MARKET_ANCHOR_BETWEEN/ABOVE/BELOW (weather_markets.py:173-175).
  Nothing ever inverts the full sibling ladder (all brackets of one
  city/date, already fetched in the same scan) into a market-implied
  temperature distribution -- implied mean/sigma from a monotone CDF fit
  over cumulative bracket prices. The existing JOINT FULL-LADDER BRACKET
  SELECTION entry is about *sizing* across brackets; this is about
  *extracting the market's own forecast* as a signal. Uses: (a) a
  self-consistent anchor (individual mids can be stale/one-sided -- the
  fitted CDF pools the whole event book); (b) a scalar "market-implied
  mean minus model mean, F" logged per prediction -- directly comparable
  to run_trend_delta and testable against settled_temp_f with the same
  enablement-trigger shape; (c) research into which bucket types
  systematically misprice (favorite-longshot tilt on tails).

What it would look like:
  Helper that groups sibling markets by event (the parse machinery in
  parse_city_date :3448 already extracts city/date), sorts brackets, fits
  mean/sigma minimizing squared error to bracket mids weighted by
  liquidity, returns {implied_mean, implied_sigma, fit_residual}. Log into
  predictions (2-3 columns, same migration pattern as run_trend). Do NOT
  wire into blend until settled-data correlation check passes (same
  ENABLEMENT TRIGGER precedent as run_trend, above).

Why not now:
  - Needs thin-book handling (skip fit when fewer than ~3 two-sided
    brackets -- is_liquid :3777 gives the gate); value is research-first,
    so it competes with other log-only signals for the same "will anyone
    ever check back" risk this file already flags.

When to revisit:
  - Alongside JOINT FULL-LADDER BRACKET SELECTION, since both read the
    same sibling-ladder data -- worth scoping together rather than
    separately.

[PUBLIC TRADES REST BACKFILL -- GET /markets/trades AT SETTLEMENT]
Priority: Medium -- closes the exact gap the PUBLIC TRADE-FLOW SIGNAL entry
  admits ("coverage between manual cron runs is zero") without needing the
  always-on host.

Problem:
  The PUBLIC TRADE-FLOW SIGNAL entry (above) is scoped to the WS `trade`
  channel, which only helps while a watch session is running. Verified
  2026-07-16 via docs search: GET /markets/trades is public,
  unauthenticated, cursor-paginated (limit 1-1000), returns
  taker_side/count/yes_price/no_price/created_time per fill.
  kalshi_client.py has no trades endpoint (full method list read today:
  markets/market/orderbook/candlesticks/events/series/balance/positions/
  orders only). That means the complete signed-flow history of every
  settled market is retrievable retroactively -- the same "data
  accumulates regardless of process uptime" property that justified the
  candlestick capture, but with direction (taker_side) that OHLC candles
  lack.

What it would look like:
  kalshi_client.get_trades(ticker, min_ts, max_ts) + a trade_history
  table, wired into tracker.sync_outcomes() (tracker.py:3081) right
  beside the existing candlestick backfill (fires once per market at
  outcome recording, isolated try/except, same pattern commit ba3527e
  established). Analysis payoffs ride the already-deferred candlestick
  analysis passes: adverse selection around our fill times (joins against
  execution_log's filled_at/market_mid_at_fill), and "did informed flow
  precede settlement-direction moves" -- the caution-flag hypothesis the
  WS entry wants, testable on history before any live WS work.

Why not now:
  - Storage volume check first (trades per weather market are modest, but
    confirm with one live call); shares the enablement-trigger risk with
    the candlestick entry -- pair its analysis check-back with that
    entry's existing 2026-07-16 trigger so they're evaluated together.

When to revisit:
  - Alongside the PUBLIC TRADE-FLOW SIGNAL entry above and the
    candlestick capture entry's own check-back trigger.

[NBM PROBABILISTIC QUANTILES -- ORPHANED CONVERTER, MISSING FETCHER]
Priority: Low-Medium -- the consumer half already exists in-tree; only the
  fetch half is missing, but the fetch half needs real research.

Problem:
  nws.nws_prob_from_quantiles() (nws.py:324-376) is a complete,
  documented NBM-percentile->probability converter (linear ECDF over
  {10,25,50,75,90} percentiles with tail extrapolation) with zero
  production callers and zero code anywhere fetching NBM percentiles
  (repo-wide grep 2026-07-16: only its own definition). nws.py is outside
  tests/test_dead_code_scan.py's scope (paper/tracker/weather_markets
  only), so the scan will never surface it. NBM's quantile output (QMD)
  is a calibrated per-day, per-station uncertainty estimate -- a daily
  dynamic sigma that would sharpen the current per-month climate sigma
  (get_historical_sigma :2209) and capture skew a symmetric Gaussian
  can't.

What it would look like:
  Research pass first -- NBM percentiles live in the QMD grib products on
  NOMADS (not in the NBS text bulletins live-tested for the REAL NBM
  entry above, and not exposed by api.weather.gov); check whether IEM or
  another JSON-friendly mirror serves them before committing to grib
  parsing. If a clean source exists: fetch per station/day, feed
  nws_prob_from_quantiles as either the NWS-slot probability for tail
  buckets or a sigma source, gated/log-only first.

Why not now:
  - No verified JSON source yet (grib parsing would add a heavy
    dependency for one signal -- likely not worth it alone); do the
    source-hunting as part of the REAL NBM entry's IEM work since it's
    the same product family. If no clean source exists, decide
    wire-up-or-delete for the orphan explicitly and extend the dead-code
    scan to nws.py either way.

When to revisit:
  - Scoped together with REAL NBM VIA IEM NBS STATION BULLETINS above.

[FORECAST-CONDITION COVARIATES FOR SIGMA -- INCLUDING FIELDS ALREADY FETCHED AND NEVER READ]
Priority: Low-Medium -- sigma today conditions on city/month/horizon/spread
  but never on what kind of weather day it is; several inputs are already
  being paid for.

Problem:
  The daily forecast fetch requests only
  temperature_2m_max,temperature_2m_min,precipitation_sum
  (weather_markets.py:983); the temperature sigma path
  (weather_markets.py:5620-5660) uses climate sigma x horizon x spread,
  and regime.py conditions only on ensemble mean/std. No weather-type
  conditioning exists anywhere -- yet precip/frontal days have
  systematically larger high-temp errors and cloud-cover busts are the
  classic daily-high miss. Concretely unused today: precipitation_sum is
  fetched with every forecast and never consumed in the temperature path
  (grep of analyze_trade's temp section: precip only routes precip-market
  types at :5340-5350); Pirate Weather's wind_gust/_wind_gust_time_unix/
  _hourly_window_high_f are fetched and stored into the forecast dict
  (weather_markets.py:1097-1100, 2114-2119) with zero readers anywhere
  (repo-wide grep). The NBS bulletins from the REAL NBM entry above would
  add sky/gst/p12 in the same call.

What it would look like:
  Log 2-3 covariates (forecast precip_sum, sky/cloud, gust) into
  predictions alongside the existing run_trend columns (same v-migration
  pattern), correlate |settled_temp_f - forecast| against them once
  sample floors clear, then a bounded sigma multiplier for high-precip-
  probability days. Distinct from RICHER ML CALIBRATION FEATURES, which
  proposes *internal* features (ensemble spread, model disagreement,
  data_quality) -- this is external weather-type covariates; if the ML
  entry ships first, these become additional candidate features for the
  same GBM instead of a separate mechanism.

Why not now:
  - Same weeks-of-accumulation gate as every log-only signal; keep
    feature count small vs the 200-samples-per-city training gate the ML
    entry already flags. The dead wind_gust/_hourly_window_high_f fields
    deserve a wire-up-or-delete decision regardless (dict fields are
    invisible to the dead-code scan).

When to revisit:
  - Alongside RICHER ML CALIBRATION FEATURES -- decide then whether these
    are separate covariates or additional features for the same model.

[CROSS-CITY RECENT-ERROR POOLING -- SYNOPTIC-REGIME BIAS SHARE]
Priority: Low-Medium -- the spatial correlation structure is already
  learned and stored; it's only ever used to *penalize risk*, never to
  *improve the forecast*.

Problem:
  Bias correction is strictly per-city and slow: _get_combined_station_bias
  (weather_markets.py:303-337) blends a static table with
  tracker.get_dynamic_station_bias (min 10 samples, weeks of
  accumulation, 4h cache). Nothing uses the last 24-48h of realized
  errors in *other* cities to nudge today's forecast -- even though
  forecast errors are spatially correlated under a shared synoptic
  regime, and the bot already quantifies exactly those correlations
  (_CITY_PAIR_CORR paper.py:313-332; tracker.get_recent_city_correlations
  refreshed into monte_carlo.simulate_portfolio at monte_carlo.py:
  242-264) -- but solely for portfolio VaR. If the model ran 2F hot
  across the whole Gulf Coast group yesterday, today's NOLA/Houston
  forecasts are more likely hot too; the per-city dynamic bias can't see
  that for weeks.

What it would look like:
  tracker.get_regional_recent_bias(city, var, hours=48) --
  correlation-weighted mean of correlated cities' settled errors over the
  last 1-2 days (data already in outcomes.settled_temp_f; the
  correlated-group structure already in _CORRELATED_CITY_GROUPS
  paper.py:283-287), returned with sample count. Ship log-only (a
  regional_bias column beside run_trend), verify it actually correlates
  with same-day own-city error at settlement, then a small bounded lean
  capped well below _get_combined_station_bias's magnitude.

Why not now:
  - Settled-outcome latency (an error is only known after settlement +
    sync -- check whether "yesterday's" errors are actually available by
    today's scan time in practice); needs the same sample-floor and
    enablement-trigger discipline; low per-city row counts today (3-14
    per the 2026-07-12 audit) mean the pooled signal is the *point* but
    also hard to validate initially.

When to revisit:
  - Once per-city settled counts grow past the same floor used elsewhere
    (see DATA-DRIVEN SIGMA FROM SETTLED HISTORY's own count check).

[MARKET_LIFECYCLE_V2 WS CHANNEL -- LISTING-TIME AWARENESS]
Priority: Low -- real timing edge, but structurally blocked behind the
  always-on host decision.

Problem:
  Confirmed by direct docs fetch 2026-07-16, Kalshi's WS channel list is
  orderbook_delta, ticker, trade, fill, market_positions,
  market_lifecycle_v2, multivariate_market_lifecycle, multivariate,
  communications, order_group_updates, user_orders, cfbenchmarks_value,
  pyth_value -- this bot subscribes to exactly two (kalshi_ws.py:362) and
  the backlog covers trade/fill/order_groups, but market_lifecycle_v2
  (and user_orders) appear nowhere in repo or backlog (grep: zero hits
  for market_lifecycle). New daily temperature markets are discovered
  only when the next REST scan happens (get_weather_markets + 60s
  _MARKETS_CACHE_TTL :744-745, on cron cadence) -- hours after listing.
  Freshly listed markets have the widest spreads and least-informed
  quotes; the first maker quote at model fair value gets the best prices
  of the market's life, and the deferred candlestick entry-timing
  analysis can verify this from data already being captured (candles
  cover the full open->close window, so "edge vs hours-since-open" is
  computable today for settled markets).

What it would look like:
  Subscribe market_lifecycle_v2 in the (future) always-on watcher; on a
  new KXHIGH*/KXLOW* listing, run analyze_trade for that market
  immediately and post a resting maker order if gates pass. Cheap
  precursor available now with zero new infra: use the existing candle
  history (tracker.get_price_history) to measure whether early-window
  prices were actually more mispriced vs settlement -- do that before
  building anything live.

Why not now:
  - Meaningless without the always-on host (explicitly parked in the
    MOVE OFF LOCAL CRON entry, trigger = live trading imminent); do the
    candle-based "is early entry actually better" check first -- if edge
    doesn't decay with market age, this whole candidate dies cheaply.

When to revisit:
  - Once the always-on host decision executes. Note user_orders as a
    sibling channel to fold into the already-deferred fill-channel work
    when that resumes.

======================================================
ARCHITECTURE & DESIGN-DEBT CANDIDATES -- 2026-07-16 whole-program scouting
session (architecture + refactor halves; see the section above for the
feature/signal half from the same session). Same three-Fable-pass sourcing
and verification discipline as above.
======================================================

[THE ONLY LIVE-ORDER PATH IS THE INTERACTIVE WATCH CLI -- NO HEADLESS TRADE-CYCLE ENGINE]
Priority: High -- a stated prerequisite for the already-decided VM move +
  deferred always-on watcher; this is the refactor that determines whether
  that work is a port or a rewrite.

Problem:
  There are two parallel scan pipelines, and the hardened one can't trade
  live while the live one isn't hardened. cron pipeline (cron.py:475
  _cmd_cron_body, ~2,100 lines): the production path -- kill-switch/
  override/accuracy-halt/graduation gates, consistency-violation halt
  (cron.py:1112-1136), batch cache prewarm, ThreadPoolExecutor scan via
  _enrich_and_analyze (cron.py:1369), is_stale exclusion, gate counters,
  shadow logging, tiered ctx.auto_place_trades(...) (cron.py:1886/1895)
  -- paper only; cron never passes live=True. watch pipeline (main.py:
  2822 cmd_watch): a `while True` terminal UI wrapping _analyze_once
  (main.py:1294) -- a *sequential* scan with different gates (skips
  days_out==0 at 1337, no is_stale, no consistency halt, no prewarm) --
  and this is the *only* path that can place a live order
  (_auto_place_trades(..., live=live) at 2875-2877, then poll/reprice/
  exit-protection at 2878-2886). So when live trading starts, real money
  runs through the interactive-display pipeline while the
  production-hardened pipeline stays paper. And the deferred always-on
  watcher ("reacts between cycles") has no home: it would be a *third*
  hand-assembled sequence of scan->analyze->place->poll->reprice->
  protect, because that sequence exists only as inline orchestration
  inside two different functions, each with its own subset and ordering.
  The exit-protection calls are already duplicated verbatim between them
  (cron.py:757-768 vs main.py:2878-2886).

What it would look like:
  Extract a headless run_trade_cycle(client, mode: paper|live, triggers)
  engine owning the canonical ordered sequence (recover-pending -> settle
  -> scan -> analyze -> decide/place -> poll fills -> reprice -> position
  protection -> log), with the CLI display and the cron wrapper both
  becoming thin callers. The always-on watcher then becomes "call the
  engine's protection stage on WS events + the full cycle on a timer"
  instead of a third reimplementation. The CronContext injection seam
  (cron.py:78, main.py:208) is already halfway there -- the engine is
  mostly a matter of moving orchestration out of _cmd_cron_body/
  cmd_watch into it. Smallest safe step available right now, independent
  of the full engine: add cron's per-ticker dedup + is_stale filter to
  _analyze_once's loop (~8 lines) -- closes the feature gap between the
  two pipelines without the reshuffle.

Why not now:
  - Do it as the *first* step of the VM/always-on work (the trigger
    already defined: live trading imminent), not before -- it's a large,
    risky reshuffle of the two most safety-audited files, and doing it
    while the process model is still "one-shot cron + occasional watch"
    adds no immediate behavior. But do it *before* writing any watcher
    code, or the watcher hardcodes pipeline #3.

When to revisit:
  - Same trigger as MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM
    (above): whenever the user is ready to actually execute that move,
    this is the first engineering step, not the VM provisioning itself.
    The smallest-version dedup/is_stale parity fix is safe to do sooner,
    independent of that trigger.

[NO MARKET-TYPE SEAM -- TEMPERATURE IS THE ARCHITECTURE, NOT A MARKET TYPE]
Priority: Medium-High -- hourly-directional (KXTEMPxxxH) is the nearest
  stated expansion and would hit every one of these walls; rain is second.

Problem:
  Market-type knowledge is not behind an interface; it's a set of
  string-match conventions repeated independently across the pipeline,
  and the deepest layer (ground truth/calibration) only has a concept of
  temperature at all. Dispatch: analyze_trade() (weather_markets.py:5145)
  is a ~1,660-line monolith whose market-type "routing" is two
  early-return fast-paths keyed on parsed condition type (precip at
  5340-5347, snow at 5350-5355); everything after line 5357 IS the
  temperature model. There is no third place to plug in an hourly-
  threshold model except a third inline fast-path. Variable derivation by
  ticker substring is duplicated: `var = "min" if "LOW" in series else
  "max"` exists independently at weather_markets.py:5414 and 6248,
  tracker.py:2544-2554 (settlement audit), and paper.py:1148-1154
  (_score_ensemble_members) -- four copies of the same convention, each
  of which a new market type must extend or silently mis-handle
  (paper.py's copy defaults unknown tickers to "max"). Ground truth is a
  temperature column: outcomes stores settled_yes + settled_temp_f
  (tracker.py:122, 310-314). The settlement-audit path explicitly bails
  for anything else -- tracker.py:2554: `return False  # precipitation or
  unknown -- skip`. Everything downstream of settlement -- EMOS training,
  data-driven sigma, dynamic station bias, per-model accuracy, ml_bias
  temperature scaling -- consumes settled_temp_f. A rain market can trade
  today (the precip fast-path exists) but can never learn: no ground
  truth is ever recorded for it, so it's permanently outside the
  calibration loop that is this bot's actual moat. Degraded analysis
  dicts already bite: the precip/snow fast-paths return reduced dicts
  (hardcoded "time_risk": "HIGH" at 4792/4999, no entry_price), which
  already silently disabled the reprice taker-cross for precip markets
  (documented as an "informational finding" in the maker-execution
  entry). check_series_drift() (weather_markets.py:3308) diffs only
  KXHIGH*/KXLOW* (3332-3339) against the hardcoded KNOWN_WEATHER_SERIES
  list -- already flagged in the hourly/rain entries, but note it's the
  same missing seam, not a separate fix.

What it would look like:
  A MarketType protocol owning the five things currently keyed on string
  conventions: (1) series-prefix set for discovery/drift, (2) condition
  parsing, (3) probability model entry point (analyze(enriched,
  condition) -> analysis), (4) ground-truth spec -- what observable
  settles it and how to fetch it, (5) calibration-pool key (so hourly
  rows never pollute the daily 'sameday' T-scaling pool -- the exact
  separation the hourly backlog entry already demands). The outcomes
  table needs a generic settled_value REAL + settled_var TEXT shape
  instead of settled_temp_f. The temperature implementation would
  initially just be the existing code behind the interface -- the win is
  that hourly/rain get slots instead of fourth and fifth copies of the
  "LOW" in ticker convention.

Why not now:
  - Worth doing the ground-truth generalization (settled_value/
    settled_var) as a cheap early step the moment any second market type
    is greenlit -- retrofitting calibration history is impossible (the
    v35 migration comment at tracker.py:165-171 documents exactly this:
    rows logged before var existed could never be backfilled). The full
    MarketType protocol only pays when the hourly or rain entry is
    actually picked up; don't build it speculatively.

When to revisit:
  - The moment HOURLY-DIRECTIONAL TEMPERATURE MARKETS or RAIN / SNOW /
    HURRICANE MARKETS (above) is actually greenlit -- do the ground-truth
    generalization first, before either.

[PAPER AND LIVE POSITIONS ARE TWO LEDGERS WITH ADAPTER GLUE -- EVERY RISK FEATURE SHIPS TWICE]
Priority: Medium -- cost compounds per feature once live trading is real;
  currently only ~4 features deep. Includes a real, already-observed
  drift: exit-gate thresholds duplicated as bare literals across both
  files with a docstring/code mismatch already caught (see below).

Problem:
  A "position" exists in two unrelated persistence models -- paper's JSON
  ledger (data/paper_trades.json, full read-modify-write under
  _DATA_LOCK, paper.py:87/107) and live's execution_log SQLite rows --
  and the live side reaches feature parity by adaptation and replication,
  not sharing. order_executor._get_live_open_positions()
  (order_executor.py:955) reshapes execution_log rows into "whatever keys
  paper's check functions happen to read (verified by reading both
  bodies)". _update_live_peak_profits() (order_executor.py:996)
  *replicates* paper's peak math because paper.update_peak_profits
  ignores its own parameter and re-reads paper's JSON internally --
  i.e., paper's functions are storage-coupled in ways discovered
  per-function. _check_live_model_exits() (order_executor.py:1214) is a
  ~100-line near-clone of _check_early_exits (order_executor.py:1625),
  built as a separate function "to avoid any risk of regressing the
  existing paper-only path". The divergence already produced asymmetry:
  the multiple-positions-per-ticker collapse bug was fixed on the live
  side only, with paper's identical pattern left in production
  (documented in the RESTING EXIT ORDERS resolution above). Beyond the
  structural glue, gate constants are hand-copied as bare literals: the
  24h settlement-convergence gate at four sites (paper.py:1339-1357,
  1431-1449; order_executor.py:1693-1713, 1277-1298), the 12h
  minimum-hold gate at three (paper.py:2185-2197; order_executor.py:
  1671-1683, 1263-1275) -- none in get_config_fingerprint(). Drift has
  already started at the doc level: _check_early_exits' docstring
  (order_executor.py:1628) says "shifted >15 percentage points" while the
  code gates on `shift > 0.25` (line 1715).

What it would look like:
  Two-tier fix. Now: named module constants (EXIT_MIN_HOLD_HOURS=12,
  EXIT_SETTLEMENT_GATE_HOURS=24, MODEL_EXIT_SHIFT_PP=0.25) shared by both
  files and added to get_config_fingerprint(), plus one extracted
  _passes_exit_gates(entered_at, close_time, log_tag) -> bool used by all
  5-6 sites. Later: a Position read-model -- one dataclass with the
  fields the check functions actually consume (ticker, side, qty,
  entry_price, entry_prob, cost, peak_profit_pct, close_time), two
  PositionStore implementations (paper JSON, execution_log), and the
  stop-loss/breakeven/model-exit/peak logic operating solely on
  list[Position] + an injected exit(position, price, reason) callback.
  Fix update_peak_profits to actually operate on its parameter (thin
  paper wrapper keeps JSON persistence; live path calls the pure
  function), collapsing _update_live_peak_profits to persistence-only.

Why not now:
  - The constants+helper extraction is low-risk and could ride along the
    next time either file is open. The full read-model touches
    paper.py's battle-tested check functions -- exactly what the
    2026-07-13 session was avoiding -- so save it for when the next
    live-position feature (partial-fill handling is the likeliest) needs
    a third consumer to validate the interface against.

When to revisit:
  - Named constants + shared gate helper: next time either file is open
    for any reason. Full Position read-model: when the next live-position
    feature lands, using it as the third consumer that validates the
    interface.

[SIGNAL GRADUATION IS A CONVENTION, NOT A MECHANISM -- SIGNAL #3 REBUILDS RUN-TREND'S PLUMBING]
Priority: Medium -- the log-only->trigger->blend pattern is now this
  project's explicit strategy for all new alpha; its marginal cost is
  currently ~6 touch points per signal.

Problem:
  Run-trend established the graduation *pattern* but implemented it as
  bespoke wiring at every layer, so signals #3-#5 (trade-flow, AFD,
  richer-ML features are all queued in this file) each repeat it.
  Storage: one named migration per scalar (tracker.py:221-223, v40-v42)
  plus dedicated columns per signal -- predictions is already at schema
  v42 largely from this accretion. API: a new named parameter on
  log_prediction() (tracker.py:610 run_trend: dict | None) -- the
  signature grows one kwarg per signal, forever. Threading: every
  log_prediction call site updated -- order_executor's shared helper
  _prediction_kwargs_from_analysis (order_executor.py:1926) *plus*
  main.py's two direct call sites, each re-importing
  get_forecast_run_trend_from_analysis locally (see the LOG_PREDICTION
  KWARGS ASSEMBLY TRIPLICATED entry below -- same root cause).
  Enablement: run-trend's trigger (sample floor + correlation check)
  exists only as backlog.txt prose that someone must remember to run by
  hand. Meanwhile the codebase already *has* an in-code activation
  mechanism -- sample-gated auto-activation with one-time notification
  (_regime_blend_active() weather_markets.py:3887 +
  _notify_feature_activation 3852, and siblings: PDO/PNA gate at 3912,
  MIN_BIAS_CORRECTION_TRADES) -- but it's re-derived per feature and was
  not used for run-trend. Blend entry (the eventual step): the core
  blend is a *positional 3-tuple* -- _blend_weights() returns
  (w_ensemble, w_climatology, w_nws) (weather_markets.py:3954-3962). Any
  signal graduating to a real blend weight either changes that arity
  everywhere or joins the growing chain of bespoke post-blend
  adjustments, which is how analyze_trade got to 1,660 lines.

What it would look like:
  (a) log_prediction(..., signals: dict[str, float] | None) persisting to
  a signal_values(prediction_id, name, value) table (or one JSON column,
  matching the blend_sources/run_trend_points precedent) -- new signal =
  new dict key, zero migrations, zero signature/call-site changes; (b) a
  small signal registry (name, extract(analysis) -> value(s),
  sample_floor, correlation_target) driving both a standing
  graduation-report command (runs every pending signal's enablement query
  against settled outcomes -- replaces the per-entry prose triggers) and
  the auto-activation notification machinery that already exists for
  regime/PDO; (c) leave blend *wiring* manual -- the deliberate human
  step is the point of the pattern -- but let graduated signals enter as
  named entries in a weights dict rather than tuple-position N+1.

Why not now:
  - Do (a)+(b) when the *next* log-only signal is built (trade-flow is
    queued post-Belgium) -- that's when the second data point proves/
    disproves the shape, and the cost is near-zero incremental to work
    already planned. Not worth a standalone pass while run-trend's own
    trigger hasn't fired.

When to revisit:
  - When PUBLIC TRADE-FLOW SIGNAL (or any other log-only signal above) is
    next picked up -- build the registry then, not before.

[THE ANALYSIS-DICT -> TRADE-RECORD BOUNDARY EXPLODES DICTS INTO NAMED SCALARS]
Priority: Medium -- sharpens the existing GENERALIZED PER-MODEL ACCURACY
  TRACKING entry with the structural root cause; read together.

Problem:
  The existing entry frames this as "per-model columns in tracker" -- but
  the tracker column is the *last* link of a six-hop scalar chain, and
  the chain's shape is the real cost. Per-model means travel: analyze_trade
  locals (weather_markets.py:5731-5732) -> return-dict keys (6775-6776)
  -> *named kwargs* at two independent placement call sites
  (main.py:3576-3577, order_executor.py:2663-2664) -> place_paper_order's
  signature (paper.py:853-854) -> trade JSON keys (957-958) ->
  _score_ensemble_members's hardcoded dict (paper.py:1174-1187) ->
  tracker.log_member_score. The comment at paper.py:1180-1186 states the
  consequence: ECMWF "can never earn a data-driven weight... permanently
  stuck at the static seasonal default" -- and the same is true for
  GEM/UKMO/WeatherNext or any future source. place_paper_order's
  signature is itself the pattern at scale: 19 named parameters, each a
  field some analysis produced, meaning *every* new per-trade datum (not
  just model means) pays the same multi-file kwarg-threading toll -- the
  same disease as log_prediction's kwarg growth in the SIGNAL GRADUATION
  entry above, at a different boundary.

What it would look like:
  One model_means: dict[str, float] key on the analysis dict (populated
  by _get_consensus_probs, which is already model-agnostic via
  _model_prob_and_mean -- the generic helper exists at the source end),
  passed through as a single kwarg -> stored as one JSON field on the
  trade record -> iterated by _score_ensemble_members (which already
  loops a dict at paper.py:1191, so the consumer end is *also* ready --
  only the transport is scalar). Keep icon_forecast_mean/gfs_forecast_mean
  as legacy read-fallbacks for settling old trades. This converts "add a
  model source" from a 6-file signature change into a fetch + one dict
  entry, which is precisely what GENERALIZED PER-MODEL ACCURACY TRACKING
  asks for -- that entry should be read together with this one when
  picked up, since the endpoints are already generic and only the
  transport chain needs replacing.

Why not now:
  - Same as GENERALIZED PER-MODEL ACCURACY TRACKING (weeks before learned
    weights activate, needs the which-ECMWF-product decision) -- but when
    picked up, do the dict transport *first*; instrumenting ECMWF through
    the scalar chain and then generalizing would do the plumbing twice.

When to revisit:
  - Same trigger as GENERALIZED PER-MODEL ACCURACY TRACKING -- do the
    dict-transport change as step one of that entry, not a separate pass.

[ONE-SHOT PROCESS LIFECYCLE IS BAKED INTO MODULE STATE -- THE ALWAYS-ON WATCHER INHERITS STALE-STATE HAZARDS]
Priority: Medium -- gated on the same trigger as the VM move; partially
  live *today* via loop/watch, which already call cmd_cron repeatedly
  in-process.

Problem:
  Correctness currently leans on process death as the cache-invalidation
  and config-reload mechanism. Import-time snapshots that feed live
  decisions: _CITY_WEIGHTS/_SEASONAL_WEIGHTS/_CONDITION_WEIGHTS load once
  at module import (weather_markets.py:748-750); _CONDITION_WEIGHTS feeds
  every trade's blend. In an always-on process, a recalibration writing
  new weight files changes nothing until restart. _LEARNED_WEIGHTS
  (2451-2453) is "cached after first load" with a log-*once* TTL warning
  flag -- a once-per-process warning is a once-per-*months* warning on a
  VM. Config frozen at import: utils.py alone has 41 module-level
  os.getenv constants. The operational model "edit .env -> next cron run
  picks it up" silently becomes "edit .env -> nothing happens" on a
  long-lived process. Per-cycle resource lifecycles: the WS client is
  constructed, subscribed, started, and torn down per cron cycle
  (cron.py:1038-1156, stop at 2668-2674) -- every fresh cycle starts with
  an empty book cache, which is exactly what caused the HIGH bug in the
  exit-protection review (REST-fallback shape on "the exact path the
  feature was built for"). An always-on design inverts this: the WS
  becomes the persistent event source and the scan becomes the periodic
  task -- none of the current orchestration is shaped for that
  inversion. Filesystem liveness protocol: cron lock, heartbeat, dead-
  man's-switch reading .cron_last_run mtime, _MONDAY_SWEEP_PATH touch-
  file -- a signaling protocol between *invocations* of a dying process.

What it would look like:
  An explicit cycle-scoped context (fits the headless trade-cycle engine
  entry above): weights/config refreshed at cycle start (or converting
  the import-time trio to the TTL-checked read-through pattern the
  forecast caches already use); log-once flags becoming log-once-per-
  cycle; WS lifted out of the cycle to process scope with reconnect/
  heartbeat ownership; an inventory pass over module globals in
  weather_markets.py/paper.py classifying each as (a) true constant, (b)
  TTL cache -- already safe, (c) load-once snapshot -- the hazard list.
  The existing autouse-fixture machinery (isolate_dynamic_sigma etc.)
  shows the project already knows which globals are dangerous -- that
  list is roughly the (c) list.

Why not now:
  - Same trigger as the VM move. But the inventory pass (c-list) is cheap
    and worth doing *before* the watcher design starts, because it sizes
    the real work; and any *new* module-global cache added between now
    and then should be TTL-checked rather than load-once, so the list
    stops growing.

When to revisit:
  - Same trigger as MOVE OFF LOCAL CRON -- HOST ON A SMALL ALWAYS-ON VM.
    The module-global inventory pass is cheap enough to do sooner, ahead
    of the watcher design itself.

[PER-CITY KNOWLEDGE IS SCATTERED ACROSS ~8 REGISTRIES WITH SILENT-DEFAULT FALLBACKS]
Priority: Low-Medium -- the LV/NOLA history proves the failure mode is
  real and slow-burning: months of silently degraded sigma; climate_indices
  still half-covered.

Problem:
  CITY_COORDS is nominally data-driven (data/cities.json, weather_markets.py:
  190-236, "#119: ...new cities can be added without modifying code") --
  but a tradeable city actually requires coordinated entries in at least:
  _parse_city_from_ticker's hardcoded if-chain (weather_markets.py:
  3384-3445 -- city detection is *code*, including per-city substring
  workarounds like the LA/LOWTLA disambiguation), KNOWN_WEATHER_SERIES
  (3159-3199, plus KNOWN_DEAD_WEATHER_SERIES 3210), metar.
  MARKET_STATION_MAP (metar.py:306, consumed by mos.py:32 and
  settlement_monitor.py:65), _STATION_BIAS_HIGH/_LOW (weather_markets.py:
  243-300), _HISTORICAL_SIGMA (2160-2180, static fallback tier),
  climate_indices' ao/nao/enso dicts (climate_indices.py:212-251, still
  10 of 20 cities), and paper.py's correlation groups/pair-corr
  (283-332). Each consumer has a silent safe-side default for a missing
  entry (_DEFAULT_SIGMA=3.5, .get(city, 0.5), Open-Meteo fallback instead
  of ASOS) -- individually reasonable, collectively meaning an
  incompletely-onboarded city trades with quietly degraded signal quality
  and *nothing* reports the incompleteness. This is exactly how
  LasVegas/NewOrleans ran on generic sigma and remain on generic
  climate-index sensitivities. The planned city->WFO/pil table (CLI-
  report fetch + AFD entries) will be registry #9.

What it would look like:
  Not necessarily one mega-table -- a *completeness manifest*: a
  city_registry check (startup log + a test) that walks every per-city
  consumer and reports, per CITY_COORDS city, which of the ~8 registries
  has a real entry vs. a fallback, so "half-onboarded" becomes a visible
  state instead of a silent one. Second step, only if city count actually
  grows: fold the pure-data ones (station, tz, series tickers,
  ticker-match patterns) into cities.json so _parse_city_from_ticker
  becomes data-driven and #119's promise becomes true -- today adding a
  city without code changes is not actually possible.

Why not now:
  - City count has been stable at 20 and Kalshi adds cities rarely; the
    manifest/report is a small, self-contained piece worth folding into
    whichever session next touches a per-city table (the climate-indices
    or WFO-table work are natural hosts). The cities.json consolidation
    only pays if Kalshi's roster actually starts moving.

When to revisit:
  - Whenever climate_indices.py or the CLI-report/WFO table work is next
    picked up -- fold the manifest in as a drive-by rather than a
    dedicated session.

[LOG_PREDICTION KWARGS ASSEMBLY TRIPLICATED -- ONE COPY HAD ALREADY DRIFTED]
Priority: Medium-High to fix (trivial), Medium as debt -- drift is not
  hypothetical, it already happened once (main.py's cmd_market call site
  silently dropped model_consensus, found and fixed 2026-07-16) and skews
  the very analytics (per-signal-source scoring) shadow-logging exists to
  keep honest.

Problem:
  order_executor._prediction_kwargs_from_analysis (order_executor.py:
  1926-1954) was built 2026-07-16 explicitly so real placement and shadow
  logging derive metadata identically -- but main.py's two direct
  log_prediction call sites remained hand-copies of the same assembly
  (main.py:1164-1189, main.py:3605-3629). The cmd_market copy was
  missing model_consensus=analysis.get("model_consensus") until this
  same 2026-07-16 session's Fable review caught and fixed it -- so
  shadow rows logged via cmd_market had been silently recording
  model_consensus=NULL while rows from every other path recorded a real
  value. Each copy also re-derives _std * _std and re-imports
  EDGE_CALC_VERSION/get_forecast_run_trend_from_analysis independently,
  so the next new prediction column (schema grew v39->v42 this week
  alone) must still be added in three places by hand.

What it would look like:
  Move _prediction_kwargs_from_analysis to tracker.py, next to
  log_prediction (its natural home -- it already only builds tracker
  kwargs) or export it from order_executor; replace both main.py
  hand-copies with a call plus their per-site extras (is_shadow=True for
  the display path).

Why not now:
  - No real blocker -- this is small enough to ride along with any
    main.py session. Only care point: log_prediction fires post-
    placement for real trades, so keep the helper's run_trend fetch out
    of any pre-placement path (2026-07-16's own review finding on the
    signal itself).

When to revisit:
  - Next time main.py is open for any reason -- the model_consensus
    instance is already fixed; this entry is about preventing the next
    one structurally.

[DISPUTED-ROW EXCLUSION PREDICATE HAND-COPIED ~40 TIMES IN tracker.py]
Priority: Medium -- same "one shared rule, many consumers, one gets
  missed" shape this project has hit 7+ times; the existing mutation
  tests protect the 30 *existing* filters but nothing forces a *new*
  query to add one.

Problem:
  The 2026-07-12 disputed-outcome restoration added the literal predicate
  `AND (o.disputed IS NULL OR o.disputed = 0)` as a hand-copied string to
  every calibration query: tracker.py:838, 987, 1082, 1155, 1198, 1228,
  1263, 1320, 1416, 1461, 1634, 1720, 1775, 1798, 1857, 1896, 1934, 1978,
  2113, 2190, 2222, 3517, 3589, 3651, 3700, 3775, 3988, 4119, 4160, 4629,
  4772 -- ~40 occurrences. The 30-filter mutation test proves existing
  filters work, but the failure mode that matters going forward is a NEW
  function joining outcomes on settled_yes and simply not knowing the
  rule exists -- exactly how the original 3-function scope grew to 30 in
  the first place. tracker.py already has a CREATE VIEW precedent
  (multiday_predictions, tracker.py:372), so a view is in-style, and
  there is no outcomes_valid view or shared SQL-fragment constant
  anywhere (grep confirmed).

What it would look like:
  Either (a) a `CREATE VIEW IF NOT EXISTS outcomes_valid AS SELECT * FROM
  outcomes WHERE disputed IS NULL OR disputed = 0` in init_db(), migrate
  the 40 queries to join the view (audited consumers like count_settled_*
  keep joining raw outcomes deliberately); or cheaper (b) a module
  constant `_NOT_DISPUTED = "AND (o.disputed IS NULL OR o.disputed = 0)"`
  interpolated into each query, plus a guard test in the
  test_config_divergence_guard.py style: regex tracker.py for `JOIN
  outcomes` / `FROM outcomes` and assert every hit either uses the
  view/constant or is on a named allowlist (the deliberate raw consumers
  from the 07-12 audit).

Why not now:
  - 40 query rewrites in the calibration-scoring core deserve their own
    pass with the existing 30-filter test suite as the safety net (it
    will fail loudly if a view/constant swap changes semantics). No
    behavior change intended, so it can wait for a slow session -- but
    should land before the next tracker.py analytics function is written
    by someone unaware of the rule.

When to revisit:
  - Next time tracker.py is opened for analytics work, or the next time
    a new settled_yes-consuming function is added without the filter
    (repeat of the original discovery pattern).

[ANALYZE-TRADE PRICING/EV/KELLY TAIL TRIPLICATED ACROSS TEMP/PRECIP/SNOW PATHS]
Priority: Medium -- this block has already needed the same fix applied to
  every copy at least three times (the NO-sign inversion, the maker-fee
  migration), and it contains a fresh instance of the shadow-constant bug
  class the config guard can't see.

Problem:
  The ~60-line tail (entry-price selection with the yes_ask / 1-yes_bid
  fallback chain -> net_ev -> net_edge cap 3.0 -> entry_side_edge NO-side
  fallback -> fee-adjusted kelly -> bayesian_kelly with NO-side CI flip
  -> CI-width scale `max(0.25, 1 - width*2)` -> condition_type scale ->
  cap) exists three times in weather_markets.py: precip path 4688-4757,
  snow path 4893-4964 (its own comment says "see _analyze_precip_trade's
  identical fix above" -- the copies are acknowledged in-source), and a
  divergent temperature variant 6521-6691 (adds time-decay, quality/
  anomaly/spread/bimodal scales the other two lack). Every historical fix
  to this logic had to be applied per-copy. Separately: the final Kelly
  cap is a naked `min(ci_adj_kelly, 0.25)` at 4757, 4964, and 6691 (0.33
  consensus variant at 6689) -- utils.KELLY_CAP (utils.py:105) is
  env-configurable, so setting KELLY_CAP in .env changes kelly_fraction's
  cap elsewhere but NOT these three ci_adjusted_kelly caps. That's a new
  instance of the MORE INERT config.py FIELDS divergence class in a spot
  test_config_divergence_guard.py doesn't scan (it compares env-default
  literals between config.py/utils.py, not naked literals in
  weather_markets.py).

What it would look like:
  Extract a `_price_and_size(blended_prob, prices, condition, days_out,
  *, extra_kelly_scales=(), ci=(lo,hi), time_decay=1.0) -> dict` helper
  returning the shared keys (entry_price, rec_side, net_ev, net_edge,
  edge, entry_side_edge, fee_kel, ci_adj_kelly, adjusted_edge); precip/
  snow call it plainly, the temperature path passes its extra scale
  factors and decay. Replace the three 0.25 literals with utils.KELLY_CAP
  (current value identical -> no behavior change), and decide
  deliberately whether 0.33-consensus should be KELLY_CAP*1.33 or stay
  independent.

Why not now:
  - This is the EV/sizing core of every trade -- needs the full
    blast-radius treatment (the PAPER_MIN_EDGE precedent), golden-value
    regression tests pinning current outputs for all three paths before
    extraction, and an opus review per the 8-point checklist. Not a
    drive-by.

When to revisit:
  - Worth its own dedicated session given the blast radius -- not a
    drive-by addition to unrelated work.

[KALSHI CENTS/DOLLARS PRICE NORMALIZATION IMPLEMENTED INDEPENDENTLY 3+ TIMES]
Priority: Medium -- this exact normalization has already produced two real
  bugs fixed one-copy-at-a-time (the zero-bid falsy bug in
  parse_market_price; the 2026-07-13 HIGH bug where the REST-fallback raw
  shape was read as dollar floats), and Kalshi is actively migrating
  shapes, so every new API shape must be taught to each copy separately.

Problem:
  order_executor._coalesce_cents_or_dollars (order_executor.py:106-126)
  and the nested _coalesce + to_float pair inside
  weather_markets.parse_market_price (weather_markets.py:3089-3113)
  implement identical logic (None-skipping coalesce; str->float/100 if
  >1; int>=1 -> /100; float>1.0 -> /100), with the order_executor
  docstring admitting "Mirrors weather_markets.parse_market_price's
  coalesce so callers agree" -- a mirror enforced by nothing. The
  weather_markets copy is nested inside the function so it *can't* be
  imported, which is why the duplicate exists. schema_validator.py:34-61
  carries a third partial variant of the same legacy/current key-pair
  knowledge, tracker.py:926's _candle_dollars handles the candlestick
  flavor ("_dollars"/"_fp"), and kalshi_ws.py's ticker parser normalizes
  to dollars independently (its 0.0-missing-side sentinel caused the
  2026-07-12 one-sided-book review finding).

What it would look like:
  One `coalesce_market_price(d, *keys) -> float` in market_types.py (or
  utils.py), used by parse_market_price (un-nest), order_executor
  (delete its copy), and schema_validator's range check; consolidate the
  key-pair lists ("yes_bid"/"yes_bid_dollars" etc.) as constants next to
  it. Existing tests become tests of the one helper. kalshi_ws's
  parse-time normalization can stay (different input shape) but should
  reference the same constants.

Why not now:
  - Touches live order pricing -- same review bar as the reprice work.
    Best done adjacent to the next Kalshi-API-shape change (e.g. if the
    V2 order-endpoint response migration surfaces more _fp fields), when
    the copies would otherwise all need editing anyway.

When to revisit:
  - The next time any Kalshi API response shape changes -- do the
    consolidation then, since the copies need editing anyway.

[TWO FUNCTIONS NAMED _current_forecast_cycle -- ONE IS DEAD AND HIDES FROM THE DEAD-CODE GUARD VIA NAME COLLISION]
Priority: Low-Medium -- small cleanup, but it exposes a systematic blind
  spot in tests/test_dead_code_scan.py worth closing while it's cheap.

Problem:
  order_executor.py:78-87 defines _current_forecast_cycle -> 2 cycles/day,
  date-prefixed ("2025-05-15_12z"), used everywhere real (order dedup,
  execution_log rows, all log_prediction forecast_cycle values via
  main.py:68's import). weather_markets.py:819-832 defines a *different*
  _current_forecast_cycle -> 4 cycles/day, bare "12z". The
  weather_markets one has zero production callers (grep: only
  tests/test_forecasting.py:398,414 import it; its neighbor
  _ttl_until_next_cycle is the one weather_markets actually uses). Two
  functions with the same name and the same conceptual job but
  incompatible formats is exactly the "same concept represented
  differently" trap -- and worse, the dead-code guard can't see it: the
  scan is regex-over-source (\bname\s*\(, per its own docstring), so
  order_executor's five call sites of its *own* _current_forecast_cycle
  make the weather_markets one look called. Any future dead function that
  happens to share a name with a live one in another scanned file gets
  the same free pass.

What it would look like:
  Delete weather_markets._current_forecast_cycle (or, if the 4-cycle
  granularity is genuinely wanted someday for cache keys, rename to
  _current_nwp_cycle and leave a comment distinguishing it from
  order_executor's dedup cycle); move/retarget its two tests. Then teach
  the scan to resolve calls per-module (attribute the call to the
  defining module of the name in scope -- the scan already does
  alias-resolution for `from module import name as _name`, so plain
  same-file definitions are the remaining gap).

Why not now:
  - None substantive -- candidate for the next housekeeping session. The
    scan improvement is the part worth doing carefully (avoid regressing
    the existing alias-resolution).

When to revisit:
  - Next general housekeeping/cleanup session.

[ForecastCache EXISTS, BUT ~14 HAND-ROLLED TTL DICTS DO THE SAME JOB]
Priority: Low-Medium -- consistency/tooling debt, not a live bug; each
  hand-rolled cache re-implements expiry slightly differently and each
  needs its own test-isolation hook.

Problem:
  forecast_cache.py's ForecastCache is the purpose-built abstraction
  (TTL, per-entry set_with_ttl, used by weather_markets._ensemble_cache:398,
  _forecast_cache:505, tracker._run_trend_cache:2715 (new 2026-07-16),
  config._paper_min_edge_cache:158). But the majority of caches hand-roll
  the dict[key, (value, ts)] + `time.monotonic() - ts < TTL` idiom:
  weather_markets._NBM_CACHE/_ECMWF_CACHE/_PRECIP_ENSEMBLE_CACHE
  (1610-1613), _HRRR_CACHE (1705), _WEATHERAPI_CACHE (1775),
  _MAE_WEIGHTS_CACHE (2644), _CONSENSUS_CACHE (4288), _DYNAMIC_BIAS_CACHE
  (166); mos.py:59 _MOS_CACHE; metar.py:79 _METAR_CACHE; nws.py:44-49
  (five separate dicts); climate_indices.py:29; climatology.py:52,252;
  ml_bias.py:29-31. Variations that already exist between copies:
  monotonic vs wall-clock timestamps, negative-result caching (the new
  _run_trend_cache added its own separate 30-min negative TTL -- a
  feature every fetch cache arguably wants, implementable once in the
  class), and per-cache clear() fixtures in tests (conftest.py:108 clears
  _METAR_CACHE by hand; the climatology isolation fixture is another
  one-off).

What it would look like:
  Migrate hand-rolled caches to ForecastCache opportunistically (whichever
  file is open anyway), starting with weather_markets' five model caches
  since they share one TTL constant already (_MODEL_CACHE_TTL, 1614). Add
  optional negative-TTL support to ForecastCache once (generalizing the
  run-trend pattern). A single clear_all_forecast_caches() registry would
  collapse the per-cache conftest fixtures.

Why not now:
  - Zero functional urgency; purely a convention to adopt going forward
    plus opportunistic migration. Worth stating the convention in-code
    (forecast_cache.py docstring: "new TTL caches use this class") so the
    count stops growing.

When to revisit:
  - Opportunistically, whenever any of the listed files is next open for
    other reasons.

[execution_log.py's SWALLOWED-ALTER MIGRATIONS vs tracker.py's VERSIONED IDIOM]
Priority: Low-Medium -- worked fine so far, but the live-execution feature
  stream (7 new columns across 07-12/07-13 alone) is growing the weaker
  idiom fastest, and its failure mode is silent.

Problem:
  tracker.py migrations are versioned (_MIGRATIONS list + PRAGMA
  user_version cursor, tracker.py:30-255) with guard tests asserting
  _SCHEMA_VERSION == len(_MIGRATIONS) -- and this exact machinery just
  caught and self-healed a real drift on 2026-07-16 (see the FORECAST
  RUN-TO-RUN TREND SIGNAL entry above). execution_log.py instead
  re-attempts a flat list of 16 ALTER statements on every process init
  inside `except sqlite3.OperationalError: pass` (execution_log.py:
  106-129). Structural problems: (a) a genuine OperationalError (locked
  DB, disk error) on a *needed* ALTER is indistinguishable from
  "duplicate column" and is silently swallowed -- the column just doesn't
  exist and the first INSERT referencing it fails later, far from the
  cause; (b) no version cursor means no way to write a data-backfill
  migration (only additive ALTERs are expressible); (c) every new
  live-execution feature grows the list, so the two files' migration
  styles diverge further with each shipped entry.

What it would look like:
  Port execution_log to the tracker pattern: freeze the current 16
  ALTERs + CREATE TABLEs as migrations 1..N, set user_version, add the
  same len==version guard test. Or extract tracker's ~25-line runner
  (_run_migrations) into a shared helper both files use.

Why not now:
  - The current code works and live=1 rows are precious-few (no live
    orders yet) -- ideal time to do it is actually now-ish, before live
    data exists to migrate carefully, but it's not urgent by impact.
    Needs care around the double-checked-locking init
    (execution_log.py:52-62).

When to revisit:
  - Sooner rather than later, specifically because live=1 rows are still
    empty -- this is the easiest this migration will ever be.

[utils.utc_today() SAYS "USE EVERYWHERE INSTEAD OF date.today()" -- 17 SITES STILL DON'T]
Priority: Low-Medium -- a recognized, already-bitten bug class with a
  stated convention and zero enforcement. One instance (this session's own
  new code) was found and fixed 2026-07-16; the remaining 17 are
  unaudited.

Problem:
  utils.py:15-17 defines utc_today() with the docstring "Use everywhere
  instead of date.today()", and the codebase has two documented scars
  from ignoring it: web_app.py:3082's WA-timezone comment ("was
  date.today() (server-local calendar)...") and the 07-13 test failures
  where fixtures built from local date.today() disagreed with
  simulate_portfolio's utils.utc_today() frame. tracker.py:2740 (the
  run-trend fetcher shipped 2026-07-16) was the same bug -- confirmed and
  fixed same-session, since forecast_days arithmetic against a
  UTC-anchored target_date was computed from the server-local calendar.
  Production date.today() survives at 17 more sites: tracker.py:2663
  (the sibling _fetch_previous_run_daily, pre-existing, not touched this
  session); backtest.py:156, 402, 407, 749, 794; main.py:688, 809, 2373,
  3013, 5578; cron.py:425-442; web_app.py:1992; climate_indices.py:128;
  climatology.py:72; cloud_backup.py:165. Some are plausibly fine
  (cloud_backup's 30-day retention doesn't care), but nothing
  distinguishes "audited, local is fine" from "nobody looked."

What it would look like:
  A guard test in the established style: grep production modules for
  date.today()/_date.today(), fail on any hit not in an allowlist with a
  per-site reason (mirroring _DEAD_FIELD_ALLOWLIST's honest-
  categorization approach). Audit the 17 sites once to populate it --
  tracker.py:2663 should probably just switch to utc_today() too, given
  its sibling function just proved the mismatch is real on this exact
  machine (Belgium, UTC+2).

Why not now:
  - The audit is a half-session of judgment calls, not mechanical. Worth
    pairing with any tracker.py or backtest.py session.

When to revisit:
  - Next tracker.py or backtest.py session -- pair the audit with
    whatever else brings that file open.

Related micro-item, same new code: _fetch_previous_run_daily (tracker.py:
  2646-2701) is now the single-lead, past-date special case of
  _fetch_previous_run_leads (2720-2781) -- same endpoint, same parse loop,
  differing only in past_days vs forecast_days param. Reimplementing the
  former as a thin wrapper over the latter would remove ~40 duplicated
  lines and make the past/future asymmetry live in exactly one place;
  the leads variant already has the stricter malformed-response guards,
  so the wrapper direction is safe. Fold into whichever of the two
  entries above (utc_today audit, or a standalone drive-by) touches this
  function next.
[DEAD TAKE-PROFIT PLUMBING STILL RUNS EVERY WATCH CYCLE AFTER THE FEATURE WAS DECLINED]
Priority: Low -- vacuous no-op, but it's a decided-against feature whose
  machinery still executes, prints user-facing messaging, and carries
  tests; the dead-code guard can't flag it because it *is* called.

Problem:
  The 2026-07-13 resting-exits research established that nothing in the
  repo ever sets exit_target ("fully dead -- threaded through
  place_paper_order's signature and checked by check_exit_targets(), but
  ... none of them ever actually sets it") and the user explicitly
  declined building take-profit. The plumbing was left in place:
  paper.py:849 (parameter), 866 (docstring), 955 (stored on every trade
  as None), check_exit_targets (paper.py:1561-1603, filters to
  exit_target is not None -> always empty), and main.py:2908-2922 calls
  it every cmd_watch cycle with a green "[Auto-exit] ... reached
  take-profit target" print that can never fire. Four test classes
  (tests/test_paper.py:880-1039) keep the dead path green. Invisible to
  test_dead_code_scan.py because the function has a real caller -- the
  deadness is in the data (no producer of the field), a shape the guard
  doesn't model.

What it would look like:
  Per this repo's wire-up-or-delete convention: delete the exit_target
  param from place_paper_order, the stored field (leave old JSON rows'
  key harmlessly present), check_exit_targets, the main.py call site, and
  the four tests -- OR keep it with a one-line comment at
  check_exit_targets stating "no producer sets exit_target by design
  (take-profit declined 2026-07-13, see RESTING EXIT ORDERS entry
  above); retained for manual/dashboard use" if a manual-set path is
  actually wanted (the web_app manual-close route at web_app.py:2862 is
  the closer fit for that use case, so probably: delete).

Why not now:
  - None -- pure cleanup, candidate for any small session. Only decision
    needed is delete vs. document, which the user should make (matches
    the "ask before scope decisions" checklist item).

When to revisit:
  - Any small housekeeping session; needs a one-time user decision
    (delete vs document) before touching code.

